# AI 室内设计 V0.2：平面图结构化生效果图技术方案

> 文档版本：V0.2
>
> 编写日期：2026 年 7 月 30 日
>
> 文档状态：开发实施稿
>
> 样例输入：`example/平面图.jpeg`

---

## 1. 版本目标

V0.1 已经验证以下后半段链路：

```text
结构化房间参数
→ Blender 基础场景与真实相机
→ 本地可替换写实增强适配器
→ 效果图与结构检查结果
```

V0.2 增加从普通平面布局图进入该链路所缺失的桥梁：

```text
上传平面图
→ 比例标定
→ 候选墙线识别
→ 人工取舍与补画
→ 目标房间框选
→ 结构化户型 JSON
→ 全屋俯视白模
→ 目标房间透视预览
→ 写实增强效果图
```

本版本的产品目标不是实现任意图纸的全自动识别，而是让用户可以在一张清晰、规则的住宅平面图上，以少量人工确认完成一次可追溯的三维和效果图生成任务。

---

## 2. MVP 边界

### 2.1 支持范围

- PNG、JPG、JPEG、WEBP 平面图；
- 单层普通住宅；
- 以水平、垂直直墙为主；
- 用户输入户型实际总宽、总深完成比例标定；
- OpenCV 自动提取水平、垂直候选墙线；
- 用户可以保留、删除候选墙线；
- 用户可以在图上拖动补画墙线；
- 用户可以框选一个目标房间；
- 默认墙厚、层高可以修改；
- 输出结构化户型 JSON；
- 根据确认墙线生成全屋俯视 Blender 白模；
- 根据房间框选生成一个室内安全机位；
- 输出房间基础渲染和一张本地增强效果图。

### 2.2 不支持范围

- 不承诺从任意低清照片、手绘图或 CAD 截图全自动恢复结构；
- 不自动判断承重墙；
- 不自动识别水电、烟道、燃气和施工构造；
- 不恢复复杂曲墙、楼梯、复式或错层；
- 不承诺自动识别所有门窗；
- 不从二维图推断准确窗台高、窗高和门高；
- 不生成施工图、材料清单、工程预算或正式报价；
- 不承诺效果图中的家具品牌和真实 SKU；
- 不把图像生成结果作为空间结构真值。

---

## 3. 样例图分析

`example/平面图.jpeg` 的原始文件内容为 1075 × 737 PNG 图像，文件扩展名为 `.jpeg`。图中具备：

- 清晰的矩形外轮廓；
- 两间卧室、卫生间、厨房和客餐厅区域；
- 完整的尺寸标注；
- 门扇开启圆弧；
- 家具、洁具和厨具轮廓；
- 以水平、垂直墙线为主。

该样例适合作为 V0.2 固定测试用例，但自动识别存在以下干扰：

- 尺寸线与墙线均为黑色直线；
- 床、柜、餐桌、沙发和洁具也包含长直线；
- 门洞会使墙线中断；
- 文字和尺寸数字会产生短边缘；
- 图纸外侧标注会扩大视觉包围盒。

因此 V0.2 采用“自动建议 + 用户确认”，不把候选墙线直接写入三维场景。

---

## 4. 用户流程

### 4.1 上传与比例标定

用户上传平面图后填写：

- 户型实际总宽，样例建议值为 `8150 mm`；
- 户型实际总深，样例建议值为 `6060 mm`；
- 默认墙厚，首版默认 `100 mm`；
- 层高，首版默认 `2800 mm`。

系统先检测主要图形包围盒，再分别计算：

```text
scaleX = planWidthMm / detectedBounds.widthPx
scaleY = planDepthMm / detectedBounds.heightPx
```

当两个比例差异超过 5% 时提示用户检查输入尺寸或包围盒，不静默拉伸。

### 4.2 墙线确认

系统显示原图和候选墙线叠加层：

- 蓝色：已保留的自动候选墙线；
- 灰色：已取消的候选墙线；
- 橙色：用户手动补画墙线；
- 绿色矩形：目标房间范围。

用户可以：

- 单击候选墙线切换保留状态；
- 使用“补画墙体”模式拖出一条水平或垂直墙线；
- 撤销最近一次补画；
- 清除全部补画墙线；
- 查看保留墙线数量和自动识别置信度。

### 4.3 房间框选

用户进入“框选房间”模式，在原图上拖动一个矩形。V0.2 只要求一个矩形目标房间，用于生成室内相机，不支持任意多边形房间编辑。

样例首选客餐厅区域。房间框选至少满足：

- 宽度不小于 `2400 mm`；
- 深度不小于 `2400 mm`；
- 完全位于检测到的户型包围盒内。

### 4.4 三维与效果图

用户确认后创建一个异步场景任务，任务依次输出：

1. 全屋俯视白模；
2. 目标房间室内透视预览；
3. 一张固定风格的设计意向图；
4. 结构检查结果。

---

## 5. 结构化数据契约

### 5.1 平面图分析结果

```json
{
  "schemaVersion": "0.2",
  "sourceImageUrl": "/artifacts/floorplan.png",
  "imageWidth": 1075,
  "imageHeight": 737,
  "planWidthMm": 8150,
  "planDepthMm": 6060,
  "scaleX": 8.9857,
  "scaleY": 9.7271,
  "detectedBounds": {
    "x": 82,
    "y": 51,
    "width": 907,
    "height": 623
  },
  "wallCandidates": [
    {
      "id": "wall_auto_001",
      "x1": 96,
      "y1": 67,
      "x2": 985,
      "y2": 67,
      "orientation": "horizontal",
      "thicknessPx": 4,
      "confidence": 0.96,
      "source": "auto"
    }
  ],
  "quality": {
    "candidateCount": 80,
    "orthogonalRatio": 1.0,
    "scaleDeltaRatio": 0.0762,
    "scaleWarning": true,
    "requiresUserConfirmation": true
  }
}
```

上述数值是当前算法对固定样例的实际输出。候选数包含家具与尺寸标注造成的干扰线，因此前端默认只启用高置信候选，并要求人工确认。

坐标使用原图像素坐标：

- 原点位于图片左上角；
- `+X` 向右；
- `+Y` 向下；
- 所有候选墙线均吸附为水平或垂直；
- 进入三维前再转换为毫米和 Blender 坐标。

### 5.2 确认后的场景输入

```json
{
  "schemaVersion": "0.2",
  "sourceImageUrl": "/artifacts/floorplan.png",
  "imageWidth": 1075,
  "imageHeight": 737,
  "planWidthMm": 8150,
  "planDepthMm": 6060,
  "ceilingHeightMm": 2800,
  "wallThicknessMm": 100,
  "detectedBounds": {
    "x": 82,
    "y": 51,
    "width": 907,
    "height": 623
  },
  "walls": [
    {
      "id": "wall_auto_001",
      "x1": 96,
      "y1": 67,
      "x2": 985,
      "y2": 67,
      "source": "auto"
    }
  ],
  "roomSelection": {
    "x": 95,
    "y": 325,
    "width": 770,
    "height": 345
  },
  "roomName": "客餐厅",
  "stylePresetId": "modern_warm_v1",
  "cameraPresetId": "corner_02",
  "useBlender": true
}
```

### 5.3 坐标转换

对检测包围盒中的像素点：

```text
xMm = (xPx - bounds.x) × planWidthMm / bounds.width
yMm = (bounds.y + bounds.height - yPx) × planDepthMm / bounds.height
```

进入 Blender：

```text
blenderX = xMm / 1000 - planWidthM / 2
blenderY = yMm / 1000 - planDepthM / 2
blenderZ = heightMm / 1000
```

墙线使用中心点、长度、墙厚和墙高生成长方体，不将图像生成模型输出反写为结构。

---

## 6. 后端技术方案

### 6.1 新增接口

#### `POST /v1/floorplans/analyze`

请求类型：`multipart/form-data`

字段：

- `source_image`
- `plan_width_mm`
- `plan_depth_mm`
- `project_id`，可选

返回：统一 `JobRead`，任务类型为 `FLOORPLAN_ANALYZE`。

#### `POST /v1/floorplan-scenes`

请求类型：`application/json`

请求体：确认后的场景输入。

返回：统一 `JobRead`，任务类型为 `FLOORPLAN_SCENE`。

### 6.2 图像分析流程

```text
读取图片
→ 灰度化与轻度高斯模糊
→ Canny 边缘
→ 概率霍夫线检测
→ 水平／垂直角度过滤
→ 最短长度过滤
→ 共线线段合并
→ 平行双边缘合并为墙体中心线
→ 图形主体包围盒估计
→ 置信度计算
→ 输出候选墙线与叠加预览
```

候选墙线不直接等同于墙体。置信度主要由以下因素组成：

- 线段长度；
- 线段附近黑色像素密度；
- 是否位于主体包围盒内。

### 6.3 失败条件

- 图片无法读取；
- 检测不到有效主体包围盒；
- 实际宽或深超出 `2400～30000 mm`；
- 候选墙线少于 4 条；
- 用户确认墙线少于 4 条；
- 目标房间尺寸小于最小值；
- 房间框选超出户型包围盒；
- Blender 调用超时。

Blender 不可用时返回 OpenCV 俯视图和基础透视兜底图，但明确标记供应方为 `local-floorplan-fallback`。

---

## 7. Blender 场景方案

### 7.1 全屋俯视白模

- 根据确认墙线创建墙体立方体；
- 创建覆盖户型外接矩形的地板；
- 使用正交相机从上方观察；
- 墙体统一浅灰色；
- 地面使用暖白色；
- 输出 1024 × 768 PNG；
- 同时保存 `.blend` 文件用于调试。

### 7.2 房间室内预览

- 根据房间矩形计算中心和安全边距；
- 相机位于矩形一角内部；
- 相机高度默认 `1600 mm`；
- 目标点位于房间中心、高度 `900 mm`；
- 使用 24～28mm 等效广角；
- 保留全屋墙体，但在相机近裁剪和可见性上使用安全设置；
- 仅在相机渲染中隐藏紧贴镜头的遮挡墙段，对象仍保留在 `.blend` 场景中；
- 输出 1024 × 768 PNG。

V0.2 不自动生成完整家具资产。室内预览以墙体、地面和简单体块为主，家具语义恢复放到后续版本。

### 7.3 写实增强

复用 V0.1 生成适配层：

- Blender 结果是结构控制输入；
- 风格预设控制颜色和氛围；
- 本地适配器仅验证流程；
- 后续生成供应商不改变平面图和场景数据协议。

---

## 8. 前端工作台

新增“平面图结构化”页面，采用左右工作台：

```text
左侧：上传、尺寸、墙厚、层高、模式和任务按钮
右侧：原图、候选墙线、补画、房间框选和三类结果
```

### 8.1 编辑状态

```ts
type EditorMode = "review" | "draw-wall" | "select-room"

type EditableWall = {
  id: string
  x1: number
  y1: number
  x2: number
  y2: number
  confidence: number
  source: "auto" | "manual"
  enabled: boolean
}
```

所有编辑均发生在原图像素坐标中，SVG 使用原图尺寸作为 `viewBox`，避免页面缩放改变结构数据。

### 8.2 首版交互

- 单击自动墙线：切换启用；
- 补画墙体：拖动起点到终点，自动吸附为水平或垂直；
- 框选房间：拖动生成矩形；
- 撤销：仅撤销最近一次补画；
- 重置：恢复分析结果；
- 生成结构化场景：提交当前启用墙线和房间框选。

---

## 9. 质量检查

场景任务返回：

```json
{
  "structureCheck": {
    "passed": true,
    "wallCount": 24,
    "scaleConsistent": true,
    "roomInsideBounds": true,
    "cameraInsideRoom": true,
    "requiresUserConfirmation": true
  }
}
```

V0.2 的 `passed` 只表示机器可验证条件通过，不表示建筑或施工正确。

---

## 10. 验收标准

以 `example/平面图.jpeg` 和至少 9 张同类清晰直墙平面图作为测试集：

| 指标 | V0.2 门槛 |
|---|---:|
| 图片上传成功率 | 100% |
| 主体包围盒可用率 | ≥ 90% |
| 返回至少 4 条墙线候选 | ≥ 90% |
| 比例标定差异提示正确率 | 100% |
| 用户可以删除错误候选 | 100% |
| 用户可以补画墙线 | 100% |
| 用户可以框选目标房间 | 100% |
| 结构化 JSON 可重复生成 | 100% |
| 全屋俯视白模任务成功率 | ≥ 90% |
| 目标房间相机位于框选区域内 | 100% |
| Blender 不可用时返回兜底结果 | 100% |
| 样例图完整端到端任务 | 必须通过 |

人工验收重点：

- 外墙和主要隔墙能通过候选线与补画完成确认；
- 全屋白模的长宽比例与输入一致；
- 客餐厅机位在目标房间内部；
- 写实增强前后主要墙体关系不发生变化；
- 页面明确显示“需要用户确认”和“非施工依据”。

---

## 11. 开发顺序

### 阶段一：文档与数据契约

- 固化请求、响应和坐标规则；
- 将样例图纳入固定测试输入；
- 确定 V0.2 非目标。

### 阶段二：后端

- 平面图候选墙线处理器；
- 分析任务接口；
- 确认场景接口；
- Blender 全屋脚本；
- OpenCV 兜底；
- 自动化测试。

### 阶段三：前端

- 上传与比例表单；
- SVG 墙线叠加；
- 墙线启用/禁用；
- 补画墙线；
- 房间框选；
- 场景结果展示。

### 阶段四：端到端验收

- 使用 `example/平面图.jpeg`；
- 生成分析结果；
- 确认墙线与客餐厅范围；
- 生成全屋俯视白模；
- 生成房间预览和效果图；
- 核对 PostgreSQL 任务记录和输出文件。

---

## 12. 后续版本

V0.2 验证成功后，再按以下顺序增强：

1. 门窗候选识别与人工编辑；
2. 闭合房间多边形和房间语义；
3. OCR 尺寸读取与多段比例校验；
4. 家具图标识别和资产映射；
5. 非矩形房间和斜墙；
6. 多房间批量机位；
7. 正式受控生成模型接入；
8. 场景版本、人工修改历史和多人协作。

---

## 13. 发布结论

V0.2 的核心交付不是“任意平面图一键生成漂亮图片”，而是建立一个可见、可改、可验证的空间数据入口。

只有用户确认后的结构化墙线和房间范围可以进入 Blender；任何写实增强结果都不能覆盖结构真值。该原则保证后续自动识别、生成模型和资产系统可以独立升级，而不会破坏项目数据和用户已经确认的空间关系。
