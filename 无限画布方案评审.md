# 《无限画布改造方案》评审报告

> 评审日期：2026-08-04
> 评审对象：[无限画布改造方案.md](无限画布改造方案.md)（152 行）
> 对照材料：[优化计划.md](优化计划.md)、用户指定的 5 张风暴AI 参考截图、仓库实际源码
> 方法：6 个维度并行源码审计 → 每条发现由独立 agent 对抗性复核 → 综合
> 规模：13 个 agent，177 万 token，495 次工具调用，耗时 71 分钟
> 结果：**72 条发现，66 条通过验证，6 条被推翻**

---

## 评审维度

| # | 维度 | 发现数 | blocker | 被推翻 |
|---|---|---|---|---|
| 1 | 后端就绪度：「后端基本不用动、90% 在前端」是否成立 | 8 | 5 | 0 |
| 2 | 前端架构与技术选型：自研 CanvasViewport vs React Flow | 11 | 5 | 0 |
| 3 | 状态时序契约：自由画布会破坏哪些不变量 | 13 | 6 | 0 |
| 4 | 参考图能力缺口：5 张截图 vs 方案第 6 章覆盖度 | 15 | 8 | 2 |
| 5 | 工期与排期：与《优化计划.md》的相互影响 | 13 | 5 | 3 |
| 6 | 前提质疑与替代方案：画布是否适合这个受约束的流水线 | 12 | 4 | 1 |
| | **合计** | **72** | **33** | **6** |

> 「被推翻」指初审 agent 提出、但对抗性验证 agent 复核源码后判定不成立的条目。
> 它们保留在附录原位并标注驳回理由，以免日后重复讨论。

---

## 1. 结论

**结论：需要返工（needs rework），但不是推翻——第 1、3、5、6 章的产品判断是对的，错的是第 2 章的技术前提和第 4 章由它推导出的工期。**方案的承重梁是第 53 行「后端谱系已经是画布就绪的，这次改造 90% 在前端」，这句话在四个独立维度上都不成立：一个 job 只有一行资产（`models.py:92-96` 的 `job_id unique=True`），阶段 01 根本不产生资产（`assets.py:44-57` 无 `FLOORPLAN_ANALYZE`），没有任何按项目取资产的接口（`main.py:830-878` 无 `project_id` 参数），以及画布最核心的「分叉」被后端主动 409 拒绝（`main.py:979-986`）。

**最重要的一条修正**：把第 53 行整句删掉，在第 4 章前面插入一个 6-8 人日的后端前置工作流（W0），并且在动任何画布代码之前先拍板「variant 粒度审批 / 分叉」这一个决策——它决定画布是一张真正的工作地图，还是一个更好看的向导。

---

## 2. 方案对的地方

这些判断我核对过源码，是对的，不要因为下面的批评而改掉：

- **第 63-65 行否决 canvas 自渲染、选 DOM+CSS Transform 的方向是对的**。节点内含 P1-P4 坐标输入、红笔标注、审批按钮、`BatchProgress` 进度条，全是富交互 DOM。单 `<canvas>` 方案要重写全部交互，判断准确。
- **第 102 行「阶段 01 保持全屏专注编辑，不塞进节点」是对的**。`FloorplanModule.tsx` 是 3094 行、内部约 30 个本地 state 的 Konva/SVG 重型编辑器，塞进 68% 缩放的画布节点里既不可用也不可维护。
- **第 100 行「画布为主、向导为导航，旧入口保留一段时间」是对的**，而且比方案自己意识到的更重要——它是唯一能让这次改造分批上线的策略。
- **第 149 行「增量发布的骨架卡片天然变成骨架节点」在呈现层是对的**。`jobs.py:141-152` 的 `publish_progress` 已落地，`BatchProgress.tsx:6-13` 已经在用 `job.payload.variants` 算期望数量、`AiDesignWorkflow.tsx:1096-1113` 已经在渲染骨架卡。这套逻辑确实可以直接搬到节点上。
- **第 151 行「先完成 A5/C4/C5 再启动画布第 1 步」的排序建议是对的**，与 `优化计划.md:294-297` 的建议执行顺序一致。而且我确认了：A1 并发扇出（`ai_workflow.py:53-63` 的 `ThreadPoolExecutor`+`as_completed`）、B1 增量发布、B3 取消（`jobs.py:154-161`）、C1 重启回收（`jobs.py:83`）**都已经落地**，`Job.started_at/finished_at` 也确实存在（`models.py:63-70`）。所以画布不需要等这些。
- **第 5 章提出的 3 个待决策问题，本身选得准**——节点粒度、画布与向导的关系、重型编辑器的归属，确实是三个真岔路口。只是第 2 个的答案（一图一节点）在当前数据模型下做不到（见下）。

---

## 3. 承重论断中不成立的部分

### 3.1 「后端基本不用动 / 90% 在前端」（第 51、53 行）——这是整个方案唯一必须删掉的句子

逐条拆开。**每一条都是后端改动，且都是第 1 步的前置，不是收尾项。**

| # | 方案的假设 | 源码事实 | 后端工作量 |
|---|---|---|---|
| a | 「一张图 = 一个节点」（第 101 行）可以直接落到资产上 | `models.py:92-96` `job_id unique=True`；`assets.py:465` docstring 明写 "exactly one local asset for every successful visual module job"。彩平 4 图、分空间最多 12 图、轴侧 3 图，全部塞进 `deliverables.outputs` 数组，**没有行、没有主键**。一条完整工作流约 24-28 张图，只对应 6 行 `SceneAsset` | 需要 canvas_nodes 表 + 逐图展开器，1.5-2 天 |
| b | 「读取当前项目全部 assets」（第 76 行） | `GET /v1/assets`（`main.py:830-878`）**没有 `project_id` 参数**，`limit` 硬顶 100（`main.py:833`），`moduleKey`/`workflowStage` 是取回全表后在 Python 里过滤（`:869-877`）再切片——**加了过滤后 offset 语义本身就是错的**。而且第 861 行每次请求都调 `backfill_scene_assets`，它会遍历全部 SUCCEEDED job 重跑 `ensure_scene_asset`（`assets.py:558-578`）。画布轮询 = 每秒一次 O(全部成功任务) 的写事务 | 0.5 天 |
| c | 「01 原图 → 02 布局 → 03 彩平」的连线（第 76 行） | `FLOORPLAN_ANALYZE` 不在 `JOB_TYPE_TO_MODULE`（`assets.py:44-57`），`ensure_scene_asset` 第 468 行直接 return None——**阶段 01 没有任何资产**，第 85 行的「原图节点：重新识别」无对象可挂。`/v1/layouts/ai`（`main.py:1132-1214`）的表单里没有 `asset_parent_id`，只设了 `parent_job_id`（:1210），而 `assets.py:476` 只读 `payload["asset_parent_id"]`——**阶段 02 的 `parent_asset_id` 恒为 NULL** | 1 天（含历史回填） |
| d | 节点坐标 / 「画布 1 ▾」（第 93、127 行） | `models.py:21-34` 的 `Project` 只有 id/name/description/created_at，**没有 metadata 列、没有 updated_at**。没有 canvases 表。`SceneAsset.metadata_json` 不能用：`assets.py:520-522` 在资产已存在时用从 job 重算的完整 dict 整体替换，只白名单保留 5 个 approval 键——你写进去的坐标会在下一次 GET /assets 时被擦掉 | 1.5-2 天 |
| e | 右键菜单「删除 ⌘⌫ / 创建副本 ⌘D / 粘贴 ⌘V」（第 131 行） | `main.py` 全文 **0 个** `@app.delete` / `@app.patch` / `@app.put`（已 grep 确认）。即使加了 DELETE，`backfill_scene_assets` 会在下一次列表请求把删掉的资产重建回来。「创建副本」被 `job_id unique` 从物理上挡住 | 落在 canvas_nodes 层，含在 d 里 |
| f | 缩略图 | `assets.py:450-462` 的 `_thumbnail()` 只是从 deliverables 里挑一个**全尺寸原图 URL** 返回，全仓无任何 resize。产物是 1024×1024 / 1536×1024（`ai_workflow.py:238-241`）。`.local/artifacts` 现在 906 文件 745 MB，304 个 PNG 超过 1 MB。`main.py:93` 的 StaticFiles 无 Cache-Control（D3 未做）。40 个节点的画布会直接卡死 | 0.5-1 天 |

**合计 6-8 人日的后端改动，全部是第 1 步的硬前置。**「90% 在前端」是本方案最主要的误导来源。

### 3.2 「谱系已经完整，直接映射为节点连线」——连线是资产级的，画布要的是图级的

`parent_asset_id` 指向整行资产。图级来源只有阶段 06/07/08 通过 `_validate_parent_approved_space` 回写的 `parent_variant_id`（`main.py:756-760` → `assets.py:389-392`）。阶段 03→04、03→05 的 payload 里**没有** `parent_variant_id`（`main.py:1316-1331, 1387-1403`），只能反查父资产 metadata 的 `approvedVariantId`——而那是可变状态，连线会随审批漂移。方案对此一字未提。

另外阶段 03/04/05 的 `asset_parent_id` 是 `Form(None)`（`main.py:1232, 1294, 1363`），`_validate_parent_asset` 里 `if not asset_id: return project_id` 直接放行（`main.py:635-636`）；前端那个字段是折叠在 `<details>「项目与版本谱系（可选）」` 里的**手打输入框**（`AiDesignWorkflow.tsx:2184-2192`）。只有 06/07/08 是 `Form(...)` 必填。所以现网数据里大量资产的父指针就是空的。

### 3.3 「谱系是空间化的，原图 → 风格 A → 调色 B 在画布上自然铺开」（第 32 行）——后端明确禁止分叉

`main.py:979-986`：一个资产一旦有了下游子资产，改批另一个 variant 直接 409「该批准版本已有下游资产，不能直接改批」。审批是 per-asset 单值（`main.py:988-999` 的 `approvedVariantId`）。且没有任何撤销审批的端点（grep `unapprove|revoke` 零命中）。`tests/test_ai_workflow_derivatives.py:321-327` 把这个 409 固化成了预期行为。

后果比听起来严重。因为一批 12 张分空间图共用一个 `approvedVariantId`，而 `_validate_parent_approved_space` 第 722 行还会校验 `approved_output.get("spaceId") != space_id` → 409：**一次 12 空间的任务，只有一个空间能走到 06/07/08**。截图 1 那个 node1→node2 且 node1→node3→node4 的分叉 DAG，在我们的数据模型里画不出来。

### 3.4 「审批/设基准/重试直接复用现有逻辑，只是换入口」（第 87 行）——那三个函数**就是**单链状态机本身

- `submitStage` 第一行 `AiDesignWorkflow.tsx:1611`：`if (!isGenerationStage(activeStage) || busyStage !== null) return`——读全局 `activeStage`，且 `busyStage` 是**全应用单任务锁**，一次只能跑一个生成。
- `jobs` 的类型是 `Partial<Record<GenerationStage, Job>>`（`:1164`）——每阶段只能存 1 个 job，第二次彩平会**覆盖**第一次。
- `selectApprovedBaseline`（`:1827-1854`）在设基准时会 `withoutStageJobs(current, ['style','tone','local_edit'])` **删掉下游 job** 并 `setActiveStage('style')` 强制跳转。在画布上给某个节点设基准，会让画面上别的节点凭空消失、左侧导航莫名跳走。
- 基准是三个单值槽 `spaceBaseline/styleBaseline/toneBaseline`（`:1157-1159`），且持有的是**内存里的 `File` 对象**而不是 URL——`commonForm` 第 1482 行 `form.append('source_space_image', baseline.file)`。所以「从任一节点发起生成」必须先把该节点的图**全量下载成 File 再重传**（`workflowAssetImageFile`，`:1788-1791`），06-08 还要服务端和客户端各算一次 sha256 比对（`main.py:739-755`）。
- 轮询是单例：`pollAbortRef`（`:1161`）只有一个 AbortController，`cancelActiveJob`（`:1571-1585`）取的是 `jobs[busyStage]`——画布并发时它不知道该取消哪个。

整个主组件 `AiDesignWorkflow.tsx:1119-2629` 是 1510 行单组件、约 35 个 useState。**第 2 步不是「换入口」，是把这个状态机拆成 per-node 上下文 + 多任务并发调度。5-7 人日，不是 1-2 天。**

### 3.5 第 2 步列出的节点操作里，有两条是后端 100% 409 的非法组合

`_validate_parent_asset`（`main.py:665-670`）：`tone_scheme` 的父必须是已批准的 `style_scheme`，`local_edit` 的父必须是 `tone_scheme`。而方案第 84-85 行写的是「**空间**节点：…生成色调方案」「**风格**/色调节点：…局部修改」。这两个按钮点下去必然报错。节点操作表应该直接从 `main.py:648-672` 生成，不要手写。

### 3.6 第 7 章「与优化计划不冲突」——呈现层不冲突，节点身份冲突，优先级排序冲突

- `assets.py:468` 要求 `job.status == 'SUCCEEDED'`。B3 取消路径（`jobs.py:175-181`）保留已生成输出到 `job.result` 但**直接 return，不归档资产**。所以：用户在 4 张生成到第 2 张时点取消 → 那 2 张付过费的图有 `job.result.outputs`、永远没有 asset → 在「一图一节点」模型下刷新即消失。C1 重启回收（`jobs.py:83-101`）把 RUNNING 无差别置 FAILED，同理。
- `优化计划.md:312` 把「`busyStage` 改成 Set」明确列在**不建议现在做**的表里，理由是 A1 之后大部分冗余。画布把这条决策**反转成刚需**，并且会撞上该行警告的解锁 bug（`run()` 的 `finally { setBusyStage(null) }`，现在 `AiDesignWorkflow.tsx:1566`）。
- C3（WAL + busy_timeout）**未落地**——`database.py:20-25` 的 `create_engine` 没有任何 WAL / timeout 设置。A1 的变体级并发 × 画布的任务级并发，会让 SQLite 并发写次数相乘。
- 画布轮询 `/v1/assets` 会与 C4 的独立作业线程池争 DB，因为每次调用都跑一遍全表 backfill。

**第 7 章应该改写成一张交互表，而不是一句「不冲突」。**

---

## 4. 方案遗漏的部分

### 4.1 视觉：第 6 章与第 3 章自相矛盾（2-4 人日）

第 67 行说「节点卡片复用现有样式体系」，第 112-116 行要求近纯黑 #0a0a0d + 蓝 #3b82f6。现有体系是相反的：`App.css:6-49` 是 `--surface: #ffffff` / `--canvas: #f4f5f7` / 主色紫 `--blue-600: #7a5af8`；5664 行 CSS 里 `prefers-color-scheme|dark` **0 命中**，580 个 hex 字面量（359 个不同值），而 `var(--*)` 只用了 270 处。**颜色没有被 token 化到可以换肤的程度。**

更具体的接缝：第 102 行要「双击节点 → 全屏专注编辑」，那个编辑器就是 `AiDesignWorkflow.tsx:2027-2031` 挂载的 3094 行浅色 `FloorplanModule`。暗色画布点进去弹出一个亮色重型编辑器。

### 4.2 第 6 章是 mood board 不是 spec（决策成本 > 工程成本）

5 张截图里约 40 项能力，第 6 章明确覆盖了 4 项（暗色+点阵、圆角、贝塞尔连线、节点卡片结构），提到但未估时的 14 项，**完全没提的 23 项**：⚡点数余额、普通用户/升级、限时40折、竞赛卡片、风暴学堂、PS插件/合伙人/邀请有礼、推荐/最新 tab、★收藏、⊕New Folder、菜单分隔符、禁用项语义（截图 1 里 9 项中有 3 项是灰的）、节点重命名与「- 副本」命名、小地图关闭 X……

其中相当一部分是 SaaS 计费/社区外壳，而本产品是单人本地实例：`models.py` 只有 Project/Job/SceneAsset，无用户无鉴权，owner 是硬编码常量（`main.py:858`），UI 头部写死「本地个人账户 · BL」（`App.tsx:1084-1087`）。**在单人本地版里放一个 ⚡25 余额和「升级」按钮，是 UI 在撒谎。**

第 136 行还有一处误读：截图的「点击图片上的物体进行标记」是**点选物体**（分割模型），我们的阶段 08 是**手绘闭合红圈**（`AiDesignWorkflow.tsx:2452-2453` 的原文：「用红色画笔圈出或涂满要修改的区域」）。别照抄这句提示语。

### 4.3 第 0 步首页（第 142 行）——最大的工作流，0 估时

`App.tsx:1034` 是一个 `useState<ModuleId>`，**没有路由**；`react-router-dom@7.18.2` 装了但 `src/` 下 0 引用（同样 0 引用的还有 `@tanstack/react-query`、`zustand`、`zod`、`axios`、`react-hook-form`、`konva`、`react-konva`）。前端**从未调用过 `/v1/projects`**——`POST/GET /v1/projects`（`main.py:812-827`）是死接口，项目 ID 是 `AiDesignWorkflow.tsx:2175` 的一个手输框。截图 5 的「编辑于 3 分钟前 + 项目缩略图」需要 `Project.updated_at` + 封面列，都不存在。

截图 3 的三级模型级联在后端**没有对应物**：`config.py` 只有单一硬编码的 `kuyao_vision_model` / `kuyao_image_model`，没有 LLM、没有视频、没有 per-request 模型选择。截图 2 的功能卡片网格对应我们只有 6 个 `AI_*` job type，且它们是**有门禁的流水线步骤**不是自由函数——`/ai-workflow/color-plans` 要求 `layout_approved` + 500KB `semantic_layout`（`main.py:1226-1245`）。「对应我们的各阶段生成能力」这个映射不成立。

### 4.4 会话恢复与并发竞态（含在第 2 步重估里）

全前端 **0 处** localStorage/sessionStorage/IndexedDB。向导之所以用 `hidden` 而不是条件渲染挂载（`App.tsx:1091-1096`），正是因为状态全是不可序列化的 `File`。「画布为主、向导降级为可收起面板」隐含向导会被卸载/重挂载——那等于把用户上传的原图、识别结果、未提交的 P1-P4 编辑全丢掉。另外没有 jobs 列表端点（`main.py:1749` 只有单条 GET），刷新后无法知道本项目有哪些任务在跑。

并发竞态：提交时携带的 `source_space_version_id` 必须等于父资产**当前**的 `approvedVersionId`（`main.py:702-706`）。画布上 A 节点任务在途时改批了同一资产的另一个 variant，在途任务落地即 409。方案没有定义这个乐观锁语义。

### 4.5 测试（2 人日 + 1-1.5 人日后端修复）

后端 `apps/api/tests` 6062 行，其中 `test_assets.py`(481)、`test_ai_workflow_derivatives.py`(484)、`test_workflow_resume.py`(374) 全部建立在「一 job 一资产、资产不可删、单一审批」的假设上。前端 `vitest` / `@playwright/test` / `@testing-library/react` **都装了，0 个用例，package.json 没有 test 脚本**，`scripts/check.sh` 只跑 lint+build。这是项目史上最大的一次前端改动，却没有回归网。

---

## 5. 修正后的路线图

先说与《优化计划》的排序结论：**A1/B1/B3/C1 已落地，不阻塞画布；A5/C4/C5/D3 可与画布并行；但 C3（WAL）必须先于画布的多节点并发落地**，否则会撞 `database is locked`。`优化计划.md:312` 那条「不做 busyStage→Set」的决策**必须撤销**。

| 阶段 | 内容 | 估时 | 硬依赖 |
|---|---|---|---|
| **W-1 决策** | 拍板第 6 章能力表（必做/降级占位/不做）+ variant 粒度审批要不要做 | 0.5 天（会议） | — |
| **W0-a 后端解锁（必须先做）** | `/v1/assets` 加 projectId 并把过滤下推 SQL；把 `backfill_scene_assets` 从请求热路径移走（改启动时一次或显式端点）；新增 `GET /v1/jobs?projectId=&status=` | 1 天 | — |
| **W0-b 谱系补齐** | `FLOORPLAN_ANALYZE` 建资产（需补 asset_type/deliverables/thumbnail 四处分支）或合成虚拟根；`/layouts/ai` 与 `/floorplan-scenes` payload 补 `asset_parent_id`；03/04/05 的 `asset_parent_id` 改必填；历史回填 | 1 天 | W0-a |
| **W0-c 缩略图** | 后端生成 256/512 webp 写入 `thumbnail_url`，原图另存 `fullUrl`；`/artifacts` 加 Cache-Control（= 优化计划 D3）；906 个存量文件回填 | 0.5-1 天 | — |
| **W0-d 画布持久化** | `canvases(id, project_id, name, viewport_json, updated_at)` + `canvas_nodes(id, canvas_id, asset_id, variant_id, job_id, x, y, w, h, z, source_node_id, deleted_at)` + CRUD + 批量坐标 PATCH。删除/副本/粘贴全部落在 canvas_nodes（软删），`SceneAsset` 保持不可删。**好消息**：`database.py:31` 用 `create_all`，新表零迁移成本；**坏消息**：无 alembic，给既有表加列要照 `_ensure_job_timing_columns`（`database.py:35-54`）手写 shim | 1.5-2 天 | W0-a |
| **W0-e 逐图展开器** | 统一的「资产 → 逐图变体」服务，覆盖 ai_workflow/layout/floorplan/white_model/material 五种互不相同的 deliverables 形状（`assets.py:189-336`）。注意 layout 的 `previewUrls` 与 `metadata.variantIds` 是**靠数组下标隐式配对**（`assets.py:197-206, 346-355`），必须改成结构化配对。建议新文件 `app/canvas.py` | 1-1.5 天 | W0-d |
| **W0-f 部分结果可见** | 让 canvas-graph 显式返回未完成/被取消 job 及其 partial outputs 作为临时节点（或让 `ensure_scene_asset` 接受 CANCELED-with-partial 并打不可审批标记） | 0.5 天 | W0-e |
| **W0-g 后端测试修复** | | 1 天 | 全部 |
| **小计 W0** | | **6.5-8 天** | |
| **W0-X（可选，见第 6 节）** | variant 粒度审批 / 分叉：审批下沉到 `(asset_id, variant_id)`（可复用 `metadata_json`，无需重建表），改写 `_validate_parent_approved_space` / `_workflow_approved_output` / `workflow-resume` 取值；21 处单数审批引用 + 三个测试文件 | +2-3 天 | W0-e |
| **W1 设计令牌化** | `App.css` 359 个不同 hex 收敛到 token + 一套深色 token；或作用域化只给画布用（更便宜，但会出现两套视觉语言） | 2-4 天 | 可与 W0 并行 |
| **W2 画布引擎** | 先花 **0.5 天 spike 验证 `@xyflow/react` 对 React 19.2 的兼容 + 自定义节点塞现有卡片 + CSS 变量覆盖到深色**。成功则 2.5-4 天，失败回落自研 4-6 天。**方案第 64 行否决 React Flow 的理由不成立**：custom node 就是任意 React 组件，富卡片正是它的标准用法；而第 6 章要的贝塞尔连线/小地图/点阵背景/缩放百分比/框选/`onNodeContextMenu`/`onlyRenderVisibleElements`/`fitView(duration)` 全部是它的现成 API。它的 store 就是已在 `package.json:23` 的 zustand | 3-6 天 | W0-e |
| **W3 向导状态层重构** | `jobs` 改 `Map<nodeId, Job>`；`busyStage` 改 `Set<jobId>`（含 `finally` 解锁 bug）；三个 baseline 改成节点派生值；`AbortController` 改 Map；把生成/审批/设基准抽成**不依赖 `activeStage` 的纯函数模块**（入参显式传 sourceAssetId/variantId/versionId/spaceId），向导和画布共用；设基准不再删下游 job、不再跳转阶段 | 5-7 天 | W0，且**必须先于 W4** |
| **W4 节点操作条 + 右键菜单** | 操作表由 `main.py:648-672` 生成；非法项按截图范式置灰而非隐藏；`canRunAction(node, action)` 纯函数是置灰状态与功能面板禁用态的**唯一真值来源**；快捷键分发器必须带 `isEditableTarget()` 守卫（`AiDesignWorkflow.tsx:2175/2186` 是输入框，无守卫的 ⌘⌫ 会在用户打字时删节点）；409 时在节点上渲染「上游已变更，点此重新绑定基准」 | 2 天 | W3 |
| **W5 前端测试接线** | vitest + playwright config + `test` 脚本；覆盖视口数学、`${jobId}:${variantId}` 节点身份、job→asset 身份交接 | 2 天 | W2 |
| **W6 第 0 步首页** | 引入 react-router-dom 把现有 8 模块迁到路由；左侧 56px 图标栏 + 公告条；首页问候 + 大输入框（文本直接接到已存在的 `design_prompt` 表单字段，`main.py:1230`）+ 项目卡片墙（需 `Project.updated_at` + 封面列，+0.5 天后端）。**模型级联降级为静态 pill，案例墙用用户自己的资产喂**（见第 7 节） | 3-5 天 | W0-a |

**合计**：
- **MVP（W0-a/b/c + W2 + W4 精简版 + 少量 W1，只读画布 + 基础节点操作，保留向导做生成，无首页、无分叉、无多画布）：10-14 人日**
- **截图级还原（全部 W0-W6 + W0-X）：27-35 人日 ≈ 5-7 周**

方案第 73/80/90 行的 1+1-2+1-2 = 3-5 天，是**在第 6 章被追加之前**写的估时，追加后从未修订。这是这份方案最实际的缺陷。

---

## 6. 必须先拍板的决策

**D1. variant 粒度审批 / 分叉：做还是不做？（最重要）**
不做 → 一批 4 张彩平只能有 1 张带下游子树；一次 12 空间任务只有 1 个空间能进 06/07/08；截图 1 的分叉 DAG 画不出来；画布本质上是「更好看的向导」。
做 → +2-3 天（审批状态可以留在 `metadata_json` 里，不用重建表），但要改 `_validate_parent_approved_space`、`_workflow_approved_output`、`workflow-resume` 的取值逻辑和三个测试文件。
**我的建议：做，但排在 MVP 之后。** 先上只读画布，观察用户是否真的频繁尝试从非批准节点派生——那才是启动这项改造的信号。在此之前，第 2 步的「设为基准/批准」入口对已有下游的节点必须**置灰并说明原因**，不要让用户撞 409。

**D2. 画布引擎：自研 vs `@xyflow/react`？**
**我的建议：先做 0.5 天 spike 验证 xyflow。** 方案否决它的理由（「富卡片会被约束」）与事实不符，而自研会在框选坐标、小地图视口矩形、边的命中测试、触控板 pinch 消歧这四个点上反复出 bug——恰好是用户一眼能看出「做得糙」的地方。spike 失败再回落方案 A，同时把工期改成 4-6 天。顺手清掉 konva/react-konva/axios/react-hook-form 等 0 引用依赖。

**D3. 深色主题：全站 token 化 vs 画布作用域？**
全站 = 2-4 天审计 359 个 hex，一致但慢；作用域 = 0.5-1 天，但暗色画布双击进去弹出亮色 3094 行 FloorplanModule。
**我的建议：作用域化先上，把「阶段 01 编辑器 + AssetLibrary」这两个跨主题接缝作为已知问题登记，单独排期。** 不要在第 1 步就吞掉 4 天 CSS 审计。

**D4. 「画布 1 ▾」多画布：v1 要不要？**
便宜版 = `Project.canvas_state JSON` 一列 + PATCH（0.5 天），单画布，把选择器藏掉；完整版 = canvases + canvas_nodes 两张表（1.5-2 天）。
**我的建议：直接做完整版**，因为副本/粘贴/软删都要落在 canvas_nodes 上，便宜版做完立刻要重做。但把「画布 1 ▾」的 UI 藏到 v2。

**D5. 第 6 章的 40 项能力，逐行裁决。**
把第 110-136 行改写成一张三列表（必做 / 降级占位 / 不做），一行一项能力，开一次会拍完。其中 SaaS 计费与社区外壳可以一次性打包砍掉（见下），实际需要逐行讨论的大约 15 项。**这件事必须在写任何代码之前完成**，否则第 0 步是开放式需求。

---

## 7. 建议砍掉的范围

1. **全部计费/会员/社区外壳**：⚡点数余额、普通用户/升级、限时40折、限时赛事、风暴学堂/讲师课程/讲师作品、PS插件/创作者挑战赛/加入合伙人/邀请有礼、手机图标。单人本地实例（`main.py:858` owner 硬编码常量，`App.tsx:245` 自称 "LOCAL PILOT · V0.6"）背后没有任何东西支撑它们，放上去就是假 UI。
2. **三级模型级联选择器**（第 121 行）。我们只有一个图片模型，没有 LLM、没有视频。保留问候语 + 大输入框（文本接到已存在的 `design_prompt` 字段），下拉降级为一个显示 `/health` 里配置模型名的静态 pill。这是真省钱，不是纯砍需求。
3. **节点四角缩放手柄**（第 21 行）。给固定宽高比的图片节点加缩放手柄，有 aspect-ratio 与重光栅化成本，没有产品价值。
4. **「优化工作流布局」自动排版菜单项**（第 131 行）。第 1 步的 lineage 自动布局已经是它了，做成显式菜单项属于重复。
5. **精选案例瀑布流**——降级而不是砍：**用用户自己已生成的资产喂它**（每个资产都有 `thumbnail_url`，`assets.py:607`）。但要知道混合宽高比瀑布流需要 CSS columns（破坏 DOM/Tab 顺序）或 JS 测量布局，`App.css` 里 30+ 条 `grid-template-columns` 一条也用不上，是真工作量。
6. **响应式与可访问性**——不建议现在写进方案（用户没要求）。明确声明画布是 desktop-only（`App.css:58` 的 `body { min-width: 1120px }` 已经是这个事实）。但**键盘快捷键是硬需求**（截图 1 明确标注了 ⌘C/⌘D/⌘V/⌘⌫），需要在画布容器上做焦点管理。

**最后一条建议，也是我最想让你考虑的：先做一个 2-3 天、零后端改动的「方案地图」。** 在现有向导右侧加一块可收起的只读面板，用 `/v1/assets?limit=100` 的结果按 `metadata.workflowStage` 分列、按 `parentAssetId` 画 SVG 贝塞尔、CSS transform 容器做平移缩放，点节点直接复用 `WorkflowAssetPicker.tsx:239-262` 已经实现好的 `approveAndResume`/`resumeAsset`。零件全都现成。它会以极低成本回答两个决定 27-35 人日投资方向的问题：用户到底是用它「回看」还是用它「跳转操作」？他们会不会真的尝试从非批准节点派生？

（一个诚实标注：这个版本受限于 `main.py:830-859` 无 projectId 过滤，只能显示「我的最近 100 个资产」且跨项目混排——UI 上必须如实说明，不要假装是本项目全景。）
## 附录：72 条发现全文

6 个维度并行审计，每条再由独立的对抗性验证 agent 逐条复核 file:line。**存活 66 条，推翻 6 条**。
被推翻的条目保留在原位并标注驳回理由，以免日后重复讨论。


### 维度 1 — 后端就绪度：「后端基本不用动、90% 在前端」是否成立

共 8 条，存活 8，推翻 0。

| 级别 | 判定 | 问题 |
|---|---|---|
| 🔴 blocker | ❌ 方案错误 | 「一张图 = 一个节点」与 SceneAsset.job_id UNIQUE 直接冲突：一次彩平任务产出 4 张图，但数据库里只有 1 行 |
| 🔴 blocker | ❌ 方案错误 | parent_asset_id 并非「已经完整」：阶段 01 根本不产生资产，阶段 02 永远是孤儿根，阶段 03 的父指针是一个用户手打的可选输入框 |
| 🟠 major | ◐ 方案不完整 | 连线是「资产级」的，画布要的是「图级」的——parent_asset_id 无法告诉你 4 张彩平里哪一张生出了轴侧 |
| 🔴 blocker | ❌ 方案错误 | 没有任何「按项目取全部资产 + 谱系」的接口；现有 /v1/assets 既不支持 projectId，又有 100 条硬上限，还在每次请求里全表回填 |
| 🔴 blocker | ⚠️ 方案乐观 | 节点坐标、画布归属、「一个项目多张画布」在数据库里零基础；而计划把它降级成 localStorage，与截图要求的多画布切换器矛盾 |
| 🔴 blocker | ❌ 方案错误 | 右键菜单要求的删除/副本/粘贴：后端一个写接口都没有，而且 backfill 会把删掉的资产复活 |
| 🟠 major | ❌ 方案错误 | 与《优化计划》的冲突是存在的：取消和重启回收会让已生成的图从画布上消失，因为资产只在 SUCCEEDED 时创建 |
| 🟠 major | ⚠️ 方案乐观 | 工期评估：后端真实工作量 5-8 人日，不是「基本不用动」；1+2+2=5 天的总预算连后端都不够 |

#### 「一张图 = 一个节点」与 SceneAsset.job_id UNIQUE 直接冲突：一次彩平任务产出 4 张图，但数据库里只有 1 行

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：models.py:92-96 `job_id` 带 `unique=True`；assets.py:465-466 `ensure_scene_asset` 的 docstring 明写「Create exactly one local asset for every successful visual module job」，assets.py:490 用 `select(SceneAsset).where(job_id == job.id)` 做 upsert，539-554 只 `session.add` 一行。多图全部塞进 `deliverables.outputs`（assets.py:258-276 `safe_outputs` / `outputUrls`）。变体上限：彩平 4（main.py:1248 + ai_workflow.py:81-86）、轴侧 3（main.py:1311）、分空间 12（main.py:1378）、风格 4（main.py:1449）、色调 3（main.py:1519）。此外 FLOORPLAN_SCENE 一行资产的 deliverables 里就有 topDown/roomPreview/dollhouse/baseRender/finalRender/semantic/depth 共 7 个图 URL（assets.py:323-336），LAYOUT 一行里是 `previewUrls` 字符串数组（assets.py:197-206）且**没有**逐图 id——layoutId 只在 metadata.variantIds 里（assets.py:346-355），两者靠**数组下标隐式对应**，任一 layout 缺 previewUrl 就会错位。
- **后果**：节点没有数据库身份。一条完整工作流约 24-28 张图只对应 6 行 SceneAsset。第 3 步「节点可拖拽换位、坐标持久化」无处落坐标；第 6 章的「创建副本 ⌘D / 粘贴 ⌘V」在数据层根本插不进去（job_id UNIQUE 直接拒绝第二行指向同一 job）。「后端基本不用动」在第一个需求上就不成立。
- **建议**：必须新建 `canvas_nodes` 表，节点主键为 (asset_id, variant_id) 的实例化行，而不是复用 SceneAsset。同时写一个统一的「资产 → 逐图变体」展开器（ai_workflow 走 outputs[].variantId，layout 走 previewUrls×variantIds 且必须改成结构化配对，floorplan/white_model/material 各有各的 deliverables 形状），建议新文件 app/canvas.py 约 250-350 行。不要试图去掉 job_id 的 UNIQUE：SQLite 无法 DROP CONSTRAINT，要整表重建。

#### parent_asset_id 并非「已经完整」：阶段 01 根本不产生资产，阶段 02 永远是孤儿根，阶段 03 的父指针是一个用户手打的可选输入框

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：(1) `FLOORPLAN_ANALYZE` 不在 `JOB_TYPE_TO_MODULE`（assets.py:44-57），`ensure_scene_asset` 第一行就 return None（assets.py:468）——阶段 01 原图/语义识别**没有任何 SceneAsset**。(2) `create_ai_layout_job` 的 payload（main.py:1193-1205）**不含** `asset_parent_id`，只设了 `parent_job_id=analysis_job.id`（main.py:1211）；assets.py:476 只读 `payload.get("asset_parent_id")`，所以 LAYOUT_AI 资产的 `parent_asset_id` **恒为 NULL**。(3) `FloorplanSceneRequest`（schemas.py:191-217）没有 asset_parent_id 字段，`create_floorplan_scene`（main.py:1106-1118）直接 model_dump，所以主路径的 FLOORPLAN_SCENE 也是 NULL；只有 `/assets/{id}/renders` 派生路径会设（main.py:1040）。(4) 阶段 03 的 `asset_parent_id` 是 `Form(None)`（main.py:1232），`_validate_parent_asset` 里 `if not asset_id: return project_id` 直接放行（main.py:635-636）；前端 `layoutParentAssetId` 是折叠在「项目与版本谱系（可选）」里的手输框（AiDesignWorkflow.tsx:1138, 2184-2192），只有走 WorkflowAssetPicker（:1304）或 workflow-resume（:1334/1360/1365）才会自动回填。阶段 04/05 同样是 `Form(None)`（main.py:1294, 1363）。只有阶段 06/07/08 是 `Form(...)` 必填（main.py:1433, 1503, 1571）。
- **后果**：第 1 步「按 lineage 自动布局、贝塞尔连线表示 parent → child」在现有数据上会画出一堆断裂的孤立节点：01 完全不存在，02 是无父根，03 在「新上传图直接生成」这一主路径上父指针为空。用户看到的不是「工作地图」而是「碎片」。历史数据全部如此，且无法靠前端补救。
- **建议**：三件事必须先做后端：把 FLOORPLAN_ANALYZE 纳入 ASSET_JOB_TYPES（需要新的 asset_type / deliverables / thumbnail 分支，assets.py 四处 dict 都要加）；在 layouts/ai 与 floorplan-scenes 的 payload 里写入 asset_parent_id；把阶段 03-05 的 asset_parent_id 从可选改为前端强制携带（否则连线永远缺一环）。另外要写一次历史数据回填。约 1 人日。

#### 连线是「资产级」的，画布要的是「图级」的——parent_asset_id 无法告诉你 4 张彩平里哪一张生出了轴侧

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：`parent_asset_id` 指向整行资产（models.py:97-101）。图级来源只有两个出处：阶段 06/07/08 通过 `_validate_parent_approved_space` 回写 `parent_variant_id` / `parent_approved_version_id`（main.py:756-760 → assets.py:389-392）；阶段 04/05 的 payload 里**没有** parent_variant_id（main.py:1316-1331, 1387-1403 只有 approved_color_plan_path），只能反查父资产 metadata 的 `approvedVariantId`（main.py:991-999 写入）。
- **后果**：第 1 步的贝塞尔连线对阶段 03→04、03→05 无法直接从字段读出源图，必须在前端反查父资产的当前审批变体；而审批是可变状态（main.py:970-986 只在「已有下游」时才拒绝改批，无下游时可以改），连线会随审批漂移。这是画布语义的核心，plan 里一个字都没提。
- **建议**：在图级展开器里统一解析边：优先 metadata.parentVariantId，回退父资产 metadata.approvedVariantId，并在节点上冻结解析结果（存进 canvas_nodes.source_node_id），而不是每次渲染重新推导。

#### 没有任何「按项目取全部资产 + 谱系」的接口；现有 /v1/assets 既不支持 projectId，又有 100 条硬上限，还在每次请求里全表回填

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：`GET /v1/assets`（main.py:830-878）的查询参数只有 limit(≤100)/offset/generationMode/moduleKey/workflowStage，**没有 projectId**；SQL 只按 owner_id 过滤（main.py:862-865），module/stage 过滤是取完全表后在 Python 里做的（main.py:870-877），最后才切片。每次调用第一行就是 `backfill_scene_assets(session)`（main.py:861），它会 `select(Job).where(type.in_(...), status=='SUCCEEDED')` 拉出**全部**成功任务并逐个跑 ensure_scene_asset（assets.py:558-578）；同样的调用还出现在 main.py:894, 908, 924, 1026。唯一带 lineage 的接口是 `/assets/{id}/workflow-resume`，它只返回**单条祖先链**（main.py:611-617），链上每个节点都 `session.get(Job, ...)` 一次（main.py:404, 441, 483, 494），还要对文件算 sha256（main.py:364-382），并且非 approved 资产直接 409（main.py:352-360）。
- **后果**：第 1 步「读取当前项目全部 assets」这一句所需的接口不存在。照现状实现只能：拉 /v1/assets（拿到的是全局资产，不是本项目）→ 对每个资产再打一次 workflow-resume 拿谱系（N+1，且未审批资产会 409）。而且画布在生成期间需要轮询，每次轮询触发一遍全表回填，是 O(全部成功任务) 的写事务。
- **建议**：新增 `GET /v1/projects/{id}/canvas-graph`：一次返回该项目的资产、逐图变体、边、以及**未完成的 job**。把 backfill 从读路径上摘掉（改成启动时跑一次或显式端点）。给 /v1/assets 加 projectId 过滤并下推到 SQL。约 1-1.5 人日含测试。

#### 节点坐标、画布归属、「一个项目多张画布」在数据库里零基础；而计划把它降级成 localStorage，与截图要求的多画布切换器矛盾

`🔴 blocker` ｜ `⚠️ 方案乐观`

- **证据**：models.py 全文 118 行只有三张表 Project / Job / SceneAsset，没有任何 x/y/canvas 字段。SceneAsset 也没有 canvas_id。截图 1 明确要求顶部 `画布 1 ▾` 选择器（一个项目多张画布），计划第 6 章 :127 也写进了硬性规范，但第 3 步 :93 只说「布局存 localStorage / project 元数据」。Project 表连 metadata 列都没有（models.py:21-34 只有 id/name/description/created_at）。更麻烦的是前端**从未调用过 /v1/projects**：`grep -rn "/projects" apps/web/src` 零命中，`POST /v1/projects`（main.py:812-822）和 `GET /v1/projects`（main.py:825-827）是死接口，项目 ID 目前是 AiDesignWorkflow.tsx:2175 的一个手输框。
- **后果**：localStorage 方案在「多画布 / 多设备 / 分享」三个截图明示的需求下立刻失效，等于第 3 步做完还要重做一遍。而且首页「项目卡片墙 + 新建项目」（截图 5）背后没有任何在用的项目生命周期——不是「演进现有我的资产」那么轻，是从零接起来。
- **建议**：直接建 `canvases`(id, project_id, name, viewport_json, created_at) 与 `canvas_nodes`(id, canvas_id, asset_id, variant_id, job_id, kind, x, y, w, h, z, source_node_id, deleted_at) 两张表，跳过 localStorage 阶段。好消息：database.py:31 用 `Base.metadata.create_all`，**新表零迁移成本**；坏消息：项目没有 alembic（database.py:35-54 是手写的 ALTER 补丁），任何给既有表加列的动作都得自己写 shim。另需把 project_id 从各阶段的 `Form(None)` 变成实际必填并接上项目 CRUD UI。约 1-1.5 人日后端。

#### 右键菜单要求的删除/副本/粘贴：后端一个写接口都没有，而且 backfill 会把删掉的资产复活

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：`grep -n "app.delete\|app.patch\|app.put" main.py` 零命中——整个 API 没有任何 DELETE/PATCH/PUT。SceneAsset 的唯一写接口是 `POST /assets/{id}/approve`（main.py:915-1012）。即使加了 DELETE，`backfill_scene_assets`（assets.py:558-578）在每次 `/v1/assets`、`/assets/{id}`、`/workflow-resume`、`/approve`、`/renders` 调用时都会遍历全部 SUCCEEDED job 重新 `ensure_scene_asset`，**被删的资产会在下一次列表请求时自动复活**。「创建副本」则被 job_id UNIQUE（models.py:92-96）从物理上挡住。删除还会破坏两处强一致校验：`_validate_parent_approved_space` 要对父资产的输出文件算 sha256 比对（main.py:735-755），以及改批时的下游存在性检查（main.py:979-986）。
- **后果**：截图 1 的右键菜单（删除 ⌘⌫ / 创建副本 ⌘D / 粘贴 ⌘V）是硬性样式规范，但没有一项有后端支撑。若只在前端做「视觉删除」，刷新即还原，是明显的假功能。
- **建议**：把删除/副本/粘贴全部落在 canvas_nodes 层（软删 deleted_at，副本 = 新 node 行指向同一 asset_id+variant_id），SceneAsset 保持不可删——这样既满足菜单又不破坏谱系校验。同时给 ensure_scene_asset / backfill 加一个「已被画布软删的不重建节点」的判定。约 0.5-1 人日。

#### 与《优化计划》的冲突是存在的：取消和重启回收会让已生成的图从画布上消失，因为资产只在 SUCCEEDED 时创建

`🟠 major` ｜ `❌ 方案错误`

- **证据**：assets.py:468 `if job.type not in ASSET_JOB_TYPES or job.status != "SUCCEEDED": return None`，`_archive_job_asset` 也只在 `succeeded` 时调用（jobs.py:104-124, 233-234）。而 B3 取消路径明确保留已完成输出到 `job.result`（jobs.py:175-181，写 batchStatus=canceled 后直接 return，**不归档资产**）；ProcessorError 的 `partial_result` 同理（jobs.py:196-205）；C1 的 `reclaim_stale_jobs` 把 RUNNING 无差别置为 FAILED/WORKER_LOST（jobs.py:81-101）。另外即使 job 成功，`_asset_deliverables` 的 `safe_outputs` 只保留有 url 的输出（assets.py:260-265），失败变体在资产里**不存在**，而 B4「仅重试失败项」恰恰依赖它们。B1 增量发布只写 `job.result`（jobs.py:150-157），生成中完全没有资产。
- **后果**：计划第 7 章「不改变优化计划的性能路线、增量发布的骨架卡片天然变成骨架节点」是错的。实际后果：用户在 4 张里生成到第 2 张时点取消 → 画布上 2 个节点在刷新后消失；服务重启同理。而且画布必须同时消费「已归档资产」和「进行中 job」两个数据源，这本身就否定了「90% 在前端」。
- **建议**：要么让 ensure_scene_asset 接受 CANCELED / FAILED-with-partial（同时给这些资产打不可审批标记，避免绕过 _validate_parent_asset 的审批门禁），要么让 canvas-graph 接口显式返回未完成 job 及其 partial outputs 作为临时节点。前者约 0.5 人日 + 改动 test_assets.py / test_job_cancellation.py。

#### 工期评估：后端真实工作量 5-8 人日，不是「基本不用动」；1+2+2=5 天的总预算连后端都不够

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：逐项汇总本次核查结果：(1) /v1/assets 加 projectId 并把 backfill 移出读路径（main.py:830-878, 861/894/908/924/1026 + assets.py:558-578）≈0.3 天；(2) 新建 canvas-graph 接口 + 逐图变体展开器，需覆盖 ai_workflow/layout/floorplan/white_model/material_replacement 五种互不相同的 deliverables 形状（assets.py:189-336）≈1.5 天；(3) canvases / canvas_nodes 建模 + 批量坐标 PATCH + 视口持久化 ≈1.5 天；(4) 删除/副本/粘贴 + backfill 防复活 ≈1 天；(5) 谱系补齐（FLOORPLAN_ANALYZE 建资产、layouts/ai 与 floorplan-scenes 补 asset_parent_id、历史回填）≈1 天；(6) 项目生命周期接起来 + project_id 由可选转必填（会开始触发 main.py:640-641 的跨项目 409）≈0.5 天；(7) 取消/失败/重启的部分结果可见性 ≈0.5 天。测试面：tests/ 共 6062 行，其中 test_assets.py(481)、test_workflow_resume.py(374)、test_ai_workflow_derivatives.py(484) 都建立在「一 job 一资产、资产不可删」的假设上。前端侧 package.json 无 reactflow/xyflow（只有 konva/react-konva），CanvasViewport 需自研，而第 6 章的样式规范（首页图标栏+瀑布流+级联模型选择器、画布左侧功能面板+底部工具栏+小地图+右键菜单+快捷键）远超「约 300 行」。
- **后果**：按计划的 5 天排期启动，第 1 步就会卡在「没有项目级谱系接口、01/02 节点不存在」上，第 3 步的坐标持久化会退化成 localStorage 而与多画布需求冲突，需要返工。
- **建议**：把方案表格里「后端基本不用动」改成「后端需先做谱系补齐 + 画布持久化两组前置改动（≈5-8 人日）」，并把第 0 步定义为后端前置而非首页改版。前端按现有截图规范另计 8-15 人日。同时保留一条真实成立的优势：新表由 create_all 自动建（database.py:31），不需要引入 alembic。


### 维度 2 — 前端架构与技术选型：自研 CanvasViewport vs React Flow

共 11 条，存活 11，推翻 0。

| 级别 | 判定 | 问题 |
|---|---|---|
| 🔴 blocker | ❌ 方案错误 | 「自研约 300 行 CanvasViewport」严重低估，实际 900–1500 行 / 5–8 天 |
| 🔴 blocker | ❌ 方案错误 | 否决 React Flow 的理由不成立；参考截图要的功能 xyflow 全部原生自带，且 zustand 已在依赖里 |
| 🔴 blocker | ❌ 方案错误 | 「审批/设基准/重试直接复用现有逻辑，只是换入口」是错的——向导是单活跃谱系状态机，节点级调用会互相摧毁 |
| 🔴 blocker | ❌ 方案错误 | 第 1 步的入口条件不成立：/v1/assets 没有项目过滤，project_id 是一个折叠面板里手打的可选字段 |
| 🔴 blocker | ❌ 方案错误 | 「后端谱系已经完整」不成立：01→02 这条边在数据库里根本不存在，直传上传路径也会断链 |
| 🟠 major | ◐ 方案不完整 | 「一张图 = 一个节点」在前后端都没有稳定 ID，节点身份与布局持久化无处落脚 |
| 🟠 major | ⚠️ 方案乐观 | 50–200 图片节点的性能：产物是 1024×1024/1536×1024 原图，thumbnail_url 就是原图 URL，全流程没有缩略图生成 |
| 🟠 major | ◐ 方案不完整 | §6 强制的近黑主题与 §3「节点卡片复用现有样式体系」直接矛盾：现有 5664 行 CSS 是浅色紫调，422 处硬编码颜色 |
| 🟠 major | ◐ 方案不完整 | 新增的「第 0 步首页改版」没有路由可用：react-router-dom 已安装但零引用，全应用靠一个 useState 切视图 |
| 🟠 major | ⚠️ 方案乐观 | §7「与优化计划不冲突」部分成立但不完整：B3 取消 / C1 重启恢复在画布多任务下的前端侧全部失效 |
| 🟠 major | ⚠️ 方案乐观 | 三步走总工期 3–5 天，按本次核对的实际工作量应为 4–7 周 |

#### 「自研约 300 行 CanvasViewport」严重低估，实际 900–1500 行 / 5–8 天

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：方案 §3 line 67 声称「自研约 300 行的 CanvasViewport（wheel 缩放 + pointer 平移 + 小地图）」，§4 line 73 给 1 天。但 §6 line 130-134 把需求扩到：贝塞尔连线、右键菜单带快捷键（⌘C/⌘D/⌘V/⌘⌫）、底部工具栏、缩放百分比、小地图、点阵背景；§4 line 88 还要「平滑缩放平移动画」，§5 line 101 要「同批变体自动对齐成组（可整组拖动）」，§1 line 21 要「四角缩放手柄」。现有代码里可复用的 viewport 基建为零：grep `zoom|scale|pan|transform` 在 App.tsx / FloorplanModule.tsx / AssetLibrary.tsx / AiDesignWorkflow.tsx 只命中 App.tsx:715 的一次性 `Math.min(1, 820/image.width)` 缩略图缩放和 FloorplanModule.tsx:2850 的 SVG `rotate()`；App.css 5664 行里 `.canvas` / `zoom` / `minimap` 类名 0 命中。按功能拆：指针中心 wheel 缩放 + macOS 触控板 pinch(ctrlKey) 与二指平移消歧 60–90 行；pointer 平移 + 空格拖拽 + 中键 40–60；点阵背景随视口 25；节点拖拽 + 组拖拽 + 吸附 80–120；单选/Shift 多选/世界坐标框选 80–120；SVG 贝塞尔层（锚点计算 + 层级 + pointer-events 穿透）80–120；边的命中测试（右键菜单要点中连线）40；视口剔除（world bbox + rAF 节流 + 每帧扫描）60–100；小地图（二次渲染 + 世界包围盒拟合 + 视口矩形拖拽导航）100–150；zoom-to-fit / setCenter 缓动动画 50–70；缩放百分比与 +/- 20；键盘 + 剪贴板模型 80–120；右键菜单定位与边缘翻转 60；四角缩放手柄 80。合计 855–1345 行，且不含节点卡片本身，也不含 transform 图层爆炸、分数 scale 文字模糊、拖拽期间 pointer-events 这类必踩的调试尾巴。
- **后果**：第 1 步按 1 天排期，实际到「不卡不跳、交互不打架」需要 5–8 个工作日；整个三步走的排期基线从一开始就错了 3–5 倍，后面两步的估算都建立在这个错误基线上。
- **建议**：要么改用 @xyflow/react（见另一条），要么把 §3 line 67 的「约 300 行」改成「约 1000–1500 行」、§4 line 73 的「约 1 天」改成「5–8 天」，并把小地图、框选、组拖拽、缩放手柄、快捷键从第 1 步挪到第 2/3 步，第 1 步只保留 wheel 缩放 + 平移 + 点阵背景 + 只读节点 + 连线。

#### 否决 React Flow 的理由不成立；参考截图要的功能 xyflow 全部原生自带，且 zustand 已在依赖里

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：方案 §3 line 64 否决 React Flow 的唯一理由是「节点定制和连线语义要被它的模型约束，我们『节点即富卡片』的场景会有点拧」。这个判断与事实不符：xyflow 的 custom node 就是任意 React 组件（注册 nodeTypes 即可），审批按钮、BatchProgress 进度条、P1-P4 输入框都能直接塞进去——「富卡片」正是它的标准用法，被约束的只有 Handle 连接点（可 `isConnectable={false}` 隐藏或用 floating edge）。而 §6 line 130-134 列的强制样式需求与 xyflow 的现成 API 几乎一一对应：贝塞尔连线 = 默认 edge type、小地图 = `<MiniMap/>`、点阵背景 = `<Background variant="dots"/>`、缩放百分比 = `<Controls/>`、多选框选 = 内置 shift-drag、节点右键菜单 = `onNodeContextMenu`、视口剔除 = `onlyRenderVisibleElements`、§4 line 88 的「点击阶段→平滑聚焦」= `fitView({nodes, duration})` / `setCenter`。反过来，「不想加依赖」这个隐含理由在本仓库不成立：package.json:12-24 声明的 8 个运行时依赖里，@tanstack/react-query、zustand、react-router-dom、react-hook-form、zod、axios、konva、react-konva 在 src/ 下 import 命中数全部为 0（只有 react / react-dom 真在用）——项目已经背着一整套 canvas 引擎（konva）却没用。xyflow 的 store 就是 zustand，已在 package.json:23，不引入新的状态库范式。反方最强的论点只有两条：xyflow 自带 CSS 需要为 §6 的近黑主题做覆盖；以及需要先验证它对 React 19.2（package.json:18）的兼容。这两条的代价都远小于自研 1000+ 行。
- **后果**：按方案 A 走，团队会花 5–8 天重写一遍 xyflow 已经做好且经过大量生产验证的东西，并且会在框选坐标、小地图视口矩形、边的命中测试、触控板缩放这几个点上反复出 bug，而这些恰好是截图里用户能一眼看出「做得糙」的地方。
- **建议**：把 §3 的推荐从方案 A 改为 @xyflow/react（MIT），先花半天做 spike 验证 React 19 兼容 + 自定义节点里放现有 workflow-result-card + 覆盖它的 CSS 变量到近黑主题。若 spike 失败再回落方案 A，但同时把工期改成 5–8 天。另外顺手清掉 konva/react-konva/axios/react-hook-form 等 0 引用依赖。

#### 「审批/设基准/重试直接复用现有逻辑，只是换入口」是错的——向导是单活跃谱系状态机，节点级调用会互相摧毁

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：§4 line 87 声称「审批/设基准/重试直接复用现有 approveGeneratedColorPlan / selectApprovedBaseline / submitStage 逻辑，只是入口从阶段面板换成节点操作条」。读代码，这三个函数都不可能被节点调用：(1) submitStage 第一行 AiDesignWorkflow.tsx:1611 `if (!isGenerationStage(activeStage) || busyStage !== null) return` —— 读全局 activeStage、且 busyStage 是全应用单任务锁，一次只能跑一个生成；(2) jobs 的类型是 AiDesignWorkflow.tsx:1164 `Partial<Record<GenerationStage, Job>>`，每个阶段只能存 1 个 job，1540/1544/1548 行直接按 stage 键覆写——画布上同阶段并存多个批次就会互相覆盖；(3) selectApprovedBaseline 在 1827-1838 行设基准时会 `withoutStageJobs(current, ['style','tone','local_edit'])` 删掉下游 job 并 `setActiveStage('style')` 强制跳转，approveGeneratedColorPlan 在 1738-1741 行同样把 jobs 裁成只剩 color_plan——在画布上「给某个节点设基准」会让画面上别的节点凭空消失；(4) 基准只有三个单值槽 spaceBaseline/styleBaseline/toneBaseline（1157-1159），restoreRequiredAsset 在 1411-1413 行恢复时先把三个全 null 再设一个；invalidateFromSourceChange（1223-1240）一次清 12 项状态含 `setJobs({})`；(5) 生成靠内存里的 File 对象而非 URL：commonForm 1482 行 `form.append('source_space_image', baseline.file)`、1500 行 `form.append('approved_layout_image', layoutImage)`，所以「从任一节点发起生成」都必须先把该节点的 URL 下载成 File（restoreRequiredAsset 1311-1429 这 119 行干的就是这 …
- **后果**：第 2 步「约 1-2 天」实际是把 1510 行单组件的状态机拆成 per-node context + 多任务并发调度（jobs 改成按 jobId 索引、busyStage 改成 per-node busy、AbortController 改成 Map、baseline 三槽改成节点属性、File 改成 lazy URL→File 缓存）。这是重写不是复用，6–10 天，且会同时动到 stage 01/02 的接线（acceptStageOne 1259 / acceptStageTwo 1300 都调 invalidateFromSourceChange）。
- **建议**：把第 2 步拆成两步：2a「先把生成/审批/设基准从组件里抽成不依赖 activeStage/busyStage 的纯函数模块（入参显式传 sourceFile/assetId/versionId/spaceId），向导和画布共用」，2b「画布节点操作条接这套 API」。2a 必须先做且要有测试兜住旧向导不回归。工期改 6–10 天。并明确写出「设基准不再删除下游 job、不再跳转阶段」这条行为变更。

#### 第 1 步的入口条件不成立：/v1/assets 没有项目过滤，project_id 是一个折叠面板里手打的可选字段

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：§4 line 76 第 1 步要「读取当前项目全部 assets」，§1 line 15 定「一个项目 = 一张画布」。但 main.py:830-877 的 `list_scene_assets` 只支持 generation_mode / module_key / workflow_stage 三个过滤器，**没有 project_id 参数**，且 `limit` 被 `Query(ge=1, le=100)` 硬顶在 100（877 行还是先全查再 Python 切片）；AssetLibrary.tsx:892 现在也只能 `apiFetch('/v1/assets?limit=100')`。更根本的是数据侧：AiDesignWorkflow.tsx:2170-2182 的「项目 ID」是一个放在 `<details><summary>项目与版本谱系（可选）</summary>` 里、默认空字符串、需要用户手打 project_id 的 input；commonForm 1488/1504 行 `if (projectId.trim()) form.append('project_id', ...)`——不填就不传。所以实际库里绝大多数 SceneAsset 的 project_id 是 NULL。此外 Project 模型（models.py:21-33）只有 id/name/description/created_at，没有 updated_at、没有缩略图、没有 metadata JSON 列——截图 5 要的「编辑于 3 分钟前 + 项目缩略图」和 §4 line 93「布局存 project 元数据」都无处安放；仓库也没有 alembic（find 无 migrations 目录，database.py:31 靠 `create_all` + 手写的 `_ensure_job_timing_columns()` 打补丁）。
- **后果**：第 1 步「读取当前项目全部 assets 按 lineage 布局」这句话今天一行都写不出来：既没有查询接口，也没有可查的项目归属数据。§2 line 51「后端基本不用动」和 line 53「90% 在前端」直接被推翻。
- **建议**：在第 1 步之前插入必须的后端前置：(1) `/v1/assets` 增加 projectId 过滤 + 提高/去掉 limit 上限；(2) Project 增加 updated_at / thumbnail_url / metadata JSON（按 `_ensure_job_timing_columns` 的手写迁移模式）；(3) 前端把「项目 ID」从可选手打字段改成进入画布时必然存在的上下文（新建项目即 POST /v1/projects，之后所有 job 强制带 project_id）。这三项约 2–3 天，且必须写进方案。

#### 「后端谱系已经完整」不成立：01→02 这条边在数据库里根本不存在，直传上传路径也会断链

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：§2 line 51 断言「后端 asset 谱系（parent_asset_id / approvedVersionId / lineage）**已经完整**」，§4 line 76 要画「01 原图 → 02 布局 → 03 彩平 → …」的 parent→child 连线。实际上 assets.py:476 `parent_asset_id = payload.get("asset_parent_id")` —— 父边**完全**来自前端在 FormData 里传的字段，后端不做任何推导。而 AiLayoutStage.tsx:163 只传了 `form.append('project_id', ...)`，**从不传 asset_parent_id**；FloorplanModule.tsx 里 `asset_parent_id|project_id` 全部 0 命中。所以阶段 01 资产既无 parent 也无 project_id，阶段 02 布局资产的 parent_asset_id 恒为 NULL——「01 原图 → 02 布局」这条边在库里不存在，链条最早只能从 03 开始。另外 AiDesignWorkflow.tsx:2063-2072 的直传文件输入 onChange 会调 `invalidateFromSourceChange(true)`，其 1236-1239 行清掉 layoutParentAssetId，于是用户直传图片跑出来的彩平资产 parent 也是 NULL。后端也没有反向查询：`_workflow_asset_chain`（main.py:274-291）只能从一个已审批资产往上走祖先链（线性、上限 32、跨项目报 409），没有任何 children/DAG 接口。
- **后果**：第 1 步做出来的「谱系图」在真实数据上会是一堆孤立节点 + 从第 3 阶段才开始的碎片链，恰恰做不到方案要卖的「派生关系肉眼可见」。这个问题在写完画布之前不会暴露，属于典型的第 1 步交付即翻车。
- **建议**：把「补齐 lineage 写入」列为第 1 步的前置任务：AiLayoutStage 提交时带上阶段 01 资产 id 作为 asset_parent_id；直传路径改为「直传也先落一个 asset 再作为 parent」或明确接受孤立根节点并在 UI 上表达。同时补一个 `/v1/projects/{id}/asset-graph` 返回 nodes+edges，避免前端靠 ≤100 条列表在内存里拼 DAG。

#### 「一张图 = 一个节点」在前后端都没有稳定 ID，节点身份与布局持久化无处落脚

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：§5 line 101 决定「一张图一个节点」，§4 line 93 要「布局（节点坐标 + 视口状态）存 localStorage / project 元数据」——持久化需要每个节点有稳定主键。后端没有：models.py:92-96 `job_id` 带 `unique=True`，即一个 job 恰好一个 SceneAsset；而一个彩平 job 出 4 张图（colorPlanOptions 151-172）、空间效果最多 12 张、轴侧 3 张（axonometricOptions 174-190）、风格 4 张、色调 3 张——图片层级在库里只是 `deliverables` JSON 里的数组元素，没有行、没有 ID。前端也没有：节点数据只能来自 collectRenderOutputs（AiDesignWorkflow.tsx:611-719），它是一个**启发式 JSON 爬虫**——沿 candidateKeys(707-715) 递归任意结构，`key` 是路径拼出来的字符串如 `outputs-0`（637 行），只要 result 的形状或顺序变一次 key 就漂移；683 行 `if (!seen.has(url))` 按 URL 去重，两个批次出了同一张图会塌成一个节点；`assetId` 仅在 `record.originAssetId` 存在时才有（694-697），而 originAssetId 只有 B4 重试合并路径（mergeRetryResult 846）才写入，正常批次为 undefined。ResultGallery 的 React key 是 `${output.key}-${output.url}`（1054 行）。
- **后果**：节点坐标存不住：刷新或重新拉取后 key 变了，用户手工摆的布局全丢；§1 line 32 承诺的「工作地图」会在每次刷新后重排。同时「一张图一个节点」与 parent_asset_id 的粒度对不上——lineage 边是 asset→asset（批次→批次），画布画的是 image→image，方案没说这两级怎么映射（一条批次边要展开成 4×12 条图片边？还是画成组？§5 line 101 的「同批变体自动对齐成组」暗示了组但没定义组的 ID）。
- **建议**：先定节点主键。最省的做法：`${assetId}:${variantId}`（variantId 已由后端在 outputs 里给出，见 collectRenderOutputs 691-693），并要求所有 stage 的 output 都必须带 variantId + spaceId；对拿不到 variantId 的 output 明确降级为不可持久化节点。同时在方案里补一节说明「asset 级 lineage 边 → 图片级节点边」的展开规则（建议：父批次的**被审批那一张**连到子批次的每一张，这恰好也是后端 approvedVariantId 的语义）。

#### 50–200 图片节点的性能：产物是 1024×1024/1536×1024 原图，thumbnail_url 就是原图 URL，全流程没有缩略图生成

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：§2 line 49 明确要「所有批次、所有变体同时在场，缩放俯瞰」。产物尺寸：ai_workflow.py:238-241 `_target_size` 返回 "1536x1024" / "1024x1536" / "1024x1024"，686 行硬编码 `size = "1536x1024"`。缩略图：assets.py:450-462 的 `_thumbnail()` 只是从 deliverables 里按 finalRenderUrl/previewUrl/dollhouseUrl… 顺序**挑一个已有的原图 URL 返回**，全仓库 grep 不到任何 resize/缩略图生成（processors 里的 cv2.resize 都是给 mark_image/control image 用的）；前端 ResultGallery 1059 行 `<img src={assetUrl(output.url)}>` 直接吃原图，没有 loading="lazy"、没有 srcset。一次完整跑：1 原图 + 1 布局 + 4 彩平 + 3 轴侧 + 6–12 空间 + 每空间 4 风格 + 3 色调 + 局部修改，迭代两轮就是 100–200 张。200 张 1536×1024 RGBA 解码后约 200×6.3MB ≈ 1.2GB 位图，Chrome 必然反复丢弃再解码，平移时可见闪白；同时 `transform: scale()` 的容器里挂 200 个 <img>，连续滚轮缩放会持续触发重新光栅化。这条与库选型无关，React Flow 也救不了。另有一条前端侧的放大器：pollJob（api.ts:218）是固定 1s 间隔轮询且每次 `onUpdate` 都 setJobs 触发整个 1510 行组件重渲染——画布上 K 个并发节点任务就是 K 次/秒的全画布重渲染。
- **后果**：第 1 步一上线、资产稍多就会卡到不可用，而且诊断成本高（看起来像「画布方案不行」，实际是图片管线问题）。§7 line 148 说「不改变优化计划的性能路线」在这点上是乐观的：A1 并发扇出会同时提高并发任务数，正好放大 K 次/秒全画布重渲染。
- **建议**：把「后端生成 256/512px webp 缩略图并写入 SceneAsset.thumbnail_url，原图另存 fullUrl」列为第 1 步的硬前置（约 1 天）；前端节点默认只加载缩略图，缩放超过阈值或节点被选中时才换原图。同时把轮询从 per-node 独立 1s 循环改成「一个 project 级 job 状态订阅」，或至少把节点渲染 memo 化按 jobId 隔离。这两项写进方案第 1 步。

#### §6 强制的近黑主题与 §3「节点卡片复用现有样式体系」直接矛盾：现有 5664 行 CSS 是浅色紫调，422 处硬编码颜色

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：§3 line 67 说「节点卡片复用现有样式体系」，但 §6 line 112-116 把近纯黑 #0a0a0d / 卡片 #17181c / 主色蓝 #3b82f6 定为硬需求。现有设计系统是相反的：App.css:16-21 `--surface: #ffffff; --canvas: #f4f5f7; --ink: #1e2127`，主色是紫 `--blue-600: #7a5af8`（注意变量名叫 blue 值是紫），App.css:57-63 `body { min-width: 1120px; background: radial-gradient(...rgba(154,107,255,.07)...) }`。而且颜色没有被 token 化到可以一键换肤：全文件 `var(--*)` 共 270 处，但硬编码的 `background: #/rgb/linear-gradient` 有 188 条、`color: #xxx` 有 234 条，合计 422 条散在 263 个类里；`var(--surface)` 全文件只被用了 2 次。出现频次最高的字面量是 #6d4aff(21)、#7a5af8(13)、#f1effe(11)——全是浅色紫方案。
- **后果**：第 1 步「复用现有样式」做出来的节点卡片会是白底紫边，放在近黑点阵画布上完全不是截图的样子；等到发现要改，改的是 422 处硬编码，不是换几个变量。这部分工作量（3–5 天）在三步走里完全没有条目。
- **建议**：在第 0 步之前加一个「设计令牌化」任务：把 App.css 的 422 处硬编码颜色收敛到 :root token，并新增一套深色 token；明确画布/节点卡片只使用 token。同时把 §3 line 67 的「复用现有样式体系」改成「复用现有组件结构，但样式走新的深色 token」，避免两处自相矛盾。

#### 新增的「第 0 步首页改版」没有路由可用：react-router-dom 已安装但零引用，全应用靠一个 useState 切视图

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：§6 line 142 新增第 0 步首页改版，且 line 127 要「一个项目可多张画布（画布 1 ▾）」，截图 5 有「所有项目 >」和多张最近项目卡片、截图 4 有左侧图标栏 + 精选案例瀑布流 + 推荐/最新 tab。这些都需要 URL 可寻址（/、/projects、/p/{id}/c/{n}）。但 App.tsx:1034 `const [active, setActive] = useState<ModuleId>('workflow')` —— 整个 shell 就是一个内存态模块切换，nav 是 App.tsx:1050-1070 的按钮列表，没有任何 URL 参与；package.json:22 装了 react-router-dom^7.18.2 但 `grep "from 'react-router-dom"` 在 src/ 下 0 命中（同样 0 命中的还有 @tanstack/react-query、zustand、zod、axios、react-hook-form、konva）。另外第 0 步的工作量本身被完全省略了：截图 3 的三级级联模型选择器、截图 4 的瀑布流案例墙 + 推荐/最新、截图 5 的项目卡片横排 + 风暴学堂区块，都是全新页面。
- **后果**：§6 只用一句「新增第 0 步：首页改版」带过，实际是引入路由 + 3 个新页面 + 一套左侧图标导航壳（4–6 天），并且改路由会碰到 App.tsx 全部 8 个模块的挂载方式。三步走 3-5 天的总工期里没有它的位置。
- **建议**：把第 0 步单独列出并写清子项：(0a) 引入 react-router-dom 并把现有 8 模块迁到路由（1–2 天）；(0b) 首页（问候 + 项目卡片 + 案例墙）（2–3 天，依赖 Project.updated_at/thumbnail 后端改动）；(0c) 左侧 56px 图标栏 + 顶部公告条（0.5 天）。「模型级联选择器」我们没有多模型可选，建议明确标注为暂不实现或降级为阶段选择器，不要照抄。

#### §7「与优化计划不冲突」部分成立但不完整：B3 取消 / C1 重启恢复在画布多任务下的前端侧全部失效

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：§7 line 148 说 A1 并发、增量发布、取消等「全部继续有效」。后端侧确实不冲突（cancel 是 `/v1/jobs/{id}/cancel` 按 jobId，main.py:1786；Job.started_at/finished_at 已存在，models.py:61-68，所以对该列的质疑是错的）。但前端侧的实现全是单任务假设：cancelActiveJob（AiDesignWorkflow.tsx:1571-1585）取的是 `const stage = busyStage`、`jobs[stage]`，画布上并发时它不知道该取消哪个；pollAbortRef（1161）是单个 AbortController，1584 行 `pollAbortRef.current?.abort()` 只能中断最后一个轮询；resumeWaiting（1587-1608）同样按 stage 取 `jobs[stage]`，而 jobs 只在内存里、刷新即丢（没有任何 localStorage/sessionStorage 持久化），C1 重启恢复在画布上意味着「刷新后要把 N 个在跑的任务重新挂回 N 个节点」，现在连一个都挂不回来。增量发布的「骨架卡片→节点」这条 §7 line 149 是对的，ResultGallery 1096-1113 已有 skeleton 逻辑可搬。
- **后果**：画布做出来后，「取消这个节点的生成」和「刷新后节点继续跑」这两个在多任务画布上属于基本功能的能力会缺失，而优化计划以为自己已经交付了它们，责任边界会在联调时才被发现。
- **建议**：§7 改写为：后端路线不冲突；但 B3/C1 的**前端**部分必须随画布重做——cancel 改成 per-node（节点持有自己的 jobId 与 AbortController，用 Map 管理），并把「进行中的 jobId 列表」持久化到 project 元数据或 localStorage 以支持刷新恢复。把这两项算进第 2 步工期。

#### 三步走总工期 3–5 天，按本次核对的实际工作量应为 4–7 周

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：方案给出 §4 line 73 第 1 步约 1 天、line 80 第 2 步约 1-2 天、line 90 第 3 步约 1-2 天，合计 3–5 天，且 §6 line 142 又追加了一个没估时的第 0 步。逐项按前面各条的证据重估：后端前置（assets 项目过滤 + Project 列 + lineage 补写 + 缩略图生成）2–3 天；设计令牌化 + 深色主题 3–5 天；第 0 步首页 + 路由 4–6 天；第 1 步画布（用 xyflow 3–4 天 / 自研 5–8 天）；第 2 步把 1510 行单组件（AiDesignWorkflow.tsx:1119-2629，37 个 useState）的状态机抽成 per-node API 并接节点操作条 6–10 天；第 3 步生成入画布 + 布局持久化 + per-node 任务编排/取消/恢复 5–8 天。合计 23–34 人日 ≈ 4–7 周。
- **后果**：按 3–5 天承诺给出去，会在第 1 步就超期 5 倍，且因为第 1 步的入口条件（项目过滤、lineage 边）根本不存在，最可能的失败模式是「做了一周还看不到任何能演示的东西」。
- **建议**：把方案第 4 章重写为带前置依赖的六段式（后端前置 → 令牌化 → 第 0 步首页 → 第 1 步只读画布 → 第 2 步节点操作 → 第 3 步生成入画布），每段给区间估时并标注硬依赖。第一个可演示里程碑建议定为「第 1 步只读画布（仅 03–08 的 asset 级节点 + 连线，不含小地图/框选/组拖拽）」，在后端前置完成后 3–4 天可达。


### 维度 3 — 状态时序契约：自由画布会破坏哪些不变量

共 13 条，存活 13，推翻 0。

| 级别 | 判定 | 问题 |
|---|---|---|
| 🔴 blocker | ❌ 方案错误 | 画布分支是后端明令禁止的：改批一旦有下游资产就永久 409，且没有撤销审批端点 |
| 🔴 blocker | ❌ 方案错误 | 「一张图 = 一个节点」与数据库不兼容：SceneAsset.job_id UNIQUE，一个 job 的 4~12 张图共享唯一一个审批槽 |
| 🔴 blocker | ❌ 方案错误 | 前端状态全是单槽位，画布的并发本质要求重写状态层，不是「换个入口复用 submitStage」 |
| 🔴 blocker | ❌ 方案错误 | 第 2 步列的节点操作有两条是后端直接 409 的非法组合 |
| 🔴 blocker | ◐ 方案不完整 | 03/04/05 与 06/07/08 的服务端校验强度完全不对称：一半会静默接受错图，一半会硬 409 |
| 🟠 major | ❌ 方案错误 | 「后端谱系已经完整」在链条顶端是断的：阶段 01 根本不产生资产，阶段 02 的 parent_asset_id 恒为 None |
| 🟠 major | ◐ 方案不完整 | 画布的数据源不存在：assets 只在 SUCCEEDED 时才建，且没有按项目过滤，也没有 jobs 列表端点 |
| 🟠 major | ⚠️ 方案乐观 | 「画布为主、向导为导航」低估了改造量：审批与导航在现有代码里是同一个动作；而「阶段门控」其实早已不存在 |
| 🟠 major | ◐ 方案不完整 | 基准持有的是 File 字节而非 URL，节点级操作 = 每次全尺寸下载 + 重传，且任一审批变动会让在途提交 409 |
| 🟠 major | ⚠️ 方案乐观 | 第 102 行「阶段 01 保持全屏专注模式」方向对，但双击进入/退出的代价被低估了一个数量级 |
| 🟠 major | ❌ 方案错误 | 第 7 章「与优化计划不冲突」不成立：画布把优化计划明确否决的 busyStage→Set 变成了刚需，并放大未修的 SQLite 写竞争 |
| 🟠 major | ◐ 方案不完整 | 画布持久化没有落脚点：Project 没有 metadata 列，多画布需要新表，且没有 alembic |
| 🔴 blocker | ⚠️ 方案乐观 | 工期估算 3-5 天严重偏低，实际量级为 15-25 人日，且顺序与方案给的相反 |

#### 画布分支是后端明令禁止的：改批一旦有下游资产就永久 409，且没有撤销审批端点

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：main.py:979-986 — approve 端点在写入新 approvedVariantId 前先查 `select(SceneAsset).where(SceneAsset.parent_asset_id == asset.id)`，若已有下游且当前 approvalStatus == 'approved'，直接 `HTTPException(409, "该批准版本已有下游资产，不能直接改批；请从新方案创建新的工作流分支")`。main.py:970-978 只对「重批同一 variant」做幂等放行。全仓 grep `unapprove|revoke|reset_approval` 在 main.py / assets.py 无任何命中——审批是单向闩锁，没有撤销路径。审批状态还会被 assets.py:492-510 在 job 重跑时主动保留。
- **后果**：方案第 1 章的核心卖点「谱系是空间化的：原图 → 风格 A → 调色 B 在画布上自然铺开」（方案第 32 行）恰恰是后端禁止的动作。画布上最自然的手势——「这批空间图我先从 A 派生风格，再回头从 B 也派生一个」——第二次点击必然 409，而 UI 无法提供任何补救（没有反审批，唯一出路是重跑整个上游 job 生成新 asset，即重新烧一次 4~12 张图的钱和时间）。用户会把这理解为「画布坏了」。
- **建议**：这是先决条件，不是收尾项：必须先在后端引入分支模型（每个 asset 支持多条 approval 记录，或新增 `POST /assets/{id}/branch` 从同一 job 复制出一个新 SceneAsset 行并指定不同 approvedVariantId），再谈画布。在此之前画布只能做只读谱系视图（第 1 步），第 2 步的「设为基准 / 批准」入口必须对已有下游的节点置灰，并明确提示原因。

#### 「一张图 = 一个节点」与数据库不兼容：SceneAsset.job_id UNIQUE，一个 job 的 4~12 张图共享唯一一个审批槽

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：models.py:92-96 `job_id` 带 `unique=True`；一个 AI_SPACE_RENDER job 最多产出 12 张图（main.py:1377-1383 `_csv_values(..., maximum=12)`，ai_workflow.py:727-737 每个 room 一条 output，variantId=`space_{room_id}`），彩平最多 4 个 variant（schemas.py:251-258）。审批字段 approvalStatus/approvedVariantId/approvedVersionId 写在 asset 行上（main.py:990-999），因此一个 asset 只能有一个「已批准」的 variant。节点级操作要的 assetId 在前端是从 `output.assetId ?? result.assetId` 取的（AiDesignWorkflow.tsx:1758-1760），12 个节点指向同一个 assetId。
- **后果**：第 5 章决策 2「一张图一个节点」在视觉层可行，在数据层不可行。12 个空间节点共用一个审批槽 → 用户给客厅节点点「设为风格基准」后，卧室节点上的同一个按钮只会把客厅的批准抢走（且叠加上一条发现后是直接 409）。方案第 51 行「后端 asset 谱系已经完整、直接映射为节点连线」在节点粒度上不成立：连线的终点只有 asset 级，画布要画的是 output 级。
- **建议**：明确写下节点的两级模型：canvas node = (assetId, variantId) 复合键，只有 asset 级操作（审批/派生）才需要 assetId。同时把「一个 asset 多个已批准 variant」列为后端必改项；否则第 5 章决策 2 应改为「一批变体 = 一个节点组，审批发生在节点组上」，这与截图里的单图节点样式冲突，需要用户拍板。

#### 前端状态全是单槽位，画布的并发本质要求重写状态层，不是「换个入口复用 submitStage」

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：AiDesignWorkflow.tsx:1164 `jobs: Partial<Record<GenerationStage, Job>>`（每阶段仅 1 个 job）；:1168 `busyStage: GenerationStage | null` 且 :1611 `if (!isGenerationStage(activeStage) || busyStage !== null) return` 全局互斥；:1157-1159 spaceBaseline/styleBaseline/toneBaseline 各仅一个；:1160 `localEditAnnotatorRef` 单一红笔标注器；:1169-1172 approvingVariantId / selectingBaselineKey 单值；:1182-1197 downstreamSourceBaseline 由 `activeGenerationStage` 三元推导；:1896-1907 canSubmit 也是围绕 activeGenerationStage 的单一派生布尔。整个 commonForm（:1469-1515）以 activeStage 为唯一入参来源。
- **后果**：方案第 87 行「审批/设基准/重试直接复用现有 approveGeneratedColorPlan / selectApprovedBaseline / submitStage 逻辑，只是入口从阶段面板换成节点操作条」是错的。这些函数全部隐式依赖「当前只有一个活跃阶段、一个基准、一个 job」。画布上同时对客厅节点跑风格、对卧室节点跑风格，在现有状态模型里结构性不可能：busyStage 会拒掉第二次提交，jobs.style 会被后提交的覆盖，spaceBaseline 会被 :1827-1838 的级联清空互相踩掉。
- **建议**：第 2 步之前先插入一个「状态层重构」阶段：把 jobs 改成 `Map<nodeId, Job>`、busyStage 改成 `Set<jobId>`、三个 baseline 改成挂在节点上的派生值（node → 其 asset 的 approvedVersionId），并把 commonForm 从「读 activeStage 全局」改成「入参 = 源节点」。这一步单独就是 3-5 天，且必须先做，否则第 2 步做出来的是「画布形状的向导」。

#### 第 2 步列的节点操作有两条是后端直接 409 的非法组合

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：main.py:665-667 tone_scheme 要求 `parent_module == 'ai_workflow' and parent_stage == 'style_scheme'`，否则 409「色调方案的上游资产必须是已批准的风格方案」。main.py:668-670 local_edit 要求 `parent_stage == 'tone_scheme'`，否则 409「局部修改的上游资产必须是已批准的色调方案」。方案第 84-86 行却写：「空间节点：设为风格基准 / 生成风格方案 / **生成色调方案**」「**风格**/色调节点：… 局部修改」。
- **后果**：照方案第 2 步实现，空间节点上的「生成色调方案」和风格节点上的「局部修改」两个按钮 100% 报 409。这不是边界情况，是列在方案正文里的默认操作集。
- **建议**：把节点操作表直接从 main.py:648-672 的 `_validate_parent_asset` 生成（color_plan←layout/floorplan、axonometric←color_plan、space_render←color_plan|axonometric、style←space_render、tone←style、local_edit←tone），不要手写。右键菜单里非法项按截图 1 的范式置灰（disabled）而不是隐藏。

#### 03/04/05 与 06/07/08 的服务端校验强度完全不对称：一半会静默接受错图，一半会硬 409

`🔴 blocker` ｜ `◐ 方案不完整`

- **证据**：06/07/08 走 `_validate_parent_approved_space`（main.py:676-760，调用点 1455/1525/1586）：校验 source_space_version_id 必须等于父资产当前 approvedVersionId（:702-706），并对上传字节做 **SHA256 逐字节比对**（:739-755，不符则 409「上传的空间图与上游资产当前批准输出不一致」）。而 03/04/05 完全没有这层：`asset_parent_id` 是 Optional（main.py:1232/1294/1363），`approved_layout_version_id` 是 Optional 且只被原样存进 payload（:1258/:1321/:1392，全仓无任何比对），`layout_approved` 只是客户端自报的布尔（schemas.py:239-246 仅断言它为 True）。
- **后果**：画布上「对任意节点随时动手」时：对 03/04/05 传错源图会**静默成功**并产出一个谱系错误的资产（甚至不传 asset_parent_id 就绕过全部审批检查、生成孤儿节点，assets.py:476-483 找不到父就直接置 None）；对 06/07/08 会硬 409。目前唯一防住 03/04/05 用陈旧输入的是纯前端的级联失效：invalidateFromSourceChange（:1223-1240）、replaceApprovedColorPlan（:1242-1257）、withoutStageJobs（:1832-1852）。画布一旦允许对旧节点直接操作，这套级联失去触发点，防线归零。
- **建议**：在启动画布第 2 步之前，把 03/04/05 补齐到与 06/07/08 同级：asset_parent_id 改必填、approved_layout_version_id / approved_color_plan_version_id 做 approvedVersionId 相等性校验 + 上传字节 SHA256 比对。这是后端工作，方案「90% 在前端」（第 53 行）因此不成立。

#### 「后端谱系已经完整」在链条顶端是断的：阶段 01 根本不产生资产，阶段 02 的 parent_asset_id 恒为 None

`🟠 major` ｜ `❌ 方案错误`

- **证据**：assets.py:44-57 `JOB_TYPE_TO_MODULE` 不含 `FLOORPLAN_ANALYZE`，assets.py:468 `if job.type not in ASSET_JOB_TYPES ... return None` → 阶段 01 的分析任务**不产生任何 SceneAsset**。main.py:1137-1160 `create_ai_layout_job` 的表单参数里**没有 asset_parent_id**（对比 1232/1294/1363），job 只有 `parent_job_id=analysis_job.id`（:1210），因此 assets.py:476 取不到 asset_parent_id，阶段 02 的 layout 资产 `parent_asset_id` 永远是 None。
- **后果**：方案第 1 步写「按 lineage 自动布局：01 原图 → 02 布局 → 03 彩平 → …，贝塞尔连线表示 parent → child」，但 01 节点在数据里不存在，01→02 这条线没有数据源，02→03 这条线是画布上第一条真实连线。方案第 51 行「后端 asset 谱系已经完整」与第 53 行「90% 在前端」都建立在这个错误认知上。
- **建议**：要么给 FLOORPLAN_ANALYZE 建资产并给 /layouts/ai 加 asset_parent_id（后端改动 + 历史数据 backfill），要么在方案里明确降级为「画布从阶段 02 开始，01 是画布外的入口」。另外注意 job 侧已有 `parent_job_id`（models.py:49-52），可作为 01→02 连线的过渡数据源。

#### 画布的数据源不存在：assets 只在 SUCCEEDED 时才建，且没有按项目过滤，也没有 jobs 列表端点

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：assets.py:468 要求 `job.status == 'SUCCEEDED'`，assets.py:487-488 无 thumbnail 直接 `return None` → QUEUED/RUNNING/FAILED/CANCELED 的任务在 /v1/assets 里完全不可见。main.py:830-878 的 `GET /v1/assets` **没有 project_id 参数**，且 module_key/workflow_stage 是在取回全表后做 Python 侧过滤（:869-877），再 `values[offset:offset+limit]` 切片——带过滤时分页语义已经是错的。main.py:1749 只有 `GET /jobs/{job_id}`，全仓没有 `response_model=list[JobRead]` 的列表端点。
- **后果**：第 1 步「读取当前项目全部 assets」缺一个 project 过滤参数（后端改动）；第 3 步「增量发布时节点从骨架态渐变为图片、BatchProgress 挂到节点上」需要读运行中 job 的 result，而画布重新打开后无法知道本项目有哪些在跑的 job——只能靠内存里的 jobs 状态，刷新即丢。方案第 149 行「增量发布的骨架卡片天然变成画布上的骨架节点」因此不成立。
- **建议**：补两个端点：`GET /v1/assets?projectId=`（并把过滤下推到 SQL 修正分页），以及 `GET /v1/jobs?projectId=&status=`。后者也正好是《优化计划》C1 重启回收后展示 WORKER_LOST 节点所必需的，两件事应合并做。

#### 「画布为主、向导为导航」低估了改造量：审批与导航在现有代码里是同一个动作；而「阶段门控」其实早已不存在

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：setActiveStage 被当作审批的副作用触发：acceptStageOne :1274 → 'layout'，acceptStageTwo :1308 → 'color_plan'，selectApprovedBaseline :1838/:1847/:1853 → 'style'/'tone'/'local_edit'。反过来，阶段条本身**没有任何门控**：workflowStages 全部 `state: 'available'`（:94-102），:1942 `isOperational = stage.state !== 'planned'`，:1996 `disabled={!isOperational}` → 8 个按钮永远可点，:1943-1990 只是把前置条件渲染成文案（「等待阶段 05」）。真正的不变式只有 canSubmit（:1896-1907）。
- **后果**：两个方向都被误判。一方面「向导退化为导航面板」不是删掉一个视图那么简单：要把 5 处「审批即跳转」的耦合拆开，否则画布上批准一个节点会莫名其妙把左侧导航跳走。另一方面「阶段门控会被画布绕过」这个担心是多余的——自由跳转今天就允许，真正要在画布上保住的只有 canSubmit 这一条派生布尔，而它依赖的恰恰是第 3 条发现里那些单槽位状态。
- **建议**：在第 2 步里把「审批」与「导航」显式拆成两个动作：approve 只更新数据，节点聚焦交给画布的 focusNode 动画（方案第 88 行已经提到）。并把 canSubmit 从全局派生布尔重构成 `canRunAction(sourceNode, action)` 的纯函数，这也是右键菜单置灰状态的唯一真值来源。

#### 基准持有的是 File 字节而非 URL，节点级操作 = 每次全尺寸下载 + 重传，且任一审批变动会让在途提交 409

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：VisualBaseline 存 `file: File`（:1157-1159 类型见 :83），commonForm 直接 `form.append('source_space_image', baseline.file)`（:1482）。取得这个 File 的唯一途径是 `workflowAssetImageFile(output.url, ...)` 全量下载（:1788-1791、:1689）。服务端再对这份字节做 SHA256 比对（main.py:749-755）。同时 main.py:702-706 要求提交的 source_space_version_id 等于父资产**当前**的 approvedVersionId。
- **后果**：画布上「选中节点 → 生成风格」每次都要先把原图完整下载一遍再上传，节点越多越贵；更麻烦的是并发场景：用户在 A 节点的风格任务在途时改批了同一个 asset 的另一个 variant，在途任务对应的 version 已过期，服务端返回 409「提交的空间图版本不是上游资产当前批准版本，请重新选择基准图」。方案没有任何地方处理「节点操作因上游审批变动而失效」这个画布特有的竞态。
- **建议**：第 2 步必须定义节点操作的乐观锁语义：提交时携带 approvedVersionId，收到 409 时在节点上渲染「上游已变更，点此重新绑定基准」而不是弹全局错误。长期应加 `source_asset_url + version_id` 的服务端内取方式，免掉下载/重传往返。

#### 第 102 行「阶段 01 保持全屏专注模式」方向对，但双击进入/退出的代价被低估了一个数量级

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：今天 FloorplanModule 和 AiLayoutStage 是用 `hidden={activeStage !== ...}` **常驻挂载**的（AiDesignWorkflow.tsx:2009-2011、2030-2032），从不卸载。FloorplanModule.tsx 内部持有约 30 个本地 state，其中 `file: File | null`（:877）、`analysis`（:891）、`semanticDraft`（:892）、`semanticReviewConfirmed`（:893）、Konva 编辑器的 scene/walls/roomSelection/mode/dragStart/dragCurrent（:903-908）全部是组件本地、无 store、无持久化，且 File 对象**无法**序列化进 localStorage。AiLayoutStage 则用 `key={stageOneRevision}`（:2035）在每次阶段 01 重新批准时强制重挂载。
- **后果**：「双击节点进入全屏、编辑完回画布」只有两种实现：(a) 卸载/重挂 → 用户上传的原图 File、识别结果、未提交的 P1-P4 编辑全部丢失，退出再进入等于从头来；(b) 保持常驻 → 画布页面永久携带 3094 行的 FloorplanModule + Konva Stage，与「画布为主」的轻量首屏相矛盾。方案第 3 步「布局（节点坐标 + 视口状态）存 localStorage / project 元数据」也帮不上忙，因为丢的是 File 和 Konva 状态，不是坐标。另外 stageOneRevision 的重挂载语义（阶段 01 重批 → 阶段 02 整个作废）在画布上没有对应物，不复刻就会留下陈旧的 02 节点。
- **建议**：决策保留（重型 SVG/Konva 编辑器确实不该塞进节点），但把「把 FloorplanModule 的状态提升到一个 zustand store（依赖已在 package.json:23）+ 原图改为上传后即得服务端 URL 而非常驻 File」列为该决策的前置工作，单独估 2-3 天。同时把 stageOneRevision 的级联作废翻译成画布上的「上游节点已变更 → 下游节点标记为陈旧」视觉状态。

#### 第 7 章「与优化计划不冲突」不成立：画布把优化计划明确否决的 busyStage→Set 变成了刚需，并放大未修的 SQLite 写竞争

`🟠 major` ｜ `❌ 方案错误`

- **证据**：优化计划.md:312 在「明确不建议现在做的」表里列了 `busyStage` 改成 Set，理由是「A1 落地后大部分冗余」，并警告改动要同时处理 `run()` 的 `finally { setBusyStage(null) }`（现位于 AiDesignWorkflow.tsx:1566）否则先完成的阶段会为仍在运行的阶段解锁提交。优化计划.md:789（附录 B3）记录 SQLite 无 WAL、无 busy_timeout 覆盖、15 连接池，并发写会 database is locked；对应的 C3「拆分终态事务 + 开启 WAL」（:218）尚未完成（database.py:20-25 确认 create_engine 无任何 WAL/timeout 设置）。
- **后果**：画布的核心体验就是同时对多个节点动手（截图 1 的分支 DAG 正是这个意思），这直接反转了优化计划 :312 的决策，并把它警告的解锁 bug 变成必然遭遇。同时 A1 的变体级并发 × 画布的任务级并发会让并发写次数相乘，在 C3/WAL 未落地前撞上 database is locked 的概率显著上升。方案第 148 行的「不改变性能路线」因此是错的：它改变了优先级排序。
- **建议**：把第 7 章改写为：画布依赖 C1（重启回收，否则刷新后画布上的在途节点永久卡死）、C3（WAL，否则并发节点写库报错）、以及被否决的 busyStage→Set（现在必须做）。建议顺序改为 A1 → C1 → C3 → 状态层重构 → 画布第 1 步。

#### 画布持久化没有落脚点：Project 没有 metadata 列，多画布需要新表，且没有 alembic

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：models.py:21-34 Project 只有 id / name / description / created_at，**没有任何 JSON 元数据列**。全库没有 canvas 相关表。database.py:28-54 `init_db` 用 `Base.metadata.create_all` + 一个手写的 `_ensure_job_timing_columns()` PRAGMA 迁移（注释明写「无 alembic 的轻量迁移」）——加表/加列都要手写迁移。
- **后果**：第 3 步「布局（节点坐标 + 视口状态）存 localStorage / project 元数据」的后一半没有存储位置；第 6 章第 127 行由截图 1 引入的硬需求「一个项目可多张画布（画布 1 ▾）」需要一张 canvases 表 + 节点位置表（或 canvas.layout JSON），并要在无迁移框架下手写升级路径。这两项都不在三步走的工时里。
- **建议**：明确写入方案：新增 `canvases(id, project_id, name, viewport_json, created_at)` 与 `canvas_nodes(canvas_id, asset_id, variant_id, x, y)`（后者也顺带解决第 2 条发现的 (assetId, variantId) 复合节点标识），并在 database.py 的手写迁移里加对应分支。仅 localStorage 无法满足「多画布 + 换设备」。

#### 工期估算 3-5 天严重偏低，实际量级为 15-25 人日，且顺序与方案给的相反

`🔴 blocker` ｜ `⚠️ 方案乐观`

- **证据**：方案第 73/80/90 行给出 1 天 + 1-2 天 + 1-2 天。但据上述取证，前置的后端改动至少包含：assets 加 projectId 过滤并修正分页（main.py:830-878）、新增 jobs 列表端点（main.py 现仅 :1749 单条）、03/04/05 补齐父资产字节校验对齐 main.py:676-760、多 approval / 分支端点（main.py:979-986 的限制）、01 资产化与 /layouts/ai 加 asset_parent_id（main.py:1137）、canvases 表与手写迁移（database.py:35-54）。前端侧包含状态层重构（jobs/busyStage/三个 baseline/localEditAnnotatorRef 全部单槽位，AiDesignWorkflow.tsx:1157-1172）与 FloorplanModule 约 30 个本地 state 的提升（FloorplanModule.tsx:877-908）。另外 package.json 中**没有** react-flow/xyflow/tldraw，方案 A 的 CanvasViewport 确需自研；第 6 章从截图新增的首页改版（第 142 行「新增第 0 步」）完全没有工时。
- **后果**：按 3-5 天排期启动，最可能的结果是第 1 步（只读画布）勉强做出来但连线数据源不全（第 6 条发现），第 2 步在遇到 409 改批限制（第 1 条发现）后停摆，最后交付一个既不是向导也不是画布的中间态。
- **建议**：重排为：阶段 0 后端解锁（分支/多审批 + 03-05 校验对齐 + 两个列表端点 + canvases 表）约 5-8 天；阶段 1 前端状态层重构约 3-5 天；阶段 2 只读画布 + 连线约 2-3 天；阶段 3 节点操作 + 右键菜单/浮动工具条约 3-4 天；阶段 4 首页改版（第 6 章第 142 行的第 0 步）约 2-3 天。并把「后端基本不用动 / 90% 在前端」（第 51、53 行）从方案里删掉——它是本方案最主要的误导来源。


### 维度 4 — 参考图能力缺口：5 张截图 vs 方案第 6 章覆盖度

共 15 条，存活 13，推翻 2。

| 级别 | 判定 | 问题 |
|---|---|---|
| 🔴 blocker | ◐ 方案不完整 | Chapter 6 is a mood board, not a spec: of ~40 distinct capabilities in the 5 screenshots, the plan concretely covers 4 |
| 🔴 blocker | ❌ 方案错误 | "节点卡片复用现有样式体系" is factually wrong — the entire app is a light purple theme with zero dark support |
| 🔴 blocker | ◐ 方案不完整 | 画布 1 ▾ (multi-canvas per project) has no table, no column, no route — and there is no migration tool |
| 🔴 blocker | ❌ 方案错误 | Homepage project cards are unbuildable from the current API, and "现有我的资产演进" mischaracterizes AssetLibrary |
| 🔴 blocker | ❌ 方案错误 | The canvas DAG will render as disconnected fragments: stage 01 produces no asset and stage 02 has no parent link |
| 🔴 blocker | ~~已推翻~~ | ~~复制节点/创建副本/粘贴/删除 have no addressable entity and no endpoint — the node model the screenshots require does not exist~~ |
| 🟠 major | ◐ 方案不完整 | The entire right-click + keyboard-shortcut + clipboard layer is greenfield: zero such handlers exist in 15.6k lines of frontend |
| 🟠 major | ~~已推翻~~ | ~~「优化工作流布局」 (auto-layout command) is in the reference menu but nowhere in the plan's command set, and it conflicts with step 3's dra …~~ |
| 🔴 blocker | ⚠️ 方案乐观 | The 风暴功能 function-card marketplace is one plan bullet, but our 6 stages are gated pipeline steps that cannot be invoked as free-st … |
| 🟠 major | ⚠️ 方案乐观 | The homepage cascading 3-level model picker and AI chat entry have no backend, no second model, and no chat route |
| 🟠 major | ◐ 方案不完整 | ~40% of the screenshot chrome is SaaS monetization/community surface for a product that is explicitly a local single-user pilot |
| 🟠 major | ◐ 方案不完整 | The bottom floating toolbars contain ~13 distinct tools across the two screenshots; the plan lists 7 and budgets 0, and canvas ima … |
| 🟠 major | ❌ 方案错误 | The top-center hint's semantics are misread: 「点击图片上的物体进行标记」 is click-to-select-object, not our free-hand red pen |
| 🟠 major | ⚠️ 方案乐观 | "自研约 300 行的 CanvasViewport" understates step 1: the screenshot's canvas shell alone is 5 sub-systems |
| 🔴 blocker | ⚠️ 方案乐观 | Bottom line on effort: the plan says 3-5 days; the screenshot-as-spec scope is 20-30 person-days |

#### Chapter 6 is a mood board, not a spec: of ~40 distinct capabilities in the 5 screenshots, the plan concretely covers 4

`🔴 blocker` ｜ `◐ 方案不完整`

- **证据**：无限画布改造方案.md:106-143 is the entire reference-UI chapter (38 lines, ~14 bullets). Classifying every capability visible in the 5 screenshots against it: (a) COVERED (4): dark palette + dotted background (line 112-113), rounded/capsule radius language (114), bezier parent→child edges (130), node card w/ title bar + image area (129). (b) MENTIONED BUT UNDERESTIMATED (14): left icon rail (120); greeting + AI input (121); project card row (123); masonry 精选案例 (124); 画布 1 ▾ multi-canvas (127); left function panel w/ categories + search + card grid (128); right-click menu w/ shortcut labels (131); bottom floating toolbar (132); 资产管理 entry (133); zoom % (133); minimap (134); share + Agent (135); top-center contextual hint (136); announcement bar (115). (c) OMITTED ENTIRELY (23): ⚡credit balance (25/20) and 普通用户/升级 membership state; 限时40折 promo pill; phone icon; the two view-mode toggle buttons next to 画布 1 ▾; ★ favorite on function cards; ⊕ New Folder in the function panel; function-card thumbnails; 推荐/最新 tabs; 风暴学堂/讲师课程/讲师作品; 限时赛事 competition card; PS插件/创作者挑战赛/加入合伙人/邀请有礼 pills; 所有项目 > link; "编 …
- **后果**：The user believes the screenshots are covered because Ch.6 name-checks them. Execution will start on step 1 (1 day), and every (b)/(c) item surfaces mid-build as an unplanned request. The three-step schedule (3-5 days) is not a schedule for this UI.
- **建议**：Split Ch.6 into a real capability table with three columns: 必做 / 降级 / 不做, and get a per-row ruling from the user BEFORE any code. In particular force a decision on the 23 omitted items — most are SaaS monetization/community surfaces (see separate finding) that a local single-user pilot has no product behind.

#### "节点卡片复用现有样式体系" is factually wrong — the entire app is a light purple theme with zero dark support

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：apps/web/src/App.css:6-49 defines the only theme: `--surface: #ffffff`, `--canvas: #f4f5f7`, `--ink: #1e2127`, accent `--blue-600: #7a5af8` (purple, the comment literally says "ark-style purple accent"). App.css:56-63 sets `body { min-width: 1120px; background: radial-gradient(...rgba(154,107,255,.07)...) }`. `grep -n "prefers-color-scheme|dark" App.css` → 0 hits across all 5664 lines. The screenshots require ≈#0a0a0d background and #3b82f6 blue, which the plan itself states at 无限画布改造方案.md:112-116 — while line 67 simultaneously claims "节点卡片复用现有样式体系".
- **后果**：Nothing in the existing style system is reusable for the target look. Worse, the plan keeps the wizard alive inside the canvas (line 88 "阶段向导保留为左侧可收起面板") and puts FloorplanModule into a fullscreen focus mode (line 102) — so AiDesignWorkflow.tsx (2629 lines), FloorplanModule.tsx (3270 lines) and AssetLibrary.tsx (1748 lines) all render inside the dark shell and all must be re-themed. Budgeted: 0 days.
- **建议**：Add an explicit dark-theme workstream. Either (i) invert the token block in App.css:6-49 and then fix the ~200 hard-coded color literals per component (5-8 days, verified by grepping color literals in each file), or (ii) scope dark to a new canvas-only stylesheet and keep the wizard/asset library light behind a modal boundary (1-2 days, but the user will see two visual languages). Ask the user which; do not assume (i) is free.

#### 画布 1 ▾ (multi-canvas per project) has no table, no column, no route — and there is no migration tool

`🔴 blocker` ｜ `◐ 方案不完整`

- **证据**：apps/api/app/models.py:21-34 — `Project` is exactly `id / name / description / created_at`. No canvas table, no metadata JSON, no updated_at. main.py exposes only `POST /v1/projects` (main.py:812-823) and `GET /v1/projects` (main.py:825-828); there is no PATCH, no DELETE, no canvas route anywhere in the 25-route list. Plan line 93 says layout is persisted to "localStorage / project 元数据" — the column does not exist. apps/api/app/database.py:28-31 shows schema is created with `Base.metadata.create_all`, and database.py:33-40 (`_ensure_job_timing_columns`, a hand-written PRAGMA/ALTER helper) proves there is no Alembic: every new column needs a bespoke migration function.
- **后果**：The 画布 1 ▾ pill, the canvas switcher, per-canvas viewport state and per-canvas node coordinates cannot be built or persisted. localStorage "solves" it only for one browser on one machine and breaks the moment there are 2 canvases in 1 project, which is exactly what the screenshot shows.
- **建议**：Budget a backend slice: `canvases(id, project_id, name, ordinal, viewport_json, created_at, updated_at)` + `canvas_nodes(id, canvas_id, asset_id, output_index, x, y, w, h, title, created_at)` + CRUD routes + a hand-written SQLite migration in database.py alongside `_ensure_job_timing_columns`. 3-4 days. If the user will accept it, ship v1 as single-canvas-per-project and hide the pill — but say so explicitly rather than letting line 127 imply it is free.

#### Homepage project cards are unbuildable from the current API, and "现有我的资产演进" mischaracterizes AssetLibrary

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：Screenshot 5 needs per-project thumbnail + "编辑于 3 分钟前". `ProjectRead` (apps/api/app/schemas.py:27-30) returns only `id/name/description/created_at` — no updated_at (models.py:31-34 has no such column), no thumbnail, no asset count. `GET /v1/assets` (main.py:830-877) has NO project filter parameter at all (only generationMode/moduleKey/workflowStage) and hard-caps `limit` at 100 (main.py:833). It also calls `backfill_scene_assets(session)` on every single request (main.py:857), which re-scans and re-writes every successful job in the DB (apps/api/app/assets.py:558-578). On the frontend there is no project UI whatsoever: project id is a free-text `<input>` buried in a collapsed `<details>` (AiDesignWorkflow.tsx:2170-2182) — no create, no list, no switch. Plan line 94 claims the project wall is an evolution of "我的资产", but AssetLibrary.tsx:1263-1344 is a two-pane asset list + detail keyed by module (assetModules, AssetLibrary.tsx:40-70), not a project browser.
- **后果**：Step 3's "多项目管理：首页保持项目卡片墙" (line 94) and the whole 第 0 步 homepage cannot be implemented without new backend work the plan says is unnecessary ("后端基本不用动", line 51). A canvas that polls /v1/assets also triggers a full-table backfill each poll.
- **建议**：Add: `Project.updated_at` + `Project.cover_asset_id` (with a hand-written migration), `GET /v1/projects` returning cover thumbnail + updated_at + asset count, `GET /v1/assets?projectId=&limit=` with the project filter pushed into the SQL WHERE, and move `backfill_scene_assets` out of the hot GET path. 1.5-2 days backend before any homepage pixel is drawn.

#### The canvas DAG will render as disconnected fragments: stage 01 produces no asset and stage 02 has no parent link

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：Screenshot 1's defining visual is a connected branching DAG. Plan line 76 promises "01 原图 → 02 布局 → 03 彩平" columns wired by parent→child bezier, and line 85 promises an "原图节点" with 「重新识别」. But: (1) `FLOORPLAN_ANALYZE` is absent from `JOB_TYPE_TO_MODULE` (apps/api/app/assets.py:44-57), and `ensure_scene_asset` returns None for any job type not in `ASSET_JOB_TYPES` (assets.py:468) — stage 01 creates NO SceneAsset, so there is no 原图 node, no id, no thumbnail to draw. (2) Stage 02 `/v1/layouts/ai` (main.py:1132-1214) sets `parent_job_id=analysis_job.id` (main.py:1210) but never passes `asset_parent_id`; `ensure_scene_asset` reads the parent only from `payload["asset_parent_id"]` (assets.py:476-483), so every LAYOUT_AI asset has `parent_asset_id = NULL`. (3) `scene_asset_read` (assets.py:596-612) does not expose the job's `parent_job_id` either, so the frontend cannot even fall back to job lineage.
- **后果**：Plan step 1 ("约 1 天，按 lineage 自动布局 + 贝塞尔连线") ships a canvas where the first two stages are missing/orphaned and only 03→08 is chained. This directly contradicts "后端谱系已经完整…直接映射为节点连线" (line 51-53) and the mandatory screenshot look.
- **建议**：Before step 1: register FLOORPLAN_ANALYZE in JOB_TYPE_TO_MODULE (or synthesize a virtual root node from the job), thread `asset_parent_id` into `/v1/layouts/ai`, and add `parent_job_id` to `scene_asset_read`. Then backfill existing rows. ~1 day backend, and it must land first or step 1 has nothing to draw.

#### ❌【已推翻】复制节点/创建副本/粘贴/删除 have no addressable entity and no endpoint — the node model the screenshots require does not exist

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：Plan line 101 decides "一张图 = 一个节点" and line 131 lists 复制节点 ⌘C / 创建副本 ⌘D / 粘贴 ⌘V / 删除 ⌘⌫ as a styling bullet. But `SceneAsset.job_id` carries a UNIQUE constraint (models.py:92-96) and `ensure_scene_asset`'s own docstring is "Create exactly one local asset for every successful visual module job" (assets.py:465-466). A color-plan job stores N images inside one asset as `deliverables.outputs` (assets.py:258-293, `outputUrls`/`outputs`). So an image-node's only identity is the client-side pair (assetId, outputIndex) — it has no server id, cannot be targeted by `/v1/assets/{asset_id}/approve` (main.py:889-914) or any other route. Separately, `grep -n "delete" main.py` returns zero route hits: there is no DELETE endpoint for assets, jobs or projects anywhere in the 1794-line file. Screenshot 1's "图片节点 2 - 副本" also implies node rename, and `SceneAsset.title` is overwritten from the job on every backfill (assets.py:511, 514-516), so a user rename would be silently reverted.
- **后果**：Every write-side node operation in the reference right-click menu (副本, 粘贴, 删除, rename) is unimplementable against the current schema/API. Step 2 ("1-2 天") only budgets read-side operations (approve / set-baseline / retry) and never notices this.
- **建议**：Introduce the `canvas_nodes` table proposed above as the real node identity (node = row referencing asset_id + output_index + user-owned x/y/title), so duplicate/paste/delete/rename mutate canvas rows and never touch generated assets. Then 删除 means "remove from canvas", not "delete asset" — which also sidesteps needing a destructive asset DELETE. Add this to step 2's scope: +2 days.
- **🚫 驳回理由**：The load-bearing evidence is a misread. The finding asserts 'an image-node's only identity is the client-side pair (assetId, outputIndex) — it has no server id, cannot be targeted by /v1/assets/{asset_id}/approve'. That is false: every output carries a server-side variantId (collected into metadata.variantIds at assets.py:346-355, stored in deliverables.outputs at assets.py:259-276), and the approve route takes exactly that — SceneAssetApprovalRequest.variant_id (schemas.py:123-125) — and validates it against result.outputs[].variantId with status=='succeeded' (main.py:951-964). So a single image IS server-addressable as (asset_id, variantId), and the plan's '一张图 = 一个节点' maps cleanly onto the existing approve/baseline APIs, which is the only write path step 2 actually needs. The remaining true bits are either moot or duplicative: no DELETE route exists (confirmed — zero delete routes in …

#### The entire right-click + keyboard-shortcut + clipboard layer is greenfield: zero such handlers exist in 15.6k lines of frontend

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：`grep -rn "onContextMenu|keydown|metaKey|ctrlKey" apps/web/src` → 0 matches across all 8 source files (15,664 lines total incl. CSS). Screenshot 1's menu needs: a dark floating panel, 9 items, 2 separators, right-aligned ⌘-shortcut labels, and 3 items rendered in a DISABLED state (Seedance2.0合规校验 / 保存到我的资产 / 创建主体) — disabled state implies per-node eligibility rules that the plan never defines. Plan line 141 covers all of this in half a sentence inside step 2's 1-2 day budget.
- **后果**：Underestimated by roughly an order of magnitude. A global keydown dispatcher is also risky here: AiDesignWorkflow.tsx and FloorplanModule.tsx are full of text inputs and textareas (e.g. AiDesignWorkflow.tsx:2175, 2481), so ⌘C/⌘V/⌘⌫ must be suppressed while focus is in an editable element or the user will delete nodes while typing a prompt.
- **建议**：Budget a discrete 2-3 day slice: ContextMenu component (items/separators/shortcut column/disabled+reason tooltip), a shortcut dispatcher with an `isEditableTarget()` guard, an in-memory clipboard model, and 复制到剪贴板 via `navigator.clipboard.write(new ClipboardItem(...))` on a fetched image blob (which needs a same-origin/CORS check against API_BASE — api.ts:1-2 shows the API is on a different origin, 127.0.0.1:8000, than the Vite dev server).

#### ❌【已推翻】「优化工作流布局」 (auto-layout command) is in the reference menu but nowhere in the plan's command set, and it conflicts with step 3's draggable nodes

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：Plan line 76 has "按 lineage 自动布局（按阶段分列）" as a one-time initial placement in step 1, and line 93 makes nodes draggable with coordinates persisted in step 3. Neither is the same thing as the on-demand 优化工作流布局 menu command in screenshot 1, which must re-flow a graph whose coordinates are already user-owned. No graph-layout library is available: apps/web/package.json:12-24 lists no dagre/elkjs/d3-hierarchy; the only graphics deps, konva ^10.3.0 and react-konva ^19.2.5, are entirely unused (`grep -rn konva apps/web/src` → 0 hits), as are zustand, @tanstack/react-query, react-router-dom and zod.
- **后果**：Once positions are persisted, invoking auto-layout destroys the user's arrangement with no undo — and the plan has no undo/history model at all despite the reference toolbar showing a 历史记录 clock button.
- **建议**：Either add dagrejs/elkjs (or hand-roll a layered DAG layout, ~150 lines) plus an animated transition and a single-level undo, +0.5-1 day; or drop the menu item. Also note the 4 unused deps as an opportunity: zustand is the obvious store for viewport/selection/clipboard state, and react-router-dom is required anyway for the homepage↔project routing the 第 0 步 implies (App.tsx:1033-1104 currently switches modules with a `useState<ModuleId>` and has no routing at all).
- **🚫 驳回理由**：The claimed conflict is not a defect. Plan line 76 already defines the layout as 按阶段分列 — a column-per-stage placement over at most 8 stages, which is roughly 20 lines of arithmetic, not a graph-layout problem; 优化工作流布局 is literally 're-run that function', and overwriting user coordinates is the command's entire purpose, not a bug. The dependency evidence is accurate (package.json:12-24 has no dagre/elkjs/d3-hierarchy, and konva/react-konva/zustand/@tanstack-react-query/react-router-dom/zod are all present but unused — I confirmed 0 import hits in src for every one of them) but it argues for nothing: no library is needed. The 'destroys the arrangement with no undo' point reduces to a confirm dialog. The residual true observation — that 优化工作流布局 and the 历史记录 clock are in the reference and in no step's budget — is one more instance of the Ch.6-is-a-mood-board finding, not a separate defect.

#### The 风暴功能 function-card marketplace is one plan bullet, but our 6 stages are gated pipeline steps that cannot be invoked as free-standing functions

`🔴 blocker` ｜ `⚠️ 方案乐观`

- **证据**：Plan line 128 maps the panel to "我们的各阶段生成能力". But those stages are not free-standing: `/v1/ai-workflow/color-plans` requires `layout_approved`, a 500KB `semantic_layout`, and `_validate_parent_asset` (main.py:1226-1245); style/tone/local-edit make `asset_parent_id` a REQUIRED form field (`asset_parent_id: str = Form(..., min_length=1)` at main.py:1433, 1503, 1571); `/v1/layouts/ai` additionally verifies a stage-01 job id, an approved version id, a 64-char source sha256 and detected bounds, rejecting any mismatch with 422 (main.py:1150-1195). Meanwhile there is no capability catalog to drive the panel: `/health` returns a flat list of 6 stage strings (main.py:800-811) and `asset_modules()` (assets.py:17-43, exposed at main.py:881-887) is an asset taxonomy with name+description only — no icons, no thumbnails, no categories, no favorites. Screenshot 2 needs 6 categories, a search field, thumbnailed 2-col cards with ★ favorites, and ⊕ New Folder.
- **后果**：A marketplace panel promises "pick any node, apply any function". Our API will answer 422 for most (node, function) pairs. This is the deepest conceptual mismatch between the reference product (a generic multi-model image playground) and ours (a validated 8-stage design pipeline), and Ch.6 treats it as a layout task.
- **建议**：Design the panel as an eligibility-aware launcher: a `GET /v1/capabilities` returning {id, category, label, thumbnail, requires:[...]} and a client-side predicate that greys out cards not applicable to the selected node (which conveniently also supplies the disabled-item semantics screenshot 1 requires). Budget 3-5 days including the backend catalog + favorites persistence, and tell the user 6 real functions will fill a grid designed for ~20.

#### The homepage cascading 3-level model picker and AI chat entry have no backend, no second model, and no chat route

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：Screenshot 3 needs 大语言模型/图片生成/视频生成 → 8 model families → model cards with one-line descriptions. We have exactly one configured image model, surfaced as a single string in `/health` (`"model": settings.kuyao_image_model`, main.py:806 and 830 region) plus one vision model (main.py:801-805). There is no model catalog endpoint, no chat/LLM endpoint, and no video anything in the 25 routes of main.py. Plan line 121 renders this as "中央问候语 + AI 对话框（大输入框 + 模型选择下拉，下拉内级联：图片生成 / 视频生成 → 具体模型卡片）" — one line, inside the unestimated 第 0 步.
- **后果**：Building the cascade faithfully means shipping a three-level picker over one real option, i.e. deliberate fake UI. Building it truthfully means a model registry plus at minimum a chat-to-intent router, which is weeks, not part of a 3-5 day plan.
- **建议**：Descope explicitly and get the user's sign-off: keep the greeting + big input box (wire the text straight into stage-03's `design_prompt`, which already exists as `design_prompt: str = Form("", max_length=1000)` at main.py:1230) and replace the cascade with a single static model pill showing the configured model from /health. If the user insists on the cascade, that is a separate epic.

#### ~40% of the screenshot chrome is SaaS monetization/community surface for a product that is explicitly a local single-user pilot

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：Screenshots demand: ⚡25 / ⚡20 credit balances, 普通用户 + 升级, 限时40折 promo pill, PS插件 / 创作者挑战赛 / 加入合伙人 / 邀请有礼, 限时赛事 competition card, 风暴学堂/讲师课程/讲师作品, 精选案例 with 推荐/最新 tabs, share icon, Agent button, phone icon. The codebase has none of the substrate: models.py:1-118 defines only Project/Job/SceneAsset — no users, no credits, no auth; ownership is a hardcoded constant (`SceneAsset.owner_id == LOCAL_OWNER_ID`, main.py:858, assets.py:585); the UI header is literally hardcoded "本地个人账户 · BL" (App.tsx:1084-1087); and the product self-describes as "LOCAL PILOT · V0.6" / "本地个人资产空间" (App.tsx:245, 1073). Ch.6 mentions only 瀑布流案例墙 (line 124) and 分享 / Agent (line 135); it omits credits, membership, courses, competition, referral and plugin pills entirely.
- **后果**：If "the screenshots ARE the required frontend" is applied literally, a large fraction of the work is dressing that cannot function, and some of it (credits, 升级) would actively mislead the user's own testers.
- **建议**：Take this back to the user as a scoping question, not an implementation task. Suggested split: KEEP the left icon rail, announcement bar, greeting+input, project cards, and a 精选案例 masonry seeded from the user's own generated assets (the data for that exists — thumbnail_url on every asset, assets.py:607). DROP credits/membership/promo/competition/courses/referral/PS插件/phone. Note masonry itself is real work: App.css has 30+ `grid-template-columns` rules and not one CSS-columns/masonry layout (grep confirms none), and mixed-aspect-ratio waterfall needs either CSS columns (breaks DOM order) or a measured JS layout.

#### The bottom floating toolbars contain ~13 distinct tools across the two screenshots; the plan lists 7 and budgets 0, and canvas image upload has no endpoint

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：Screenshot 1 pill: ＋, select, node/graph, magic-wand, second node tool, clock(历史), keyboard(快捷键), ?(帮助). Screenshot 2 pill (a DIFFERENT set): cursor, grid, crop, upload, layers, folder(项目文件), ⚡风暴功能. Plan line 132 collapses both into one 7-item list and omits 裁剪, 图层, 项目文件 and 上传. Upload is the sharp one: there is no generic "upload an image, get an asset" route — every upload in main.py is a stage-specific multipart form with mandatory validation companions (e.g. `source_image: UploadFile = File(...)` alongside required `semantic_layout` + `stage01_source_sha256` at main.py:1140-1158). So dragging a JPG onto the canvas has nowhere to POST it. Likewise 资产管理 (plan line 133) would reuse AssetLibrary.tsx, but that component is a full-page two-pane shell (AssetLibrary.tsx:1217-1344) with its own toolbar and filter row — it is not drawer-shaped and will need extraction/restructuring, not just a wrapper.
- **后果**：Each of these reads as one icon in a mockup and lands as 0.5-2 days of real work (crop editor, layer/z-order model, upload endpoint + asset registration, drawer refactor, history panel over jobs, shortcut cheatsheet, help).
- **建议**：Enumerate the toolbar explicitly and mark each icon 实现/占位/删除. At minimum add a `POST /v1/uploads` that saves via the existing `save_upload` helper (used at main.py:1196) and registers a source-image asset, otherwise the canvas cannot accept new input at all — which contradicts the whole "canvas is the primary surface" premise.

#### The top-center hint's semantics are misread: 「点击图片上的物体进行标记」 is click-to-select-object, not our free-hand red pen

`🟠 major` ｜ `❌ 方案错误`

- **证据**：Plan line 136 asserts the top-center pill "对应我们的红笔标注模式". Our stage-08 annotator is explicitly a hand-drawn closed-region brush: the in-app instructions read "用红色画笔圈出或涂满要修改的区域（闭合圈的内部会一起重绘）" (AiDesignWorkflow.tsx:2452-2453, echoed at 2317 and 2562), implemented as a stroke-list canvas composited into a PNG mask file (AiDesignWorkflow.tsx:494-517, `local-edit-marks.png`). "点击物体进行标记" describes one-click object selection, i.e. a segmentation model — which does not exist anywhere in the API (the only vision capability is floorplan structure analysis, main.py:801-805).
- **后果**：Copying the hint text without the capability produces a UI that lies about its interaction. Users will click once, nothing will be selected, and the mask will be empty.
- **建议**：Reword the hint to match reality (e.g. 「用红色画笔圈出要修改的区域」) rather than copying the reference string. Also note a real integration bug the plan misses: the annotator hardcodes `canvas.width = 960` and derives brush size from `image.naturalWidth` (AiDesignWorkflow.tsx:408-413, 424-431). Its `pointFromEvent` uses getBoundingClientRect (AiDesignWorkflow.tsx:433-442) so coordinates survive a CSS-transform scale, but at 20% canvas zoom the visible brush becomes unusably thin — annotation must open in the fullscreen focus mode the plan proposes at line 102, never inline on the scaled canvas.

#### "自研约 300 行的 CanvasViewport" understates step 1: the screenshot's canvas shell alone is 5 sub-systems

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：Plan line 67 scopes CanvasViewport at ~300 lines (wheel zoom + pointer pan + minimap) and line 73-79 gives step 1 one day for viewport + auto-layout + bezier edges + node cards + approval badges. The screenshots additionally require, inside that same shell: a live zoom-percentage readout (68%), a minimap with a draggable white viewport rect AND a close button, two view-mode toggles, node selection with four corner resize handles (方案 line 21 documents this from the reference but the 三步走 never implements it), node title-bar rename, and placeholder/empty node states (the mountain glyph in screenshot 1 — which is exactly the skeleton state the plan hand-waves at line 92). For reference on how much these grow: the two existing canvas-bearing components are 3270 (FloorplanModule.tsx) and 2629 (AiDesignWorkflow.tsx) lines.
- **后果**：Step 1 will not close in a day. Anchoring the user to "1 天" makes every subsequent slip look like execution failure rather than an estimate error.
- **建议**：Re-scope step 1 to 2-3 days for a read-only canvas (viewport + dotted bg + auto-layout + bezier + node card + zoom readout), and split minimap/resize-handles/rename into a step 1.5. Keep resize handles out of v1 entirely — they add an aspect-ratio and re-render cost with no product value for our image nodes.

#### Bottom line on effort: the plan says 3-5 days; the screenshot-as-spec scope is 20-30 person-days

`🔴 blocker` ｜ `⚠️ 方案乐观`

- **证据**：无限画布改造方案.md:73/80/90 give 1 + (1-2) + (1-2) days, plus an unestimated 第 0 步 (line 142) and an unestimated Ch.6 (lines 106-143). Against the code: dark-theme retro-fit of 5664 CSS lines + 3 large components (App.css:6-49, zero dark rules) = 5-8d; canvas shell incl. minimap/zoom/skeleton nodes = 2-3d; canvas/node schema + CRUD + hand-written SQLite migration (database.py:28-40, no Alembic) = 3-4d; lineage repair so the DAG connects (assets.py:44-57, main.py:1210) = 1d; context menu + shortcuts + clipboard (0 existing handlers) = 2-3d; function panel + capability catalog (main.py:881-887 is not one) = 3-5d; homepage incl. project API changes (schemas.py:27-30) + masonry + icon rail + routing (react-router-dom unused) = 4-6d; 资产管理 drawer + history + upload endpoint = 2-3d.
- **后果**：4-6x underestimate. The plan's own Ch.7 sequencing advice (line 151: finish backend perf items first, then start the canvas) is sound but is written as if the canvas were a 1-week side quest.
- **建议**：Present the user two options with honest numbers: (A) 无限画布 MVP — dark canvas shell + read-only lineage DAG + node action bar, reusing the existing wizard for all generation, ~8-10 days, no homepage change; (B) 截图级还原 — 20-30 days with the descoping rulings requested above. Do not present the current 3-5 day plan as achievable for (B). Separately, Ch.7's claim of no conflict with 优化计划.md holds for A1/B3/C1 (Job.started_at/finished_at already exist, models.py:63-70, and BatchProgress.tsx:5-13/52-56 already consumes startedAt + per-batch counts, so incremental publish maps cleanly onto skeleton nodes) — but the canvas DOES add a new load: /v1/assets runs a full backfill per call (main.py:857, as …


### 维度 5 — 工期与排期：与《优化计划.md》的相互影响

共 13 条，存活 10，推翻 3。

| 级别 | 判定 | 问题 |
|---|---|---|
| 🟠 major | ~~已推翻~~ | ~~Chapter 7 is already obsolete: A1 + incremental publish + B2/B3/B4 + C1/C2/C3 are ALREADY MERGED on this branch~~ |
| 🟠 major | ~~已推翻~~ | ~~C5 (idempotency key) is the ONLY perf item that gets thrown away by canvas work — and only if implemented at the call sites~~ |
| 🔴 blocker | ~~已推翻~~ | ~~The plan's own 第1步 is the largest throw-away risk — not anything in 优化计划.md~~ |
| 🔴 blocker | ❌ 方案错误 | Branching — the canvas's central promise — is rejected by the backend with HTTP 409 |
| 🔴 blocker | ❌ 方案错误 | 第1步's data source does not exist: no project filter, no stage-01 asset, no stage-02 lineage edge |
| 🟠 major | ❌ 方案错误 | '骨架卡片天然变成骨架节点' is wrong: partial results compact the list, and no asset exists until the batch terminates |
| 🟠 major | ◐ 方案不完整 | Image weight makes a 40-node canvas unusable: thumbnail_url is the full-size render and /artifacts has no cache headers |
| 🟠 major | ⚠️ 方案乐观 | '只是入口从阶段面板换成节点操作条' understates 第2步: the wizard's state model is structurally single-chain |
| 🔴 blocker | ◐ 方案不完整 | 第0步 (homepage) has no estimate and is the single largest workstream — and parts of it have no backing feature at all |
| 🟠 major | ⚠️ 方案乐观 | The canvas engine is not '约 300 行' — the mandatory right-click menu alone needs endpoints that do not exist |
| 🟡 minor | ◐ 方案不完整 | Node position / multi-canvas persistence has no schema and no migration tooling |
| 🟡 minor | ◐ 方案不完整 | No frontend test workstream exists, and the approval-model change breaks ~1300 lines of backend tests |
| 🔴 blocker | ⚠️ 方案乐观 | Honest total re-estimate: ~30-40 person-days, not 4-5 |

#### ❌【已推翻】Chapter 7 is already obsolete: A1 + incremental publish + B2/B3/B4 + C1/C2/C3 are ALREADY MERGED on this branch

`🟠 major` ｜ `❌ 方案错误`

- **证据**：`git log`: a18f84e "perf: concurrent variant fanout with incremental progress publishing" and 54d063f "feat: job resilience, progress UI, and cancellation (C1-C3, B2-B4)" are both ancestors of HEAD. Verified in code: ai_workflow.py:6 imports ThreadPoolExecutor/as_completed, ai_workflow.py:53-78 is the fan-out helper, jobs.py:141-152 `publish_progress` writes partial `job.result`, jobs.py:154-161 `should_cancel`, jobs.py:137/187 started_at/finished_at, kuyao_image_edit.py:62-86 shared client + split timeouts (A2), :303-334 `_post_edit_with_retry` (A3), :239-248 preserves error body + Retry-After (A4). Frontend: AiDesignWorkflow.tsx:1524-1527 mergeRetryResult (B4), :1571-1585 cancelActiveJob (B3), BatchProgress.tsx (B2), :1096-1113 skeleton cards. Remaining unfinished perf items are only A5, A6, C4, C5, C6, D*.
- **后果**：Plan line 148-151 spends its entire sequencing argument defending against a conflict that no longer exists ('A1 并发、增量发布…全部继续有效'), and its ordering advice ('先完成 A5 实测 / C4 / C5，再启动画布') is stated without knowing what actually remains. The one real ordering conflict — C5 — is buried among two items (A5, C4) that are pure backend and have zero interaction with the canvas.
- **建议**：Rewrite chapter 7 against the actual remaining set. A5/A6/C4/C6 are backend-only and can run fully in parallel with canvas work — do not serialize behind them. Only C5 conflicts (see next finding).
- **🚫 驳回理由**：The merge facts check out (a18f84e and 54d063f are both ancestors of HEAD; ai_workflow.py:29-77 _fanout_generate, jobs.py:141-152 publish_progress, :154-161 should_cancel, :83-103 reclaim_stale_jobs, :106-125 _archive_job_asset, main.py:1786-1800 conditional cancel, AiDesignWorkflow.tsx:1524-1527/1571-1585, BatchProgress.tsx) — but the reading of the plan does not. Plan line 148 ("本方案不改变优化计划的性能路线（A1 并发、增量发布、取消等全部继续有效）") is a SCOPE statement — 'this proposal does not alter the perf roadmap' — not a defense against a conflict. It is equally true whether those items are merged or not. And the finding's own evidence refutes its charge that the plan doesn't know what remains: plan line 151 names exactly A5 / C4 / C5, and I verified all three are genuinely unfinished (grep 'ThreadPoolExecutor|add_task' apps/api/app/jobs.py returns nothing → C4 open; 14 add_task sites in main.py; grep 'idempot' …

#### ❌【已推翻】C5 (idempotency key) is the ONLY perf item that gets thrown away by canvas work — and only if implemented at the call sites

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：C5 requires the frontend to mint a UUID per user submit and attach it to all 6 create-job POSTs. Today those submissions are built in one place per stage: AiDesignWorkflow.tsx:1469-1515 `commonForm()` builds the FormData, and :1517-1568 `run()` fires it. 第3步 (plan line 90-92, '生成入画布') replaces exactly this path — the canvas fires generations from node context menus, not from `commonForm`. The auto-retry that C5 exists to defend against lives in api.ts:59-89 `xhrRequest`, which is call-site agnostic.
- **后果**：If C5's frontend half is written into `commonForm`/`run` before the canvas, it is rewritten during 第3步. If it is deferred until after the canvas, the product ships to real customers with a known double-billing path (a lost response silently starts a second full 4-12 variant batch).
- **建议**：Split C5: land the backend half (Job.idempotency_key column + conflict-returns-existing-job) immediately, and put the key generation inside the `xhrRequest` wrapper in api.ts:59-89 (one key per logical submit, reused across its retries) instead of at the 6 call sites. Then the canvas rewrite inherits it for free and nothing is thrown away.
- **🚫 驳回理由**：Three problems. (1) The consequence branch is moot: plan line 151 already sequences C5 BEFORE the canvas, so 'ships to real customers with a known double-billing path' describes an ordering the plan does not propose. (2) The premise that a call-site key is lost across retries is wrong. api.ts:86 recurses as xhrRequest(url, init, networkRetries - 1) — it re-sends the SAME `init` object and therefore the identical FormData body. A key minted in commonForm (AiDesignWorkflow.tsx:1469-1515) is automatically reused by both auto-retries; the api.ts wrapper is not needed to get that property. (3) The 'thrown away' work is one form.append line reachable from a single builder (commonForm is the only FormData constructor for all six stage POSTs — verified: :1481-1495 for style/tone/local_edit, :1499-1514 for color_plan/axonometric/space_render). Rewriting one line during 第3步 is not a sequencing def …

#### ❌【已推翻】The plan's own 第1步 is the largest throw-away risk — not anything in 优化计划.md

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：第1步 (line 73-79) builds a read-only canvas whose nodes come from assets and whose edges come from `parent_asset_id`. But a node in that model is a BATCH: models.py:92-96 puts a UNIQUE constraint on `scene_assets.job_id`, so one AI_COLOR_PLAN job (4 images) or AI_SPACE_RENDER job (12 images) is exactly one asset row, and `parent_asset_id` (models.py:97-101) points at that batch, not at a specific image. 第2步/第3步 (line 82-94, and decision #2 at line 101 '一张图一个节点') require the opposite: node = image, edge = specific-variant → child.
- **后果**：The auto-layout, edge routing, node identity and localStorage position keys written in 第1步 are all keyed on assetId. When 第2/3步 forces node identity to (assetId, variantId) or to a new per-output row, all of that is rewritten. This is real rework inside the plan, unrelated to 优化计划.md.
- **建议**：Invert the order. Land the backend node model first (per-output identity, project-scoped listing, thumbnails), then build the canvas once against the final shape. Do not ship a read-only asset-level canvas as a stepping stone.
- **🚫 驳回理由**：This misreads the plan and understates the existing data. Plan line 76 already specifies '变体纵向排开' (variants spread out vertically) inside each stage column, and decision #2 at line 101 fixes node granularity at 一张图一个节点 up front — 第1步 does not build an asset-level node model that 第2/3步 then flips. More importantly, per-image EDGES are already derivable without any schema change: for stages 04/05 the specific source image is the parent asset's metadata.approvedVariantId (written at main.py:994 and enforced at main.py:318-360), and for stages 06/07/08 it is metadata.parentVariantId (assets.py:391-392, fed from _validate_parent_approved_space's parent_variant_id at main.py:758 through the job payload). So (assetId, variantId) node identity and variant-level edges are both computable from today's SceneAsset rows. No per-output row, no dropping the UNIQUE on scene_assets.job_id (models.py:92-9 …

#### Branching — the canvas's central promise — is rejected by the backend with HTTP 409

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：main.py:979-986: after approving a variant, if any `SceneAsset.parent_asset_id == asset.id` exists, re-approving a different variant raises 409 '该批准版本已有下游资产，不能直接改批'. main.py:702-706: a derivative POST must submit `source_space_version_id` equal to the parent's CURRENT `approvedVersionId`. main.py:751-755: the uploaded bytes must sha256-match the parent's CURRENT approved output. Approval state is per-asset (single `approvedVariantId`, main.py:990-999), i.e. per 4-12-image batch.
- **后果**：Plan line 32 ('原图 → 风格 A → 调色 B 在画布上自然铺开') and line 51 ('后端谱系已经完整…后端基本不用动') are false. On the canvas, generating from variant 2 and then from variant 3 of the same batch is structurally impossible — the second attempt 409s. This is not a UI gap; it is the data model. Fixing it means per-variant approval (or one asset row per output, which requires dropping the UNIQUE on job_id — a full SQLite table rebuild, since there is no alembic: database.py:33-53 is a hand-rolled ALTER TABLE) plus rewriting main.py:274-291, :308-360, :680-760 and assets.py:465-555, and repairing ~1339 lines of tests (test_assets.py 481, test_ai_workflow_derivatives.py 484, test_workflow_resume.py 374).
- **建议**：Add a W0 backend workstream to the plan, budget 3-4 days for the approval/identity model alone, and delete the '90% 在前端 / 后端基本不用动' claim from line 53. Decide explicitly whether branching is in scope for v1 — if it is not, say so, because without it the canvas is a prettier wizard.

#### 第1步's data source does not exist: no project filter, no stage-01 asset, no stage-02 lineage edge

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：(a) main.py:830-878 `GET /v1/assets` accepts limit/offset/generationMode/moduleKey/workflowStage — there is NO project_id filter, and `limit` is capped at 100 (main.py:833). (b) main.py:861 calls `backfill_scene_assets(session)` on every single list request, which re-runs `ensure_scene_asset` over ALL succeeded jobs (assets.py:558-578). (c) FLOORPLAN_ANALYZE is a real job type (jobs.py:39) but is absent from JOB_TYPE_TO_MODULE (assets.py:42-57), so `ASSET_JOB_TYPES = frozenset(JOB_TYPE_TO_MODULE)` (assets.py:58) excludes it — stage 01 produces no asset at all. (d) `/v1/layouts/ai` (main.py:1132-1214) has no `asset_parent_id` form field; create_job at :1206-1212 sets only `parent_job_id` — so the stage-02 layout asset always has `parent_asset_id = NULL`. (e) There is no list-jobs endpoint at all (only /jobs/{id}, /retry, /cancel).
- **后果**：Plan line 76 ('读取当前项目全部 assets，按 lineage 自动布局：01 原图 → 02 布局 → 03 彩平') cannot be implemented. 01 has no node, the 01→02 and 02→03 edges do not exist in the data (03's parent comes from a manually-typed optional text box, AiDesignWorkflow.tsx:2191), the list is global rather than per-project, and it is capped at 100 items with an O(all-jobs) backfill on every fetch — pathological for a view that refetches while generating.
- **建议**：Add to W0: project_id filter + pagination on GET /v1/assets, move backfill out of the hot path, register FLOORPLAN_ANALYZE as an asset (or synthesize root nodes from parent_job_id), thread asset_parent_id through /layouts/ai, and add GET /v1/jobs?projectId= so in-flight and failed nodes survive a page reload. ~2 days.

#### '骨架卡片天然变成骨架节点' is wrong: partial results compact the list, and no asset exists until the batch terminates

`🟠 major` ｜ `❌ 方案错误`

- **证据**：ai_workflow.py:50-51 `ordered_outputs()` returns only COMPLETED items sorted by index, so if variant index 2 finishes first the published `outputs` is `[variant2]`, then `[variant0, variant2]`. jobs.py:141-152 publishes that compacted list. Meanwhile jobs.py:183-190 + :106-125 `_archive_job_asset` only run on terminal SUCCEEDED, and assets.py:468 refuses non-SUCCEEDED jobs — so a RUNNING or CANCELED batch has NO SceneAsset and therefore no node in the asset-derived canvas. Skeleton count today comes from `expectedBatchCount(job)` reading `job.payload.variants` (BatchProgress.tsx:6-13) and renders `expected - outputs.length` anonymous cards (AiDesignWorkflow.tsx:1096-1113).
- **后果**：A grid can reflow harmlessly when the outputs list grows out of order; nodes at fixed canvas coordinates cannot — images would jump between nodes mid-batch. And because assets only exist post-terminal, the canvas needs TWO reconciled data sources (live polled jobs + persisted assets) with stable identity across the handoff. Neither is mentioned; the plan treats it as free.
- **建议**：Key pending nodes by variantId derived from `job.payload.variants` / `selected_space_ids` (they do match `output.variantId`, ai_workflow.py:459-463), and design the job-node → asset-node identity handoff explicitly. Budget ~1 day, and note that canceled batches (jobs.py:175-182 keeps the partial result but never creates an asset) produce paid-for images that will never appear as nodes.

#### Image weight makes a 40-node canvas unusable: thumbnail_url is the full-size render and /artifacts has no cache headers

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：assets.py:450-462 `_thumbnail()` just returns the first full-size deliverable URL (finalRenderUrl/previewUrl/...). There is no resize/PIL call anywhere in assets.py or storage.py. Measured on this repo: `.local/artifacts` = 906 files / 745 MB, 304 PNGs over 1 MB. main.py:93 mounts StaticFiles with no Cache-Control (D3 in 优化计划.md is still open). A single full chain is 4 (彩平) + 3 (轴侧) + 12 (空间) + 3 (风格) + 3 (色调) + N edits ≈ 25-40 nodes.
- **后果**：'所有批次、所有变体同时在场，缩放俯瞰' (plan line 49) means simultaneously fetching and decoding 40 × ~2 MB. Decoded RGBA at 1024×1536 is ~6 MB each — 240 MB+ of image memory before any branching. Every pan/zoom that unmounts and remounts nodes refetches from an uncached origin.
- **建议**：Backend thumbnail generation is a prerequisite for 第1步, not an optimization. Add it to W0 (~1 day) together with the /artifacts Cache-Control header, plus viewport culling in the canvas engine. Another item that contradicts '后端基本不用动'.

#### '只是入口从阶段面板换成节点操作条' understates 第2步: the wizard's state model is structurally single-chain

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：AiDesignWorkflow.tsx holds ~35 useState in one component (:1121-1172). `jobs` is `Partial<Record<GenerationStage, Job>>` (:1164) — one job per stage, so a second color-plan run REPLACES the first. `busyStage` is a single value (:1168) — one generation at a time. There is exactly one `spaceBaseline`/`styleBaseline`/`toneBaseline` (:1157-1159). Approving CASCADES AND DELETES downstream state: :1827-1854 calls `withoutStageJobs(current, ['style','tone','local_edit'])`, `setStyleBaseline(null)`, `setToneBaseline(null)` and auto-advances via `setActiveStage`. Every node action must also re-download and re-upload the image bytes (`workflowAssetImageFile`, :1788, feeding `form.append('source_space_image', baseline.file)` at :1482).
- **后果**：Canvas semantics are the exact inverse: many concurrent branches, nothing ever cleared, no auto-advance. `approveGeneratedColorPlan` / `selectApprovedBaseline` / `submitStage` cannot be 'reused as-is' (plan line 87) — they are entangled with single-baseline state and stage auto-advance. Realistically ~600 lines of stage orchestration are rewritten, not relocated, and a per-node job registry replaces the stage-keyed map.
- **建议**：Re-scope 第2步 from '1-2 天' to a state-model rewrite: per-node job registry supporting concurrent generations (2 d), six node-type operation panels each re-deriving their form inputs (2-3 d), approval-without-clearing (1 d), stage-01 fullscreen round-trip (1 d) → 6-7 days.

#### 第0步 (homepage) has no estimate and is the single largest workstream — and parts of it have no backing feature at all

`🔴 blocker` ｜ `◐ 方案不完整`

- **证据**：Dark theme: App.css is 5664 lines with 580 hex literals (359 distinct) and only 270 `var(--*)` uses across 819 class rules; the token block at App.css:8-23 is a LIGHT palette (--surface #ffffff, --canvas #f4f5f7, purple accent #7a5af8). The screenshot spec mandates near-black + blue #3b82f6. Homepage per screenshots 3/5: a three-level cascading model picker (大语言模型/图片生成/视频生成 → 8 models → model cards) — the backend has ONE image model (`settings.kuyao_image_model`, used at ai_workflow.py:406) and no LLM/video path; a 项目卡片墙 — there is no project UI whatsoever, `project_id` is a free-text input at AiDesignWorkflow.tsx:2175-2180 and App.tsx:1034 is a single `useState<ModuleId>` with no router (react-router-dom is installed but unused anywhere in src/); 精选案例瀑布流 — no content source exists.
- **后果**：A dark-theme conversion of 819 rules with 580 hardcoded colors is a per-rule audit, not a token swap (3-4 d). The homepage adds 4-6 d, of which the model picker and 精选案例 are product decisions plus new data, not code. Presenting 第0步 with no number next to '~4-5 天 total' makes the whole estimate meaningless.
- **建议**：Estimate 第0步 explicitly at 9-12 days including the left function panel, top-center mode hint, bottom toolbar, zoom %, and 资产管理 entry from screenshot 2. Flag the model picker and 精选案例 as blocked on product input rather than schedulable engineering.

#### The canvas engine is not '约 300 行' — the mandatory right-click menu alone needs endpoints that do not exist

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：package.json has no reactflow/@xyflow (so plan option B = new dependency); konva/react-konva ARE installed but used nowhere in src/ (grep returns nothing); zustand and @tanstack/react-query are likewise installed and unused. FloorplanModule.tsx has no pan/zoom to reuse — :1100-1108 computes a static fit scale only. Screenshot 1 mandates: bezier edges, marquee/multi-select, 四角缩放手柄, 复制节点 ⌘C / 创建副本 ⌘D / 粘贴 ⌘V / 删除 ⌘⌫, 保存到我的资产, and 优化工作流布局 (an auto-layout algorithm). The route table in main.py has ZERO `@app.delete` handlers and no asset-copy/duplicate endpoint — 删除节点 and 创建副本 have no backend.
- **后果**：300 lines covers wheel-zoom + pointer-pan + minimap and nothing else. Edge routing/hit-testing, selection model, clipboard semantics, resize handles, auto-layout and viewport culling are each comparable in size. Menu items requiring a backend cannot be greyed out forever — screenshot 1 shows greyed items, so shipping them disabled is at least defensible, but 删除/副本 are shown enabled.
- **建议**：Re-estimate the engine at 6-7 days (viewport 1-1.5, edges 1, selection/drag/resize 1.5, context menu + shortcuts 1, auto-layout 1, culling/image lifecycle 1). Use the already-installed zustand for node/viewport state instead of hand-rolling. Decide per menu item whether it ships disabled or needs an endpoint.

#### Node position / multi-canvas persistence has no schema and no migration tooling

`🟡 minor` ｜ `◐ 方案不完整`

- **证据**：models.py:21-34 `Project` has only id/name/description/created_at — no metadata/JSON column. There is no canvas table. Screenshot 1 requires '画布 1 ▾' = multiple canvases per project. database.py:33-53 is a hand-rolled `PRAGMA table_info` + ALTER TABLE shim (added for started_at/finished_at); there is no alembic in the repo.
- **后果**：Plan line 93 ('布局存 localStorage / project 元数据') hides a schema change behind an 'or'. localStorage works for a single-browser demo but loses layout on any other machine and cannot support 画布 1 ▾ at all. Every schema change on this repo is hand-written migration code.
- **建议**：Pick one explicitly. If multi-canvas is in scope, add `canvases` + `canvas_nodes` tables and budget ~1 day plus migration code. If not, say localStorage-only and drop the 画布 selector pill from the v1 spec.

#### No frontend test workstream exists, and the approval-model change breaks ~1300 lines of backend tests

`🟡 minor` ｜ `◐ 方案不完整`

- **证据**：Backend: 6062 lines across 13 test files (apps/api/tests). Frontend: `find apps/web -name '*.test.*' -o -name '*.spec.*' -o -name 'vitest.config*' -o -name 'playwright.config*'` returns NOTHING, and package.json has no `test` script — despite vitest, @playwright/test and @testing-library/react being installed. scripts/check.sh runs only `npm run lint && npm run build` for web. The approval change from the branching finding directly invalidates test_assets.py (481), test_ai_workflow_derivatives.py (484) and test_workflow_resume.py (374).
- **后果**：Under this repo's own rules (80% coverage, TDD, Playwright E2E for critical flows), the largest frontend change in the project's history is planned with zero test budget, on a codebase with zero existing frontend tests to imitate. Backend test repair alone is 1-1.5 days that appears nowhere.
- **建议**：Add a 2-3 day test workstream: wire vitest + playwright config and a `test` script, cover the canvas viewport math and node identity/handoff logic, and budget backend test repair alongside the approval-model change.

#### Honest total re-estimate: ~30-40 person-days, not 4-5

`🔴 blocker` ｜ `⚠️ 方案乐观`

- **证据**：Summed from the workstreams verified above. W0 backend node model (approval/identity 3-4 incl. SQLite table rebuild for the models.py:92-96 UNIQUE; project-scoped listing + backfill removal 0.5; GET /v1/jobs 0.5; stage 01/02 lineage 1; thumbnails + cache headers 1; canvas persistence 1; test repair 1-1.5) = 8-10 d. W1 canvas engine = 6-7 d. W2 node ops + wizard state rewrite (AiDesignWorkflow.tsx 2629 lines, ~35 useState) = 6-7 d. W3 visual spec incl. 第0步 (App.css 5664 lines / 580 hex literals) = 9-12 d. W4 tests = 2-3 d.
- **后果**：The plan's 1 + 1-2 + 1-2 days is off by roughly an order of magnitude, mostly because it declares the backend out of scope (line 51-53) when the backend is the critical path. Committing to 4-5 days means either shipping a read-only demo canvas that gets thrown away, or discovering the 409 branching wall on day 3.
- **建议**：Present two options instead of one plan. (1) Demo-only, ~5-7 days: read-only canvas over existing per-batch assets, light theme, no branching, no homepage, no persistence — explicitly labelled throwaway. (2) Real, 6-8 weeks: W0 → W1 → W2 → W3 in that order, with A5/A6/C4/C6 running in parallel and C5 landed as an api.ts-level wrapper up front. Do not blend them.


### 维度 6 — 前提质疑与替代方案：画布是否适合这个受约束的流水线

共 12 条，存活 11，推翻 1。

| 级别 | 判定 | 问题 |
|---|---|---|
| 🔴 blocker | ❌ 方案错误 | 画布的核心卖点「任意变体分叉」被后端明确禁止，且有测试固化 |
| 🔴 blocker | ❌ 方案错误 | 「后端基本不用动」是错的：画布布局坐标在当前架构里无处可存，存进 asset metadata 会被自动擦除 |
| 🔴 blocker | ◐ 方案不完整 | 「读取当前项目全部 assets」的 API 不存在，且硬上限 100 条 |
| 🔴 blocker | ⚠️ 方案乐观 | 节点级生成需要 SemanticLayout + 原始图片字节，而这两样只能从「已批准」资产恢复 |
| 🟠 major | ❌ 方案错误 | 增量发布期间的节点没有 asset、没有 assetId、也没有连线锚点——与《优化计划》B1 存在真实冲突 |
| 🟠 major | ◐ 方案不完整 | 撤销/重做、删除节点在后端根本没有对应能力——而截图把「删除 ⌘⌫」列为必须项 |
| 🟠 major | ❌ 方案错误 | 「节点卡片复用现有样式体系」与 §6 强制的深色主题自相矛盾；且全站没有响应式和可访问性基础 |
| 🟠 major | ⚠️ 方案乐观 | 工期估算低了一个数量级：3-5 天对应不上截图里的需求面 |
| 🟠 major | ◐ 方案不完整 | 会话状态与崩溃恢复：整条流水线的状态是内存里的 File 对象，画布并不能改善，反而放大问题 |
| 🟠 major | ~~已推翻~~ | ~~反方立论：这个产品的核心价值是「受约束的审批闸门」，无限画布的自由度在这里主要是负债~~ |
| 🟡 minor | ◐ 方案不完整 | 更便宜的中间方案：在现有向导内加一块只读「谱系看板」，零后端改动拿到 80% 价值 |
| 🟡 minor | ◐ 方案不完整 | 线索核实结果：Job.started_at/finished_at 确实存在，且 A1/B1/B2/B3/C1/C3 已基本落地——方案的前置排期需要更新 |

#### 画布的核心卖点「任意变体分叉」被后端明确禁止，且有测试固化

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：main.py:979-986 `approve_scene_asset_variant` 在资产已有下游子资产时对「改批另一个 variant」返回 409「该批准版本已有下游资产，不能直接改批」。审批是每 asset 单值的（main.py:988-1007 写 metadata.approvedVariantId / approvedVersionId，单数字段）。而 models.py:92-96 `SceneAsset.job_id` 带 unique 约束 → 一个 job(4 张彩平 / 最多 12 张分空间) 只有 1 个 asset。apps/api/tests/test_ai_workflow_derivatives.py:321-327 明确断言这个 409 是预期行为。后果最重的地方在阶段 05：ai_workflow.py:734 每个空间一张图 variantId=`space_{room_id}`，12 个空间共用同一个 asset 的同一个 approvedVariantId——即「一个分空间任务里只有一个空间能进入 06/07/08」。
- **后果**：方案第 2 步「点任意节点 → 设为基准 / 批准并进入下一阶段」在多数节点上会直接 409。方案 §2 表格宣称「后端谱系已经完整、可直接映射为节点连线」和 §5.2「一张图一个节点」在数据模型层面不成立：节点是图片粒度，审批和谱系是 asset(=job) 粒度，二者差 4~12 倍。今天线性向导用「切换基准就清空下游」（AiDesignWorkflow.tsx:1832-1853）把这个限制藏起来了；画布把 12 个空间节点同时摆出来，等于把限制直接怼到用户脸上。
- **建议**：在写任何画布代码之前先决定数据模型：要么新增 `asset_variants` 表（asset_id + variant_id + approval 状态 + parent_variant_id），把审批和 parent 指针下沉到 variant 粒度；要么承认「一个 job 只能带一条分支下去」，画布上把非批准变体渲染成不可派生的叶子节点并显式说明。这是 3~5 天以内绝不可能完成的后端改动，必须从「90% 前端」的前提里剔除。

#### 「后端基本不用动」是错的：画布布局坐标在当前架构里无处可存，存进 asset metadata 会被自动擦除

`🔴 blocker` ｜ `❌ 方案错误`

- **证据**：方案 §4 第 3 步称布局存 "localStorage / project 元数据"。但 models.py:21-34 `Project` 只有 id/name/description/created_at——没有任何 JSON/metadata 列，也没有 updated_at。SceneAsset 有 metadata_json，但 assets.py:489+520-522 `ensure_scene_asset` 在资产已存在时用「从 job 重新计算出来的完整 dict」整体替换 metadata（`existing.metadata_json = metadata`），只白名单保留 5 个 approval 相关键（assets.py:494-502）。而 main.py:861/894/908/924/1026 在每次 GET /assets、GET /assets/{id}、approve、renders、workflow-resume 时都调用 `backfill_scene_assets`（assets.py:558-578），它会对所有成功 job 重跑 ensure_scene_asset。
- **后果**：任何写进 asset metadata 的画布坐标会在下一次列表刷新时被静默清零。落到 localStorage 则意味着换设备/换浏览器/清缓存布局全丢，且方案 §6 要求的「一个项目多张画布（画布 1 ▾）」在 localStorage 里无法与后端项目对齐。
- **建议**：第 3 步必须先加后端：`Project.updated_at`、`Project.thumbnail_url`，以及一张 `canvases` 表（project_id, name, viewport, node_positions JSON）或至少 `SceneAsset.canvas_meta` 独立列（不被 ensure_scene_asset 覆盖）。同时把 backfill 从「每次请求」改成启动时一次或显式触发，否则画布高频轮询会把它变成 O(全部 job) 的热路径。

#### 「读取当前项目全部 assets」的 API 不存在，且硬上限 100 条

`🔴 blocker` ｜ `◐ 方案不完整`

- **证据**：main.py:830-878 `GET /v1/assets` 的查询参数只有 limit / offset / generationMode / moduleKey / workflowStage——没有 project_id 过滤（对比 models.py:87-91 project_id 是有索引的列）。limit 被 `Query(ge=1, le=100)` 卡在 100（main.py:833）。前端 AssetLibrary.tsx:892 就是写死的 `/v1/assets?limit=100`，没有分页 UI。moduleKey / workflowStage 过滤还是在 Python 里对已经全量取出的列表做的（main.py:870-877），offset/limit 在最后才切片。
- **后果**：方案第 1 步「读取当前项目全部 assets 按 lineage 布局」today 无法实现——只能拿到全局最近 100 个资产、跨项目混在一起。任务书里问的「100+ 全分辨率节点」场景，第 101 个节点根本取不回来，而不是渲染卡顿的问题。
- **建议**：第 1 步的工作量估算里必须包含：给 /v1/assets 加 projectId 过滤 + 把过滤下推到 SQL + 提高/取消 limit 上限（或加 cursor 分页）。这是后端改动，不属于「90% 前端」。

#### 节点级生成需要 SemanticLayout + 原始图片字节，而这两样只能从「已批准」资产恢复

`🔴 blocker` ｜ `⚠️ 方案乐观`

- **证据**：每个生成端点都强制要求重新上传父图文件：main.py:1225 `approved_layout_image: UploadFile = File(...)`，main.py:1286-1288 轴侧同时要 approved_layout_image 和 approved_color_plan_image 两个必填文件，外加 `semantic_layout: str = Form(..., min_length=2, max_length=500_000)`。前端因此在 AiDesignWorkflow.tsx:1788-1791 先 `workflowAssetImageFile()` 把图下载成 File 存进内存（VisualBaseline.file，:84-95），再在 :1482-1487 重新上传。恢复 SemanticLayout 的唯一途径是 `/workflow-resume`，而它要求资产 approvalStatus == 'approved'（main.py:316-317）、谱系里必须存在 canonical SemanticLayout（main.py:418-419）、且链上所有副本 JSON 必须逐字节一致（main.py:427-437）。阶段 06-08 还要 `_validate_parent_approved_space` 对上传字节和服务端文件各算一次 sha256 并比对（main.py:739-755）。
- **后果**：方案 §2「点哪个节点，出哪个节点的操作」对未审批节点根本不成立——绝大多数画布节点（一批 4 张里的 3 张、12 个空间里的 11 个）既拿不到 SemanticLayout 也不能作为 parent。而对可用的节点，每次「从节点生成」是 1~2 张全分辨率图的下载+上传往返；画布鼓励的并行分叉会把这个成本乘以并发数，同时把 File 对象钉在 JS 堆里（今天只钉 3 个 baseline，画布上就是 N 个）。
- **建议**：要么新增一个「服务端按 asset_id 直接取父图字节」的生成入口（把 UploadFile 改成可选 + parent_asset_id 二选一），要么在方案里明确写死「只有已批准节点可以派生，其余节点只读」。前者是实打实的后端重构（6 个端点 + schemas.py:236/290 + 校验逻辑），后者会让画布上 70%+ 的右键项永远是灰的。

#### 增量发布期间的节点没有 asset、没有 assetId、也没有连线锚点——与《优化计划》B1 存在真实冲突

`🟠 major` ｜ `❌ 方案错误`

- **证据**：方案 §7 断言与优化计划「不冲突」，§4 第 3 步还说骨架节点天然对应增量发布。但 jobs.py:141-152 `publish_progress` 只写 `job.result`，资产归档 `_archive_job_asset`（jobs.py:107-125）要等 run_job 全部成功后才执行（jobs.py:236-237）；assets.py:468 `ensure_scene_asset` 要求 `job.status == 'SUCCEEDED'`。取消场景更糟：jobs.py:174-181 取消时写入 `batchStatus: 'canceled'` 但 status 保持 CANCELED，因此永远不产生 asset。优化计划 §1「本 PR 内必须一并处理的三个坑」第 2 条已经写明：部分结果会让审批按钮在 assetId 存在之前就可点。
- **后果**：画布的边（parent_asset_id → child）在任务跑完之前无法绘制，运行中节点是漂浮的孤儿；被取消的批次产出的图片有文件、有 job.result，但永远不会成为节点，用户看到的是「画布上的图凭空消失」。方案里对「运行中节点的 assetId gate」只字未提，等于把优化计划显式点名的坑重新引入到节点操作条上。
- **建议**：在方案里补一条硬规则：节点分三态（running-orphan / succeeded-without-approval / approved），只有第三态渲染上下游连线与派生类操作；并在第 1 步就定义 `variantId` 作为节点稳定 key（ai_workflow.py:465/527/607/734/920/995 已保证唯一），让骨架节点和最终节点是同一个节点而不是两个。

#### 撤销/重做、删除节点在后端根本没有对应能力——而截图把「删除 ⌘⌫」列为必须项

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：main.py 全文 26 个路由全是 @app.get / @app.post，没有任何 @app.delete / @app.put / @app.patch（grep 结果为空）。没有取消审批端点，没有删除资产端点，没有更新项目端点。approve 是单向的：main.py:988-1007 写入后 main.py:970-978 只对「重批同一个 variant」幂等，改批要么建新版本、要么 409。
- **后果**：截图 1 强制要求的右键菜单里，「删除 ⌘⌫」「复制节点 ⌘C」「创建副本 ⌘D」「粘贴 ⌘V」四项中至少「删除」在服务端无法落地——只能做成「从画布视图隐藏」，而用户会以为资产被删了（尤其在「保存到我的资产」也在同一菜单里的语境下）。方案通篇没提 undo/redo；在一个每步都花钱、审批不可逆的系统里，Cmd+Z 要么做不到、要么语义危险（撤销一次审批 = 让已有下游资产指向失效版本）。
- **建议**：明确写清楚：画布上的删除/复制只作用于「画布视图层」（node_positions），不触碰资产；undo/redo 只覆盖视图操作（移动、隐藏、连线折叠），生成/审批类动作显式排除并在 UI 上标注不可撤销。若要真删除，需新增软删除列 + DELETE 端点 + 与 backfill_scene_assets 的复活逻辑对齐（assets.py:558-578 会把删掉的资产重新建回来）。

#### 「节点卡片复用现有样式体系」与 §6 强制的深色主题自相矛盾；且全站没有响应式和可访问性基础

`🟠 major` ｜ `❌ 方案错误`

- **证据**：App.css:6-49 的设计令牌是浅色系：`--surface: #ffffff`、`--canvas: #f4f5f7`、`--ink: #1e2127`、主色是紫色 `--blue-600: #7a5af8`。方案 §6 要求近纯黑 #0a0a0d 背景、卡片 #17181c、主色蓝 #3b82f6。App.css 共 5664 行，全文只有 1 处 `@media`（index.css 0 处），且 App.css:58 `body { min-width: 1120px }` 把应用锁死为桌面端。三个主文件（AiDesignWorkflow 2629 行 / App 1106 行 / AssetLibrary 1748 行）里 aria-*/role=/tabIndex 合计只出现 7 次。前端没有任何测试文件，package.json 也没有 test 脚本（vitest/playwright/testing-library 都装了但零用例）。
- **后果**：§3 的「节点卡片复用现有样式体系」在 §6 落地时必须推翻——要么给 5664 行 CSS 做 token 化 + 双主题（若干天），要么画布是个跟全站割裂的深色孤岛。移动端/小屏在方案里完全没提，但 min-width:1120px 意味着无限画布在手机上连横向滚动都不成立。键盘导航是硬需求（截图 1 的 ⌘C/⌘D/⌘V/⌘⌫），却要建在一个几乎零 a11y、零测试的代码基上。
- **建议**：把「App.css token 化 + 深色主题」作为第 0 步的独立子项单独估时（我估 2-3 天），不要藏在「节点卡片」里。明确声明画布是 desktop-only（保留向导作为小屏 fallback）。在动 2629 行的 AiDesignWorkflow 之前，先补几条 Playwright 冒烟用例覆盖 01→03→05 主链路，否则任何重构都没有回归网。

#### 工期估算低了一个数量级：3-5 天对应不上截图里的需求面

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：§6 追加的第 0 步（首页改版）在 §4 的 1+1~2+1~2 天里完全没有预算。首页需要的东西一样都不存在：react-router-dom / zustand / @tanstack/react-query / react-hook-form / zod / konva 全部在 package.json 里但 src/ 下零引用（grep 无结果），App.tsx:1034 的导航是一个 `useState<ModuleId>`，没有路由；前端从未调用过 /v1/projects（grep projects 只命中后端）；Project 没有 updated_at 和缩略图列（models.py:21-34），撑不起截图 5 的「编辑于 3 分钟前」+ 项目缩略图。截图 3 的三级模型级联在后端没有对应物——config.py:29/32 只有单一硬编码模型 `kuyao_vision_model` / `kuyao_image_model`，没有任何 per-request 模型选择。截图 2 的功能卡片网格（指定材质替换/光影大师/质感增强/高清…）对应的能力我们只有 6 个 AI_* job type（assets.py:60-69），方案 §6 说「对应我们的各阶段生成能力」是不成立的映射。
- **后果**：按方案的三步走排期启动，第 1 步就会因为 /assets 缺 project 过滤 + limit 100 + 主题重做而超期，第 0 步（用户第一眼看到的界面）会变成开放式的无底洞。3-5 天的数字会让排期和期望一起崩。
- **建议**：我的估计：仅第 1 步（只读画布 + 连线 + 小地图 + 深色 token + 后端 project 过滤/分页）3-4 天；第 2 步（节点操作接入，含 variant 粒度审批的后端改造）2-3 周；第 3 步（布局持久化 + 多画布 + 项目 CRUD）1-1.5 周；第 0 步首页（路由 + 项目墙 + 案例墙，模型级联降级为静态占位）1-2 周。合计 5-8 周而不是 3-5 天。建议只承诺第 1 步，其余按「验证后再排」。

#### 会话状态与崩溃恢复：整条流水线的状态是内存里的 File 对象，画布并不能改善，反而放大问题

`🟠 major` ｜ `◐ 方案不完整`

- **证据**：全前端零 localStorage / sessionStorage / indexedDB（grep 无命中）。AiDesignWorkflow.tsx:1119-1172 有约 30 个 useState，其中 structureSourceImage / layoutImage / approvedColorPlanImage / styleReferences 以及三个 VisualBaseline.file（:84-95）都是 `File` 对象，无法序列化。FloorplanModule 的 FloorplanStage01Approval.approvedLayoutImage（FloorplanModule.tsx:121-132）同样是 File。App.tsx:1091-1096 之所以用 `hidden` 而不是条件渲染来挂载 AiDesignWorkflow，正是因为一旦卸载状态就全没了。pollJob（api.ts:220-241）超时 30 分钟，纯前端轮询，刷新即断。
- **后果**：方案对「浏览器崩溃/刷新恢复」「多标签页」完全没有讨论。画布让用户在一个页面上待更久、开更多并行任务，一次误刷新损失比线性向导大得多。多标签页打开同一画布时，审批是全局服务端状态但布局在 localStorage，最后写入者覆盖，且两个标签页的 approve 竞态会以 409 的形式暴露给用户（main.py:979-986）。
- **建议**：把「会话可恢复」提为第 1 步的并列目标，而不是第 3 步的附注：画布状态只持久化 ID（projectId + assetId + variantId + 坐标），生成时按需从 /workflow-resume 重新拉图；并在 §5 的三个待决策问题里补上第 4 条「多标签页/刷新语义」。

#### ❌【已推翻】反方立论：这个产品的核心价值是「受约束的审批闸门」，无限画布的自由度在这里主要是负债

`🟠 major` ｜ `⚠️ 方案乐观`

- **证据**：后端是一台硬编码的状态机：main.py:648-672 逐条写死了 color_plan←{layout,floorplan}、axonometric←color_plan、space_render←{color_plan,axonometric}、style←space_render、tone←style、local_edit←tone，任何越级都是 409。main.py:427-437 要求整条谱系上的 SemanticLayout 逐字节一致（几何权威）。main.py:739-755 要求提交的图片 sha256 必须等于父资产当前批准输出。ai_workflow.py:19 MAX_PROMPT_CHARS 与 :180-183 的房间/墙/开洞/家具上限说明 prompt 是被严格构造的，不是自由创作。对照参考产品：风暴 AI 是通用创作工具，几十个彼此独立的功能，自由探索本身就是产品；我们是一条 8 步单链，每张图 ~90s、真金白银（优化计划 §0）。
- **后果**：把画布做成主界面，会得到一个「右键菜单里 7 项灰、2 项可用」的界面（截图 1 里参考产品自己也灰了 3 项，但它的基数是几十个功能）。对只想「传平面图 → 拿效果图」的客户，空白点阵画布比向导劣化：没有下一步提示、没有强制顺序、也没有任何 onboarding（代码库里没有引导/文档组件）。同时画布鼓励的并行分叉会直接乘上 API 花费。
- **建议**：反方立论只在「画布作为主生成界面」这一层成立，我认为它站得住；但它不否定另外两点真实痛点：(1) 今天审批一个基准会把下游结果从视图里清掉（AiDesignWorkflow.tsx:1832-1837/1843-1851、invalidateFromSourceChange :1223-1240 直接 setJobs({})），12 张分空间图只剩 1 张可见；(2) 谱系只存在于后端，用户看不见。结论：采纳「画布作为只读的谱系/结果全景视图」，拒绝「画布取代向导成为主操作面」。§5.1 的「画布为主、向导为导航」建议应该反过来。
- **🚫 驳回理由**：这是战略意见而非缺陷，且与 [0]、[10] 重复；同时它的一条关键论据经核实是错的。它称「今天审批一个基准会把下游结果从视图里清掉…12 张分空间图只剩 1 张可见」，但 AiDesignWorkflow.tsx:1832-1837 删除的是 style/tone/local_edit 三个下游 job，jobs.space_render 原样保留，用户点阶段导航（AiDesignWorkflow.tsx:1985-2005 的阶段按钮 setActiveStage）即可回到 05 看全 12 张——那是面板切换，不是数据丢失。另一条「没有任何 onboarding」也忽略了顶部阶段栏本身就是流程指引。其余成立的部分（后端是硬编码状态机，main.py:648-672 逐条写死阶段父子关系）就是 [0] 的内容，结论（画布先做只读）就是 [10] 的内容，不应重复计一条。

#### 更便宜的中间方案：在现有向导内加一块只读「谱系看板」，零后端改动拿到 80% 价值

`🟡 minor` ｜ `◐ 方案不完整`

- **证据**：所需零件全部已存在：AssetLibrary.tsx:892 已经在拉 /v1/assets 并渲染卡片网格；WorkflowAssetPicker.tsx（436 行）已经实现「从资产恢复到某阶段」的完整流程，包括 allowGenerated（:48-51 未审批也可先审后续接）；/v1/assets/{id}/workflow-resume（main.py:901-912）已经返回完整 lineage（main.py:611-617 含 ancestorAssetIds 和 assets 链）；BatchProgress.tsx 已有骨架/计时能力；parent_asset_id 在 SceneAssetRead（schemas.py:52）里已经暴露给前端。
- **后果**：不做这个中间方案，就要为「看得见全局」这一个价值点先付掉 blocker 级的数据模型改造和主题重做。
- **建议**：具体做法（我估 2-3 天，零后端改动）：在向导右侧加一个可收起面板「方案地图」，用现有 /v1/assets?limit=100 的结果按 workflowStage 分列、按 parentAssetId 画 SVG 贝塞尔连线、节点显示缩略图 + 审批徽章；点节点 → 复用 WorkflowAssetPicker 的 resume 逻辑跳到对应阶段；平移缩放用一个 CSS transform 容器（约 150 行）。先上线这个，观察用户是否真的在用它跳转——如果他们只是用来「回看」，那就永远不需要把它变成主操作面；如果他们频繁尝试从非批准节点派生，那才是启动 variant 粒度审批改造的信号。

#### 线索核实结果：Job.started_at/finished_at 确实存在，且 A1/B1/B2/B3/C1/C3 已基本落地——方案的前置排期需要更新

`🟡 minor` ｜ `◐ 方案不完整`

- **证据**：models.py:63-70 确实有 started_at / finished_at，schemas.py:42-43 已暴露，BatchProgress.tsx:52 已经在用 `job.startedAt ?? job.createdAt`。jobs.py:141-152 publish_progress（B1 增量发布）、:154-161 should_cancel（B3）、:78-102 reclaim_stale_jobs（C1 重启回收）、:107-125 _archive_job_asset（C3 拆分事务）均已实现；api.ts:189-191 cancelJob 已存在且 AiDesignWorkflow.tsx:22 已引用；AiDesignWorkflow.tsx:1758-1760 的注释显示 B4「仅重试失败项合并」也已处理。方案 §7 的排期建议「先完成 A5 实测 / C4 / C5，再启动画布第 1 步」与此一致，但 §7 关于「不冲突」的判断不成立（见运行中节点那条）。
- **后果**：如果按方案 §7 的字面理解「优化计划全部继续有效、互不影响」去排期，会遗漏两处真实交互：运行中节点的 assetId gate，以及 C4（独立作业线程池）落地后 backfill_scene_assets 在画布高频轮询下的争用。
- **建议**：把 §7 改写成一张明确的交互表：B1 增量发布 → 画布需要三态节点模型；B3 取消 → 画布需要处理「有图但无 asset」的孤儿节点；C4 线程池 + 画布轮询 → 必须先把 backfill_scene_assets 从请求热路径移走（这一条同时是画布第 1 步的前置）。
