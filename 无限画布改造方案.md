# 无限画布模式改造方案（评审修正版）

> 调研对象：https://www.fengbaoch.com/ （风暴AI 工作空间 3.0，2026-08-04 实测）
> 目标：把当前 8 阶段线性向导改造成「无限画布」模式
> 2026-08-04 修订：按《无限画布方案评审.md》修正——删除「后端基本不用动」的错误前提，
> 插入 W0 后端前置工作流（6.5~8 人日），路线图替换为 W-1 → W6，
> 节点操作表改为从 `main.py:648-672` 的父类型约束生成。

---

## 1. 蜂豹无限画布的核心模式（实测记录）

### 画布本体

- 深色点阵无限背景，**单个 `<canvas>` 元素自渲染**所有节点与网格
- **Ctrl + 滚轮缩放**（以指针为中心）、拖拽空白处平移；右下角**小地图**
- 一个项目 = 一张画布（顶部「未命名工作区 | 画布 1 ▾」，可多画布）；首页是问候语 + 项目卡片横排

### 一切皆节点

- 上传的原图、AI 生成的每一张结果图，都是画布上的**独立节点**
- 节点头部显示：类型标签 + 像素尺寸 + ⓘ 生成信息按钮
- 选中态：蓝色边框 + **节点正上方浮动工具栏**；**节点间贝塞尔连线**表示派生关系
- **右键菜单**：保存到我的资产 / 复制节点 ⌘C / 创建副本 ⌘D / 粘贴 ⌘V / 删除 ⌘⌫（含快捷键标注，非法项置灰）

### 节点级 AI 操作（关键范式）

选中节点 → 浮动工具栏给出作用于该节点的操作（快捷编辑 / 材质修改 / 调色 / 打光 / 高清放大 / 扩图…）
→ 节点下方弹出输入卡片 → 提交后**新图作为新节点落在画布上**，谱系空间化铺开（含分叉 DAG）。

### 辅助结构

- 左侧功能面板（可收起）：功能分类 + 功能卡片网格（材质替换 / 氛围转换 / 光影 / 高清放大…）
- 底部悬浮工具栏、左下「资产管理 + 缩放百分比」、顶部居中提示条
- 注意：⚡点数、会员、赛事、风暴学堂等是 SaaS 计费/社区外壳，**单人本地实例不照搬**（见第 8 章）

---

## 2. 我们的现状 vs 目标

| | 现在（V0.6 线性向导） | 目标（无限画布） |
|---|---|---|
| 组织方式 | 8 阶段线性向导，每阶段一个视图替换 | 所有产物平铺在一张画布上 |
| 产物关系 | jobs 按阶段存 1 个 job；谱系藏在后端 lineage 里 | 节点 + 连线，派生关系肉眼可见 |
| 变体浏览 | 结果网格 2×N，只看到当前批次 | 所有批次、所有变体同时在场 |
| 操作入口 | 每阶段固定面板 + 弹层 | 点哪个节点，出哪个节点的操作 |

**后端现状（评审核实，逐条有 file:line 依据）：** 谱系数据模型（parent_asset_id / versions / approvedVersionId）的骨架在，但**不是画布就绪的**：

- 一个 job 只有一行资产（`models.py:92-96` `job_id unique=True`），一批 4~12 张图没有逐图身份
- 阶段 01 不产生任何资产（`assets.py:44-57` 无 `FLOORPLAN_ANALYZE`）
- `/v1/assets` 无 `project_id` 过滤，且每次请求跑全表 backfill（`main.py:830-878`）
- 分叉被后端 409 禁止（`main.py:979-986`），审批是 per-asset 单值
- 无画布/节点坐标持久化（`Project` 无 metadata 列、无 canvases 表）
- 无删除/副本端点（`main.py` 0 个 DELETE/PATCH/PUT）

**结论：前端是主体（约 70%），但存在 6.5~8 人日的后端硬前置（W0），不做完画布第 1 步就立不住。**

---

## 3. W0：后端前置工作流（6.5~8 人日，全部是先决条件）

| 子项 | 内容 | 估时 | 依赖 |
|---|---|---|---|
| **W0-a 查询解锁** | `/v1/assets` 加 `projectId` 并把过滤下推 SQL；`backfill_scene_assets` 移出请求热路径（改启动时一次或显式端点）；新增 `GET /v1/jobs?projectId=&status=`（刷新后恢复任务态） | 1 天 | — |
| **W0-b 谱系补齐** | `FLOORPLAN_ANALYZE` 建资产（或合成虚拟根节点）；`/layouts/ai`、`/floorplan-scenes` payload 补 `asset_parent_id`；03/04/05 的 `asset_parent_id` 从 `Form(None)` 改必填；历史数据回填 | 1 天 | W0-a |
| **W0-c 缩略图** | 后端生成 256/512 webp 写入 `thumbnail_url`，原图另存 `fullUrl`（现状是返回全尺寸原图，40 个节点的画布会卡死）；`/artifacts` 加 Cache-Control；存量文件回填 | 0.5~1 天 | — |
| **W0-d 画布持久化** | `canvases(id, project_id, name, viewport_json, updated_at)` + `canvas_nodes(id, canvas_id, asset_id, variant_id, job_id, x, y, w, h, z, source_node_id, deleted_at)` + CRUD + 批量坐标 PATCH；删除/副本/粘贴全部落 canvas_nodes 软删，`SceneAsset` 保持不可删。`database.py:31` 用 `create_all`，新表零迁移成本 | 1.5~2 天 | W0-a |
| **W0-e 逐图展开器** | 统一「资产 → 逐图变体」服务，覆盖 ai_workflow/layout/floorplan/white_model/material 五种 deliverables 形状；注意 layout 的 `previewUrls` 与 `metadata.variantIds` 靠数组下标隐式配对，必须改成结构化配对。建议新文件 `app/canvas.py` | 1~1.5 天 | W0-d |
| **W0-f 部分结果可见** | 让取消/失败 job 的 partial outputs 作为临时节点出现在画布（用户付过费的图不能刷新即消失） | 0.5 天 | W0-e |
| **W0-g 后端测试修复** | `test_assets.py` / `test_ai_workflow_derivatives.py` / `test_workflow_resume.py` 建立在「一 job 一资产、单一审批」假设上，需同步改 | 1 天 | 全部 |
| **W0-X（可选，见 D1）** | variant 粒度审批 / 分叉：审批下沉到 `(asset_id, variant_id)`，改写 `_validate_parent_approved_space` / `_workflow_approved_output` / workflow-resume | +2~3 天 | W0-e |

---

## 4. 技术路线选择（已修正）

节点内含富交互（审批按钮、P1-P4 坐标输入、画笔标注、进度条），**canvas 自渲染仍然否决**。
但原方案否决 React Flow 的理由（「富卡片会被约束」）经评审核实**不成立**——custom node 就是任意 React 组件，且参考截图要的贝塞尔连线 / 小地图 / 点阵背景 / 缩放百分比 / 框选 / `onNodeContextMenu` / `fitView` 全部是它的现成 API。

**决策：先花 0.5 天 spike 验证 `@xyflow/react`**（React 19.2 兼容性 + 自定义节点塞现有卡片 + CSS 变量覆盖深色）。
- spike 成功：画布引擎 2.5~4 天
- spike 失败：回落自研 DOM + CSS Transform `CanvasViewport`，工期改 4~6 天
- 顺手清理 0 引用依赖（konva / react-konva / axios / react-hook-form 等）

---

## 5. 落地路线图（W-1 → W6）

| 阶段 | 内容 | 估时 | 硬依赖 |
|---|---|---|---|
| **W-1 决策** | 拍板 D1~D5（见第 7 章）+ 第 6 章能力表三列裁决（必做/降级占位/不做） | 0.5 天（会议） | — |
| **W0** | 后端前置（第 3 章） | 6.5~8 天 | W-1 |
| **W1 设计令牌化** | 深色 token；**作用域化先上**（只给画布 + 新页面），「01 编辑器 + AssetLibrary 亮暗接缝」登记为已知问题单独排期 | 0.5~1 天（作用域） | 可与 W0 并行 |
| **W2 画布引擎** | 见第 4 章 | 3~6 天 | W0-e |
| **W3 状态层重构** | `jobs` 改 `Map<nodeId, Job>`；`busyStage` 改 `Set<jobId>`（含 finally 解锁 bug 修复）；三个 baseline 改节点派生值；`AbortController` 改 Map；生成/审批/设基准抽成**不依赖 `activeStage` 的纯函数模块**，向导和画布共用；设基准不再删下游 job、不再跳阶段 | 5~7 天 | W0，先于 W4 |
| **W4 节点操作条 + 右键菜单** | 操作表由第 6 章规则生成；非法项置灰并说明；`canRunAction(node, action)` 纯函数是唯一真值来源；快捷键带 `isEditableTarget()` 守卫；409 时节点上渲染「上游已变更，点此重新绑定基准」 | 2 天 | W3 |
| **W5 前端测试接线** | vitest + playwright + `test` 脚本；覆盖视口数学、`${jobId}:${variantId}` 节点身份、job→asset 身份交接 | 2 天 | W2 |
| **W6 首页改版** | 引入 react-router；左侧 56px 图标栏；问候语 + 大输入框（接已有 `design_prompt` 字段）；项目卡片墙（需 `Project.updated_at` + 封面列，+0.5 天后端）；模型级联降级为静态 pill；案例墙用用户自己的资产喂 | 3~5 天 | W0-a |

**合计：**
- **MVP（W0-a/b/c + W2 + W4 精简版 + W1 作用域，只读画布 + 基础节点操作，生成仍走向导，无首页、无分叉、无多画布）：10~14 人日**
- **截图级还原（全部 W-1 → W6 + W0-X）：27~35 人日 ≈ 5~7 周**

**探针选项（强烈建议先做）：** 2~3 天、零后端改动的「方案地图」——向导右侧加可收起只读面板，用 `/v1/assets?limit=100` 按 `workflowStage` 分列、按 `parentAssetId` 画 SVG 贝塞尔、CSS transform 平移缩放，点节点复用 `WorkflowAssetPicker` 的 `approveAndResume`/`resumeAsset`。用于回答 D1 的关键问题：用户到底会不会尝试从非批准节点派生？（诚实标注：受限于无 projectId 过滤，它只能显示「最近 100 个资产」跨项目混排，UI 须如实说明。）

---

## 6. 节点操作与连线规则（由 `main.py:648-672` 生成，不要手写）

合法的 parent → child 关系（连线只能这样画）：

| 下游阶段 | 合法上游 | 源码约束 |
|---|---|---|
| 彩平 color_plan | 平面布局 / 户型资产（layout 须已审批） | `main.py:648-652` |
| 轴侧 axonometric | 已批准彩平 | `:653-655` |
| 分空间 space_render | 已批准彩平 **或** 轴侧 | `:656-661` |
| 风格 style_scheme | 已批准分空间 | `:662-664` |
| 色调 tone_scheme | 已批准**风格**（不是空间！） | `:665-667` |
| 局部修改 local_edit | **色调**（不是风格！） | `:668-670` |

各类节点的合法操作（非法项**置灰并说明原因**，不隐藏——参考截图范式）：

| 节点 | 操作 |
|---|---|
| 原图/户型（01，W0-b 后才有资产） | 查看结构标注 / 重新识别 / 生成布局 |
| 布局（02） | 批准布局 / 生成彩平 |
| 彩平（03） | 批准彩平 / 生成轴侧 / 生成分空间 |
| 轴侧（04） | 批准 / 生成分空间 |
| 分空间（05） | 批准空间 / 生成风格方案 |
| 风格（06） | 批准风格 / 生成色调方案 |
| 色调（07） | 批准色调 / 局部修改（红笔标注） |
| 局部修改（08） | 打开原图 / 下载 |
| 通用 | 打开原图 / 下载 / 复制节点 ⌘C / 创建副本 ⌘D / 删除 ⌘⌫（后三者依赖 W0-d 软删层） |

两条额外硬规则：
- **改批抑制**：已有下游资产的节点，「改批其他 variant」置灰并提示「该批准版本已有下游资产」（`main.py:979-986`，除非做了 W0-X）
- **spaceId 约束**：一批 12 张分空间图共用一个审批值时，只有匹配 `spaceId` 的那张能派生下游（`main.py:722`）——这也是 D1 的核心动机

---

## 7. 必须先拍板的决策（W-1 会议）

- **D1. variant 粒度审批 / 分叉（最重要）**：不做 → 画布是「更好看的向导」，分叉 DAG 画不出；做 → +2~3 天。**建议：做，但排在 MVP 之后**，以「方案地图」探针观察到的真实使用信号为准
- **D2. 画布引擎**：**建议先 0.5 天 spike `@xyflow/react`**，失败再自研（4~6 天）
- **D3. 深色主题**：**建议作用域化先上**（0.5~1 天），01 编辑器亮暗接缝登记为已知问题；全站 token 化（2~4 天）不塞进本次
- **D4. 多画布**：**建议直接做完整版**（canvases + canvas_nodes 双表，1.5~2 天，含在 W0-d），但「画布 1 ▾」UI 藏到 v2
- **D5. 第 8 章能力表逐行裁决**：必做 / 降级占位 / 不做，开一次会拍完，**必须在写任何代码之前完成**

原方案的 3 个问题结论保留：画布为主向导为导航（旧入口保留一段时间）；一张图一个节点（依赖 W0-e 逐图展开）；01 结构标注编辑器双击进全屏专注模式（3094 行重型 Konva/SVG 编辑器，不塞进节点）。

---

## 8. 视觉与交互规范（用户指定参考：风暴AI 正式版 5 张截图）

### 必须落地的样式语言

- **深色主题**：背景 ≈ #0a0a0d，卡片 ≈ #17181c，1px 半透明白边框（rgba(255,255,255,.08)）；选中/主操作色蓝 #3b82f6 系
- **点阵画布背景**（小圆点网格，随视口平移缩放）
- **圆角**：卡片 ≈ 12px；悬浮工具栏/按钮胶囊全圆角
- **节点卡片**：深色卡片 + 顶部标题栏（类型图标 + 名称）+ 图片区；**节点间贝塞尔连线**
- **节点右键菜单**：深色浮层 + 操作项 + 快捷键标注，非法项置灰
- 底部居中悬浮工具栏；左下「资产管理 + 缩放百分比」；右下小地图；顶部项目名 + 画布标签
- 首页：左侧 56px 图标导航 + 中央问候语 + 大输入框 + 新建项目卡片 + 最近项目缩略图横排

### 明确不做（SaaS 外壳，单人本地实例放了就是假 UI）

⚡点数余额 / 会员升级 / 限时折扣 / 赛事卡片 / 风暴学堂 / PS插件 / 合伙人 / 邀请有礼 / 三级模型级联（降级为静态 pill，显示 `/health` 里配置的模型名）

### 降级处理

- 精选案例瀑布流 → 用用户自己已生成的资产喂
- 「点击图片上的物体进行标记」提示语 → **不照抄**：风暴是点选分割，我们 08 阶段是手绘闭合红圈，提示语按我们的实际交互写

### 待 D5 裁决

以上为 mood board 级清单，W-1 会议上逐行裁决为「必做 / 降级占位 / 不做」三列表后才算 spec。

---

## 9. 与《优化计划.md》的关系（交互表，不是一句「不冲突」）

| 优化计划项 | 与画布的关系 |
|---|---|
| A1/B1/B3/C1（已落地） | 不阻塞画布；增量发布的骨架卡片天然变成骨架节点 |
| A5/C4/C5/D3 | 可与画布并行 |
| C3（WAL + busy_timeout，未落地） | **注意**：实际运行库是 PostgreSQL（.env 覆盖），SQLite 锁定风险不适用，降级为并行项 |
| 「不做 busyStage→Set」（`优化计划.md:312`） | **决策必须撤销**——画布多任务并发把它反转成刚需（含 `finally` 解锁 bug 修复），已计入 W3 |
| B3 取消路径不归档资产 | 画布「一图一节点」下，取消时已生成的图会刷新即消失 → W0-f 处理 |
| 测试 | 前端 0 用例是项目史上最大前端改动的风险敞口 → W5 接线 |

**排序建议维持：先完成 A5 实测 / C4 / C5（后端收尾），再启动 W0。**
