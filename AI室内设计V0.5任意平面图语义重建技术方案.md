# AI 室内设计 V0.5：任意平面图语义重建技术方案

> 文档版本：V0.5
>
> 更新日期：2026 年 8 月 1 日
>
> 状态：本地 MVP 实施范围

## 1. 版本目标

V0.5 把 V0.4 的单一黄金样例能力升级为面向新上传户型图的通用处理链路。用户上传
不同户型图后，外部多模态视觉模型识别房间、墙体、门窗、家具和文字尺寸，生成该图
专属的 `semanticLayout`；用户在网页确认或校正后，系统再做拓扑、尺寸与碰撞校验。
最终生成保留“AI 直出”和“精确三维”两种模式：前者追求快速、高质量单张结果，后者
由 Blender 按真实毫米坐标建模，保留可编辑、可复现和多视角能力。

```text
任意平面图
→ 外部多模态视觉模型识别
→ q10000 归一化 semanticLayout 草稿
→ 尺度标定并换算为毫米
→ 网页人工校正
→ 拓扑、尺寸、引用与碰撞校验
→ 选择生成模式
  ├─ AI 直出：原图 + Semantic 约束图 + 风格图 → gpt-image-2
  └─ 精确三维：semanticLayout → Blender → gpt-image-2／ComfyUI
→ 最终结构一致性检查
```

V0.5 的核心原则是：

1. 每张上传图都产生自己的 `semanticLayout`，不得套用固定户型模板；
2. 多模态视觉模型负责“看懂平面图”，图像生成模型不负责输出几何真值；
3. 模型输出先作为可编辑草稿，人工校正后的数据才是当前项目的结构真值；
4. 两种生成模式都只能消费通过契约与拓扑校验的数据；
5. AI 直出可跳过 Blender，但不能跳过 `semanticLayout`、人工确认和生成后结构审计；
6. 精确三维模式中，Blender 只消费通过校验的毫米数据，最终增强不能取代结构建模；
7. V0.4 的 SHA fixture 只用于确定性回归测试，不是生产识别入口。

## 2. MVP 输入范围

### 2.1 本轮支持

- PNG、JPEG 等清晰的单层住宅平面图；
- 俯视、正交、主体完整且方向明确的图纸；
- 以水平和垂直墙为主的普通户型；
- 中文或英文房间名称、常见尺寸标注；
- 卧室、客厅、餐厅、厨房、卫生间、阳台、走廊等常见房间；
- 墙体、门、窗和床、沙发、餐桌、洁具、橱柜等常见家具／设备；
- 用户可输入总宽、总深，也可由视觉模型从总体尺寸、外部尺寸链或比例尺读取并回填；
- 自动回填的物理尺寸必须在网页核对后才能提交 Blender；
- 低置信结果在网页中补画、移动、删除、改类型和改尺寸。

### 2.2 本轮不承诺

- 手绘草图、透视拍照、严重遮挡或极低分辨率图纸；
- 复杂斜墙、弧墙、复式、错层、楼梯和多层联动；
- 从模糊标注恢复施工级绝对尺寸；
- 判断承重结构、管线、消防或施工规范合规性；
- 精确识别家具品牌、SKU 和材料型号；
- 一次模型调用后无需人工确认即可直接施工。

超出 MVP 范围的图纸仍可返回识别草稿，但必须显式标记风险，不能静默进入最终增强。

## 3. 双链路架构

V0.5 使用两条职责严格分离的外部 API 链路。

### 3.1 链路 A：视觉语义识别

用途：把上传的平面图转换为结构化 `semanticLayout` 草稿。

```text
POST {KUYAO_BASE_URL}/chat/completions
Authorization: Bearer ${KUYAO_API_KEY}
Content-Type: application/json
```

请求使用 `KUYAO_VISION_MODEL` 指定的、支持图片输入的 chat/completions 多模态模型。
图片随消息内容发送，提示词要求模型严格按照 JSON 契约返回：

- 图纸边界与方向；
- 房间类型、名称和多边形；
- 墙体中心线、内外墙类型和可见厚度线索；
- 门窗位置、轴向、关联房间和开启方向（能判断时）；
- 家具类型、中心、占地、朝向和所属房间；
- OCR 读到的总尺寸、分段尺寸和房间面积；
- 每个对象的置信度、证据与待确认问题。

视觉模型不能直接创建最终效果图，也不能把“看起来合理”的新房间或家具补进结构
真值。响应必须先经过 JSON Schema 校验和几何后处理。

### 3.2 链路 B：最终图像生成／增强

用途：根据用户选择生成最终 ArchViz 效果。两种模式共用同一个 `images/edits` 适配器，
但输入参考图和质量门不同。

```text
POST {KUYAO_BASE_URL}/images/edits
Authorization: Bearer ${KUYAO_API_KEY}
Content-Type: multipart/form-data
model=gpt-image-2
```

`images/edits` 必须使用 `multipart/form-data`，不能使用 JSON。单参考图使用 `image`，
多参考图重复使用 `image[]`。

AI 直出输入包含：

1. 原始平面图，作为房间、墙体、门窗和家具关系的首要视觉依据；
2. 由已确认 `semanticLayout` 绘制的 Semantic 约束图，锁定房间边界和家具锚点；
3. 可选的目标材质／风格参考图，仅约束视觉风格。

精确三维输入可以包含：

1. 已确认结构的 Blender 基础 RGB；
2. 可选的 Depth、Semantic 或 Edge 控制参考；
3. 可选的目标材质／风格参考图；
4. 明确要求保持房间数量、墙体、门窗、家具占地和相机不变的提示词。

当前目标模型为 `gpt-image-2`。调用方同时兼容 `data[0].b64_json` 和 `data[0].url`
两种响应形式。下载 URL 时必须设置大小、超时和内容类型限制，并把结果复制到本地
artifact，不能把临时外链直接当作长期业务数据。

### 3.3 为什么不能只用图像生成 API

图像编辑模型可以生成视觉上合理的鸟瞰室内图，但其像素结果不能稳定表达：

- 墙体中心线和厚度；
- 门窗所属墙段及连接房间；
- 房间闭合拓扑；
- 家具的毫米占地和碰撞关系；
- 后续可重复编辑的对象 ID。

因此，`gpt-image-2` 可以直接完成最终效果图，但仍只属于最终表现层；它不能替代可编辑
结构真值。`semanticLayout` 必须来自视觉结构化链路、人工校正和确定性校验。需要单张
高质量方案图时可选 AI 直出；需要精确尺寸、对象编辑、多机位一致性或后续资产替换时应选
精确三维。

## 4. 配置与密钥安全

API 服务通过以下环境变量配置通用视觉识别：

```bash
FLOORPLAN_VISION_PROVIDER=kuyao
KUYAO_API_KEY=
KUYAO_BASE_URL=https://www.kuyaoapi.com/v1
KUYAO_VISION_MODEL=gpt-5.6-sol
KUYAO_VISION_TIMEOUT_SECONDS=180
FLOORPLAN_FINAL_IMAGE_PROVIDER=auto
KUYAO_IMAGE_MODEL=gpt-image-2
KUYAO_IMAGE_QUALITY=high
KUYAO_IMAGE_TIMEOUT_SECONDS=900
KUYAO_STYLE_REFERENCE_IMAGE=
```

字段说明：

| 变量 | 作用 |
|---|---|
| `FLOORPLAN_VISION_PROVIDER` | 视觉识别适配器；`kuyao` 启用外部多模态识别，空值或 `local` 使用兼容路径 |
| `KUYAO_API_KEY` | Bearer Token，只允许放在被 Git 忽略的 `apps/api/.env` 或进程环境中 |
| `KUYAO_BASE_URL` | OpenAI 兼容 API 根地址，默认包含 `/v1`，程序应规整末尾斜杠 |
| `KUYAO_VISION_MODEL` | 支持图片输入的 chat/completions 模型名，应以供应方模型列表为准 |
| `KUYAO_VISION_TIMEOUT_SECONDS` | 单次语义识别超时；超时后任务失败并允许重试，不自动提交 Blender |

安全约束：

- 真实 Key 不得写入源码、README、`.env.example`、请求日志、任务 Manifest 或 Git；
- 前端不得接收、保存或透传供应方 Key；
- API Key 只能由服务端从环境变量读取；
- 日志只记录供应方、模型、耗时、状态码和请求 ID，不记录 Authorization Header；
- 用户已经同意将上传的平面图发送给外部多模态服务，界面仍应显示该事实；
- 若 Key 曾出现在聊天、截图或共享记录中，应在供应方后台轮换并更新本地 `.env`；
- 生产环境应设置上传保留期、访问控制、删除策略和供应方数据政策告知。

`KUYAO_VISION_MODEL=gpt-5.6-sol` 是当前源码默认值。若供应方模型列表中不存在该名称，必须在
本地 `.env` 改为实际支持图片输入的模型，不能回退到 `gpt-image-2` 做结构识别。

## 5. q10000 坐标契约

### 5.1 为什么模型不直接输出毫米

视觉模型通常能较稳定地判断对象在图中的相对位置，但未必能可靠读取每一条尺寸标注。
如果要求它直接输出毫米，容易把估算尺寸误当成真实尺寸。V0.5 因此要求模型先在户型
外包围盒内输出 `0..10000` 的整数坐标，称为 `q10000`：

```json
{
  "coordinateSystem": {
    "unit": "q10000",
    "origin": "top_left",
    "xAxis": "right",
    "yAxis": "down",
    "extent": 10000
  }
}
```

无论上传图是 800 × 600 还是 4000 × 3000 像素，同一相对位置都使用相近的整数坐标，
从而避免把截图缩放、留白或压缩误差直接带入业务数据。

### 5.2 q10000 到毫米

用户确认 `plan.widthMm` 和 `plan.depthMm` 后，服务端执行确定性换算：

```text
xMm = round(xQ / 10000 × plan.widthMm)
yMm = round(yQ / 10000 × plan.depthMm)

widthMm = round(widthQ / 10000 × plan.widthMm)
depthMm = round(depthQ / 10000 × plan.depthMm)
```

示例：总宽为 `8150 mm`，某竖墙的 `xQ = 7500`，则：

```text
xMm = round(7500 / 10000 × 8150) = 6113 mm
```

视觉尺寸只作为标定候选。优先级为：

1. 用户明确输入并确认的总宽／总深；
2. 视觉模型读取且用户确认过的总体尺寸或外部尺寸链；
3. 已知比例尺和图纸边界；
4. 无绝对尺度时提示用户手动填写，禁止根据像素比例、家具或房间面积猜测毫米尺寸。

若只有一个方向的可靠总尺寸，可以先保持图纸宽深比推算另一个方向，但必须标记
`inferred: true` 并要求用户确认。X、Y 方向应分别标定，不能因为图像被拉伸而共用一个
像素比例。

## 6. V0.5 `semanticLayout` 草稿

视觉模型响应建议分为原始归一化草稿和经校正的毫米布局两层：

```json
{
  "version": "0.5",
  "source": {
    "provider": "kuyao",
    "model": "configured-at-runtime",
    "mode": "vision",
    "requiresHumanReview": true
  },
  "coordinateSystem": {
    "unit": "q10000",
    "origin": "top_left",
    "extent": 10000
  },
  "plan": {
    "bounds": {"x": 320, "y": 280, "width": 9360, "depth": 9440},
    "widthMmCandidate": 8150,
    "depthMmCandidate": 6060,
    "dimensionConfidence": 0.84
  },
  "rooms": [],
  "walls": [],
  "openings": [],
  "furniture": [],
  "dimensions": [],
  "warnings": []
}
```

每个对象至少包含：

- 稳定、请求内唯一的 `id`；
- 语义 `type` 和界面名称 `name`；
- q10000 几何；
- `confidence`；
- 可选的 `evidence` 和 `needsReview`；
- 房间、墙体与开口之间的引用关系。

API 不直接信任模型输出。未知字段应拒绝或隔离，数值必须限制在合法范围，字符串和
数组必须设置长度上限。模型原始响应可作为受限诊断 artifact 保存，但不能直接进入
Blender 脚本。

## 7. 识别与后处理流水线

### 7.1 上传预处理

1. 校验文件类型、大小、像素数和解码结果；
2. 修正 EXIF 方向，保留未修改原图；
3. 检测图纸主体边界，记录裁切矩形但不破坏原图；
4. 生成适合视觉模型的预览图，避免把超大原图直接发送；
5. 计算 SHA-256，仅用于缓存、审计和回归定位，不用于套用固定布局。

### 7.2 视觉识别

一次请求应同时要求全局理解与结构化输出，避免房间、墙体和家具分别调用后产生坐标
漂移。对于返回不完整 JSON、拒绝响应、超时或不支持图片的模型，任务应明确失败，并
允许用户重试或切换兼容的人工墙线流程。

### 7.3 确定性规范化

模型响应进入业务层前必须执行：

- JSON Schema 和类型校验；
- q10000 坐标裁剪及退化线段删除；
- 水平／垂直墙吸附；
- 共线墙合并和短碎线抑制；
- 门窗吸附到最近且方向一致的墙；
- 房间多边形闭合及顺逆时针统一；
- 重复对象消解；
- 家具归属房间计算；
- q10000 到毫米的独立 X／Y 标定；
- 原始对象到规范化对象的变更记录。

规范化算法可以修正数值噪声，但不得自行改变房间类型或凭空添加大块结构。
水平／垂直候选使用随线段长度变化且有绝对上限的 q 坐标吸附。明显偏离声明轴向的候选
不能被强行掰直：系统应隔离该墙／门窗、保留其余可用语义草稿，并在网页明确列出需要
人工补画的对象 ID；过滤后少于 4 条有效墙体时才拒绝整次识别。

## 8. 网页人工校正

视觉模型输出不是最终真值。前端必须把原图与语义叠加显示，并提供至少以下操作：

- 选择、移动、增删和拆分墙段；
- 增加、删除门窗并修改类型、宽度和所属墙；
- 编辑房间边界、名称和类型；
- 移动、缩放、旋转家具并修改所属房间；
- 修改总宽、总深、层高和墙厚；
- 查看低置信对象、OCR 尺寸证据和校验错误；
- 撤销／重做，保存识别原稿和人工修订版本；
- 点击“确认结构”后才允许创建 Blender 基础场景。

人工修改应使用稳定对象 ID，并记录 `source: "human"`、修改前后值和时间。再次调用
视觉模型时不能静默覆盖用户修改。

当前 V0.5 MVP 采用毫米数值编辑器完成最低可用闭环：房间可逐顶点修改边界，门窗可修改
端点、类型和关联房间并支持新增／删除，家具可修改位置、占地、旋转、类型和所属房间并支持
新增／删除。前端提交 `semanticReviewConfirmed=true`，后端仍会独立校验该字段和完整
`semanticLayout`；任何尺寸或语义编辑都会把确认状态恢复为 `review_required` 并废弃旧渲染。

## 9. 拓扑、尺寸与碰撞校验

从 q10000 换算为毫米并完成人工校正后，至少执行以下质量门：

1. **契约完整性**：ID 唯一，引用对象存在，数值有限且在户型范围内；
2. **墙体拓扑**：墙端点吸附，外轮廓闭合，禁止大段重复墙和零长度墙；
3. **房间闭合**：房间多边形不自交，面积大于下限，主要边界由墙体支持；
4. **门窗合法性**：开口位于墙上、方向一致、宽度合理，门连接的房间存在；
5. **尺度一致性**：分段尺寸链与总尺寸差值在容差内，X／Y 标定不混用；
6. **家具归属**：家具中心位于所属房间，占地不明显越墙；
7. **硬碰撞**：床、沙发、橱柜等主要占地不与实体墙或彼此严重重叠；
8. **置信度质量门**：关键外墙、主要房间或总尺度低置信时必须人工确认；
9. **变更可追溯**：保留视觉原稿、规范化结果、人工修订和最终毫米布局摘要。

校验失败时返回对象级错误，让用户在网页定位；不能靠 Blender 自动“挪一下家具”来
掩盖结构错误。

最终图另有独立的像素结构质量门。精确三维模式中，Kuyao 或 ComfyUI 候选未达到边缘保留
阈值时不得成为最终图，系统返回已配准的本地保守图。AI 直出没有 Blender 像素底图可作为
最终回退，因此用 Semantic 墙线执行生成后审计：通过则标记结构指标，通过不了仍保留候选，
但必须明确标记 `requiresUserConfirmation=true`，不能把审计失败包装成结构可靠。

写实模型可能返回与请求略有不同的像素尺寸和轻微取景变化，不能仅因尺寸不相等就误拒绝。
精确三维模式使用基础图与候选图的户型外轮廓四边形执行受限单应配准，并限制缩放、XY 各向异性、
旋转和透视比；随后从 Blender Semantic 控制图提取墙体边缘，同时计算全局保留率和 4×4
局部分区的最小值／20 分位值。通过者统一回写到基础图尺寸，局部墙漂移即使全局分数较高
也必须拒绝；没有 Semantic 控制图时继续使用原有的严格像素路径。

## 10. Blender 场景编译

Blender 只接收通过质量门的毫米 `semanticLayout`：

```text
rooms      → 分区地面、房间标签和材质区域
walls      → 按长度、厚度、层高创建实体墙
openings   → 拆墙后的门洞、窗洞及门窗资产
furniture  → 按中心、占地、角度放置程序化代理或 GLB
plan       → 全屋相机、裁切范围和比例
```

建模时执行 `mm → m`，并统一二维 Y 轴到 Blender 场景轴的转换。禁止根据总宽度再次
选择 Studio、一室或两室固定模板；模型识别不到的家具应保持为空或明确显示建议，不能
由旧模板填满。

基础阶段输出同一相机下的 RGB、Depth、Normal、Semantic 和 Edge。用户确认基础图中的
房间、墙体、门窗、家具和空间尺度正确后，才允许调用耗时的最终增强。

## 11. fixture 的新职责

V0.4 的 `example/平面图.jpeg` SHA fixture 在 V0.5 中只用于自动化回归：

- 验证 q10000／毫米换算和 schema 兼容；
- 验证拓扑校验器和 Blender 场景编译没有退化；
- 在外部 API 不可用时运行离线测试；
- 对比视觉模型输出与人工真值，统计识别质量。

生产流程不得按 SHA 直接返回 fixture，也不得对“看起来相似”的图套用已有户型。新图
必须经过视觉识别或人工绘制产生独立的 `semanticLayout`。fixture 可以作为评测真值，
不能冒充通用识别结果。

## 12. 任务、超时与失败处理

视觉识别与最终增强都应作为异步任务处理：

- 前端提交后获得 `jobId`，通过轮询恢复状态；
- 视觉调用使用 `KUYAO_VISION_TIMEOUT_SECONDS`，超时只终止等待，不代表供应方一定停止；
- 重试必须有幂等键，避免重复计费；
- 浏览器刷新后可按 `jobId` 恢复，不依赖单个长连接；
- 识别失败保留原图和错误摘要，不创建伪造 `semanticLayout`；
- AI 直出失败时保留原图、Semantic 约束图和错误摘要；精确三维增强失败时保留 Blender 基础图；
- 供应方返回 URL 时，由服务端下载并落盘后任务才算完成；
- 任务状态区分 `queued`、`analyzing`、`needs_review`、`validated`、`rendering`、
  `enhancing`、`succeeded` 和 `failed`。

## 13. MVP 验收标准

### 13.1 通用性

- 至少使用多张未注册 SHA、不同布局的清晰住宅平面图进行测试；
- 每张图均通过视觉 API 产生独立 `semanticLayout`；
- 未知图片不再静默退回并冒充通用 Hough 语义结果；
- fixture 命中与否不改变生产识别路径。

### 13.2 结构识别

- 主要房间数量、类型和相对位置可在网页核对；
- 外轮廓与主要内墙可编辑，低置信墙段可定位；
- 主要门窗和家具具有 q10000 坐标、置信度及对象 ID；
- 用户确认尺寸后可稳定转换为毫米；
- 所有关键校验错误都能定位到具体对象。

MVP 不要求首轮自动识别 100% 正确，而要求“自动生成可用草稿 + 用户能低成本改正 +
错误不会绕过质量门”。

### 13.3 双模式生成与最终增强

- AI 直出请求不启动 Blender，输入至少包含原始平面图和 Semantic 约束图；
- AI 直出结果记录模式、供应方、耗时、结构审计指标和人工确认要求；
- Blender 的房间、墙体、开口和家具来自当前图的毫米布局；
- 不再因户型宽度选择固定模板；
- 全屋相机完整包含户型，家具不集中堆叠；
- 基础图未确认时外部图像增强调用次数为 0；
- 最终增强保持房间数量、墙体位置、开口和主要家具占地；
- 供应方、模型、请求耗时、结构校验和人工确认状态写入 Manifest，但不写 Key。

## 14. 已知风险与后续演进

1. 多模态模型可能漏墙、误读尺寸或返回格式错误，必须保留人工校正；
2. 不同视觉模型的坐标精度和 JSON 遵循度不同，应保持 provider adapter；
3. 供应方模型名、价格和能力可能变化，不能把模型列表硬编码为业务规则；
4. 外部 API 涉及隐私、网络、超时和费用，应提供清晰告知和任务审计；
5. q10000 解决分辨率归一化，不自动解决图纸拉伸、透视和错误比例；
6. OCR 尺寸冲突时需要用户选择可信标注；
7. AI 直出和图像增强都可能产生布局漂移，必须执行生成后审计；精确三维还必须允许回退；
8. V0.6 可进一步加入专用墙体／门窗分割、OCR 尺寸图优化、斜墙支持、真实 GLB 资产
   检索、多相机一致性和更完整的建筑拓扑求解。

> 所有识别、尺寸和效果图用于概念设计与本地开发验证，不作为施工、消防或工程验收依据。
