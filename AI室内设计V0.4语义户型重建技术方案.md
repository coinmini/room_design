# AI 室内设计 V0.4：语义户型重建技术方案

> 文档版本：V0.4
>
> 更新日期：2026 年 8 月 1 日
>
> 状态：本地 MVP 实施范围

## 1. 版本目标

V0.4 解决的首要问题不是“让扩散模型画得更写实”，而是让进入 Blender 与
ComfyUI 的结构真值正确。对于固定验收图 `example/平面图.jpeg`，系统先获得以毫米为
单位的房间、墙体、门窗和家具语义，再创建三维场景：

```text
平面图
→ 语义户型 semanticLayout（毫米坐标）
→ Blender 真实三维结构与家具锚点
→ 基础图结构质量门
→ Depth 等控制图
→ ComfyUI 写实增强
→ 最终结构一致性质量门
```

V0.4 的核心原则是：

1. `semanticLayout` 是空间和布局真值；
2. Blender 是可验证的三维执行层；
3. ComfyUI 只增强材质、软装外观、光照和细节；
4. IP-Adapter 参考图只约束视觉风格，不提供当前户型的几何结构；
5. 基础结构不合格时，不启动耗时的扩散模型任务。

## 2. V0.3 方案为什么会失败

### 2.1 Hough 线段不是建筑墙体语义

V0.3 使用图像边缘和 Hough 直线检测产生候选墙线。该方法可以快速找到大量横线、
竖线，但不能判断线条代表外墙、内墙、门扇、家具轮廓、尺寸标注还是图框。

在当前验收图中，自动候选曾把以下对象当成墙：

- 电视柜和电视的边缘；
- 沙发、床和柜体的轮廓；
- 门扇及开门弧线；
- 同一墙体的两侧边缘，形成重复墙；
- 尺寸线或相邻的短线段。

同时，按置信度阈值截断候选又可能漏掉卫生间下墙、建筑外轮廓和短墙。结果虽然在
二维叠加图上“线很多”，却没有形成正确的闭合房间拓扑。

### 2.2 像素框选房间不能表达全屋拓扑

V0.3 的 `roomSelection` 只是一块像素矩形。它可以表示一个目标区域，却不能表达：

- 两个卧室、卫生间、客餐厅和厨房之间的邻接关系；
- 哪段墙上存在门洞或窗洞；
- 门连接哪两个房间；
- 家具属于哪个房间、朝向哪里；
- 客餐厅与狭长厨房的边界；
- 家具占地、通行空间和墙体碰撞。

因此，把客餐厅矩形扩成三维空间不能等价于重建整套户型。

### 2.3 硬编码模板覆盖了原图信息

旧 Blender 脚本根据总宽度选择 Studio、一室或两室模板，再按固定比例创建地板、
隔墙和家具。模板中的沙发、电视、餐桌和厨房位置来自预设比例，不来自上传图的家具
位置。典型偏差包括：

- L 形沙发变成直排沙发；
- 电视方向和墙面错误；
- 六人餐区变成更小的固定餐区；
- 狭长厨房变成侵入客厅的 L 形橱柜；
- 卫生间缺少浴缸或洁具位置错误；
- 空间尺度被压缩，家具集中堆叠。

Depth ControlNet 会忠实约束这个错误的 Blender 基础图，因此增加 checkpoint、
Refiner 或采样步数只能美化错误布局，不能恢复原始平面图。

## 3. V0.4 MVP 范围

### 3.1 本轮包含

- 新增 `schemaVersion: "0.4"`；
- 以毫米为单位的 `semanticLayout` 契约；
- 房间、墙体、门窗和家具锚点；
- 墙体在门窗位置预先拆段；
- 固定黄金样例的 SHA-256 精确匹配适配器；
- 语义布局到二维校正界面和 Blender 场景的传递；
- 语义模式不再使用旧的固定户型模板；
- 先生成基础图并验证，再允许执行最终增强；
- 最终图继续执行图像空间的结构边缘质量检查；
- V0.2／V0.3 非语义请求保留兼容路径。

### 3.2 本轮不包含

- 任意户型图的通用 AI 识别；
- OCR 尺寸自动校准和施工级测量；
- 自动识别所有门窗开启方向；
- 从原图恢复具体家具品牌、模型和材质；
- BIM／IFC 级墙体连接和构造层；
- 自动保证人体工学、消防和施工规范；
- 用 IP-Adapter 替代结构识别。

## 4. `semanticLayout` 数据契约

### 4.1 坐标系

语义数据使用真实毫米坐标，不使用相对于图片尺寸的归一化比例：

```json
{
  "version": "0.4",
  "profileId": "cn_two_bedroom_8150x6060_v1",
  "sourceSha256": "0c9e2706...",
  "coordinateSystem": {
    "unit": "mm",
    "origin": "top_left",
    "xAxis": "right",
    "yAxis": "down"
  },
  "plan": {
    "widthMm": 8150,
    "depthMm": 6060,
    "ceilingHeightMm": 2800
  }
}
```

坐标约定：

- 原点位于户型外包围盒左上角；
- X 轴向右，Y 轴向下；
- 所有尺寸以毫米保存；
- Web 仅在显示时将毫米映射到图片像素；
- Blender 在建模时将毫米换算为米，并把二维 Y 轴转换到三维场景坐标；
- 语义样例的 `plan.widthMm` 和 `plan.depthMm` 是内在尺度，不应因浏览器缩放而改变。

二维叠加的换算可表示为：

```text
pixelX = detectedBounds.x + xMm / plan.widthMm × detectedBounds.width
pixelY = detectedBounds.y + yMm / plan.depthMm × detectedBounds.height
```

### 4.2 房间 `rooms`

每个房间必须具有稳定 ID、类型、名称和闭合多边形。当前矩形样例同时保存 `rect`，
便于前端框选和 MVP Blender 快速建模：

```json
{
  "id": "room_living_dining",
  "type": "living_dining",
  "name": "客餐厅",
  "rect": {
    "xMm": 100,
    "yMm": 2680,
    "widthMm": 6960,
    "depthMm": 3280
  },
  "polygon": [
    {"xMm": 100, "yMm": 2680},
    {"xMm": 7060, "yMm": 2680},
    {"xMm": 7060, "yMm": 5960},
    {"xMm": 100, "yMm": 5960}
  ]
}
```

`type` 是程序消费字段，`name` 是界面显示字段。V0.4 黄金样例包含：

| 房间类型 | 数量 | 作用 |
|---|---:|---|
| `bedroom` | 2 | 两间卧室 |
| `bathroom` | 1 | 卫生间 |
| `living_dining` | 1 | 客厅与餐厅共享空间 |
| `kitchen` | 1 | 右侧狭长厨房 |

### 4.3 墙体 `walls`

墙体保存建筑中心线、类型和厚度：

```json
{
  "id": "wall_kitchen_01",
  "kind": "interior",
  "start": {"xMm": 7060, "yMm": 2680},
  "end": {"xMm": 7060, "yMm": 4500},
  "thicknessMm": 100
}
```

约束如下：

- `kind` 为 `exterior` 或 `interior`；
- 当前 MVP 只接受水平或垂直墙段；
- `start` 与 `end` 不得相同；
- 同一位置不保留墙体两侧的重复检测线；
- 家具边、尺寸线和门扇不得进入 `walls`；
- 门窗洞口范围不得再存在贯穿墙段。

### 4.4 门窗 `openings` 与拆墙规则

门窗是独立语义对象，不是墙线。每个开口保存它所在的轴向、真实线段、宽度以及关联
房间：

```json
{
  "id": "door_kitchen",
  "type": "door",
  "wallAxis": "vertical",
  "segment": {
    "start": {"xMm": 7060, "yMm": 4500},
    "end": {"xMm": 7060, "yMm": 5300}
  },
  "widthMm": 800,
  "roomIds": ["room_living_dining", "room_kitchen"]
}
```

墙体必须在开口两端拆分。例如厨房隔墙和门洞不是一根完整墙叠加一个门模型，而是：

```text
墙段：(7060, 2680) → (7060, 4500)
门洞：(7060, 4500) → (7060, 5300)
墙段：(7060, 5300) → (7060, 6060)
```

这样可以保证 Blender 中真实留空，Depth、Semantic 和 Edge 控制图也不会在门洞位置
生成错误的实墙。窗洞同样拆墙，再由渲染层创建窗框、玻璃和窗台；不能先建完整墙再
仅把窗户贴在墙面上。

### 4.5 家具 `furniture`

家具保存语义类型、中心点、占地和高度、旋转角度及所属房间：

```json
{
  "id": "sectional_sofa",
  "type": "sectional_sofa",
  "center": {"xMm": 2850, "yMm": 4150},
  "size": {
    "widthMm": 2300,
    "depthMm": 1450,
    "heightMm": 850
  },
  "rotationDeg": 0,
  "roomId": "room_living_dining"
}
```

这些对象是可替换的三维资产锚点，不表示已识别出原图家具的精确品牌。Blender 可用
程序化代理或 GLB 资产实现相同的中心、占地和朝向，但不得用模板重新计算布局。

黄金样例的 15 个家具锚点覆盖两张床和床头柜、浴缸与洁具、L 形沙发、茶几、电视柜、
餐桌和狭长厨房柜体。餐椅、床品等由渲染资产派生的子部件不计入锚点数量。

## 5. 黄金样例适配器

### 5.1 当前实现

V0.4 本地 MVP 对上传文件的原始字节计算 SHA-256。只有摘要精确等于已审核样例时，
才加载对应的语义 fixture：

```text
SHA256(upload bytes)
→ fixtures/floorplans/{sha256}.json
→ semanticLayout
```

当前注册摘要：

```text
0c9e270682e512189065dfdfd584a3bbe67629a06691bad080fcc7959c7002a9
```

这一策略的价值是确定性和安全性：只有人工审核过且字节完全一致的图，才能套用该
户型数据。相似图片、截图、重编码 JPEG、裁切图或其他户型不会误用这份布局。

### 5.2 它不是通用 AI

SHA 匹配只是本地 MVP adapter，不是图像理解模型，也不能证明系统已经具备通用户型
重建能力。未知图片返回 `semanticLayout = null`，继续使用兼容的普通识别流程；界面
必须明确它没有进入 V0.4 黄金语义模式。

通用版本仍需独立建设：

```text
图框与比例识别
→ 墙体分割和中心线矢量化
→ 门窗／房间／家具检测
→ 拓扑闭合与尺度求解
→ 置信度和人工校正
→ semanticLayout
```

在这些能力达到验收标准前，不能用“视觉相似度匹配”自动套用已有 fixture。

## 6. 结构优先流水线

### 6.1 分析阶段

1. 保存上传原图并计算 SHA-256；
2. 查询已审核的语义 fixture；
3. 命中时返回 `schemaVersion: "0.4"`、`semanticLayout` 和由语义墙转换的校正线；
4. 前端将语义房间和墙体按毫米到像素的映射叠加到原图；
5. 默认目标房间使用 `living_dining`，而不是根据候选线猜测矩形；
6. 未命中时返回兼容分析结果，不冒充语义重建。

### 6.2 场景编译阶段

```text
semanticLayout
├─ rooms      → 分区地面与空间标签
├─ walls      → 按真实长度、厚度和高度建墙
├─ openings   → 门洞、窗洞、门窗资产
└─ furniture  → 按中心、占地、角度创建代理或资产
```

语义模式下禁止根据总宽度再次选择 `studio`、`one_bedroom` 或 `two_bedroom` 固定模板。
模板仅作为旧请求的兼容回退。

### 6.3 相机与基础渲染

- 使用高位正交轴测相机显示完整户型；
- 相机侧墙体使用剖切高度，避免遮挡室内；
- 背景墙保留足够高度以表达房间边界；
- 所有家具必须使用同一毫米坐标转换；
- 输出基础 RGB 与同相机 Depth、Normal、Semantic、Edge；
- 先交付可检查的基础图，不把扩散模型候选当作结构验证工具。

## 7. 基础图质量门必须早于 ComfyUI

ComfyUI 在本机可能运行数分钟。若 Blender 基础图已经出现错房间、错墙、家具挤压，
继续增强既浪费算力，也会让错误变得更难发现。因此最终流程必须是：

```text
语义契约校验
→ Blender 基础渲染
→ 基础结构质量门
   ├─ 通过：生成控制图并进入 ComfyUI
   └─ 失败：停止，返回诊断，不启动 ComfyUI
```

基础质量门至少检查：

1. **契约完整性**：房间、墙体、开口和家具 ID 唯一，引用的 `roomId` 存在；
2. **尺度一致性**：坐标均在 `0..widthMm`、`0..depthMm` 内；
3. **开口一致性**：门窗线段与所在墙同轴，且开口范围没有贯穿墙；
4. **房间拓扑**：房间多边形闭合，关键房间数量和相对位置正确；
5. **家具归属**：家具中心及主要占地位于所属房间，不穿越实体墙；
6. **相机完整性**：户型外轮廓和关键家具未被裁切；
7. **人工视觉确认**：黄金样例的五个功能区、L 形沙发、电视、餐区、厨房和卫浴方向与原图一致。

V0.4 MVP 可先采用确定性契约检查和人工基础图确认。后续再加入房间 Polygon IoU、
墙体投影覆盖率、门洞召回率、家具 Footprint IoU 等自动指标。

## 8. ComfyUI、ControlNet 与参考图的职责

### 8.1 Depth ControlNet

Depth 来自已经通过基础质量门的 Blender 相机。它约束空间层次、墙体、家具大体积和
相机，不负责发现原平面图中的房间或家具。若基础 Depth 错误，ControlNet 只会更稳定
地保留错误结构。

### 8.2 ArchViz checkpoint 与 Refiner

- ArchViz checkpoint 负责室内材质、真实家具外观和光照表达；
- SDXL Refiner 负责低强度细节修整；
- 两者不得被当作几何修复器；
- 增强强度越高，越需要最终结构质量门和人工确认。

### 8.3 IP-Adapter 参考图

`output/imagegen/gpt-image.png` 可以作为目标视觉档次与室内风格参考，但其作用必须
限定为：

- 色彩和材质倾向；
- 家具造型语言；
- 灯光、阴影和渲染质感；
- 轴测室内效果图的整体表现。

它不能替代 `semanticLayout`，也不能作为当前场景的墙体、房间或家具坐标来源。
应使用偏风格迁移的 IP-Adapter 节点，降低参考图构图污染；即使如此，仍可能发生少量
布局泄漏，因此最终结果必须与当前 Blender 基础图比较，而不是与参考图比较。

## 9. 请求与响应

场景请求示例：

```json
{
  "schemaVersion": "0.4",
  "sourceImageUrl": "/artifacts/floorplan-source.jpeg",
  "imageWidth": 1075,
  "imageHeight": 737,
  "planWidthMm": 8150,
  "planDepthMm": 6060,
  "ceilingHeightMm": 2800,
  "wallThicknessMm": 100,
  "walls": [],
  "semanticLayout": {
    "version": "0.4",
    "profileId": "cn_two_bedroom_8150x6060_v1",
    "plan": {"widthMm": 8150, "depthMm": 6060},
    "rooms": [],
    "walls": [],
    "openings": [],
    "furniture": []
  },
  "renderQuality": "base",
  "enableEnhancement": false,
  "useBlender": true
}
```

推荐分两次提交：

1. `renderQuality: "base"`、`enableEnhancement: false`，检查结构；
2. 基础图通过后再提交 `renderQuality: "final"`、`enableEnhancement: true`。

响应必须保留场景摘要、基础图、控制图、增强供应方和质量检查信息。语义模式还应报告
`profileId`、房间数、墙段数、开口数和家具锚点数，便于定位是否误走旧模板路径。

## 10. 验收标准

### 10.1 黄金样例语义验收

对未修改的 `example/平面图.jpeg`：

| 指标 | MVP 目标 |
|---|---:|
| 语义 profile | `cn_two_bedroom_8150x6060_v1` |
| 计划尺寸 | 8150 × 6060 mm |
| 房间 | 5 |
| 拆洞后墙段 | 17 |
| 门窗开口 | 9 |
| 家具锚点 | 15 |
| 未知图片误命中 fixture | 0 |
| 门窗范围内贯穿实墙 | 0 |

### 10.2 基础图验收

- 两间卧室位于上方，卫生间位于右上；
- 客餐厅位于左下和中下，厨房为右侧狭长空间；
- 两张床、L 形沙发、电视柜、茶几、餐桌、浴缸和狭长厨柜方向正确；
- 不出现家具集中在小矩形内、厨房侵入客厅或墙体穿门；
- 相机完整包含户型，空间比例不产生明显横纵压缩；
- 基础图未通过人工确认时，ComfyUI 调用次数为 0。

### 10.3 最终增强验收

- 输出尺寸与基础图一致；
- 最终图和基础图使用同一相机与构图；
- 现有图像边缘保留率 `edgeRetention >= 0.72`；
- `layoutDriftScore = 1 - edgeRetention`；
- 未通过质量门的结果不得无提示替代基础图；
- 写实重绘模式保留候选时，必须标记需要人工确认；
- 视觉效果向参考图的材质和光照档次靠拢，但不能以改变房间数量、门窗位置或家具布局为代价。

`edgeRetention` 只能验证图像边缘近似保留，不能证明房间语义完全正确。因此必须和
语义计数、基础图检查及后续 Polygon／Footprint 指标共同使用。

## 11. 已知限制与风险

1. **只覆盖一个精确样例**：图片被重新保存后 SHA 会改变，语义 fixture 不会命中；
2. **语义数据由人工审核**：当前精度来自 fixture，不代表自动识别精度；
3. **矩形房间优先**：不规则墙、弧墙、斜墙和跃层尚未覆盖；
4. **家具是锚点**：程序化代理和目标真实家具外观仍有差距；
5. **碰撞检查有限**：家具中心在房间内不等于通道和人体工学合格；
6. **门窗细节有限**：开启方向、窗台高度和构造做法可能仍需人工补充；
7. **参考图可能泄漏构图**：IP-Adapter 权重过高会带入参考图布局；
8. **结构边缘指标有限**：纹理边缘、家具替换和抗锯齿会影响分数；
9. **本机生成较慢**：Apple MPS 上 Base + IP-Adapter + Refiner 可能运行数分钟，前端必须使用长轮询并显示真实任务状态；
10. **不是施工依据**：所有结果用于概念设计和视觉验证，尺寸和构造需由专业人员复核。

## 12. 后续演进

### V0.4.1

- 为基础图增加自动语义质量报告；
- 把墙段、开口和家具锚点计数写入 Manifest；
- 增加家具包围盒与墙体碰撞检查；
- 在前端提供“基础结构已确认”后再启用最终增强。

### V0.5

- 墙体／门窗／家具分割模型与 OCR；
- 矢量吸附、共线合并、拓扑闭合和尺度求解；
- 房间类型识别和置信度展示；
- 低置信对象的人工补画、删除、分类和尺寸编辑；
- 多黄金样例回归集，而不是单一 fixture；
- 房间 Polygon IoU、墙体覆盖率、门窗召回率和家具 Footprint IoU。

### V0.6

- 可检索的真实家具 GLB／材质资产库；
- 资产级碰撞与通道检查；
- 多相机一致渲染；
- 设计版本、人工修改历史和可追溯 Manifest；
- IFC／BIM 等工程数据导出评估。
