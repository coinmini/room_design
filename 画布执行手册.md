# 无限画布改造 · 夜间自动执行手册

> 本文件是无限画布改造的唯一权威执行规范。每次开工必读。
> 配套文件：`无限画布改造方案.md`（方案全文）、`画布执行进度.md`（进度状态）。
> 所有决策已拍板（见第 2 章），执行中**不要**停下来问问题。
>
> **执行方式（2026-08-05 09:20 起）**：主会话顺序执行，用户在场。
> 夜间定时任务（automation_2dbbb178-45d3-4290-afce-44bb202519fc，每小时）已完成 W0-a ~ W0-f 并**已停用**。
> 当前由主会话按本手册逐项推进，每完成一项：全量 pytest + 前端 build + 重启 API + commit + push + 更新进度文件。

---

## 1. 环境与铁律

- 仓库：`/Users/bolin/Documents/AI/room_design`，分支 `agent/v0.2-floorplan-scene`，推送 `origin`
- 后端：`apps/api`（FastAPI），测试 `cd apps/api && .venv/bin/python -m pytest`
  - **基线：136 passed / 4 failed**。4 个已知失败（改动前已存在，不是回归）：
    `test_api.py::test_v04_floorplan_to_scene_flow_with_local_fallback`、`test_white_model_ai.py` 的
    `test_white_model_audit_uses_raw_pixel_space_and_rejects_drift`、`test_ai_quick_is_default_and_uses_three_style_only_references`、`test_ai_quick_failed_audit_keeps_raw_provider_image`
  - 已知隔离缺陷：很多测试文件单独跑会 "no such table"，**必须跑全量**判断回归
  - 新增测试文件请在文件内 `init_db()`（参考 `tests/test_idempotency.py`），避免依赖执行顺序
- 前端：`apps/web`，验证 `cd apps/web && npm run build`
- 数据库：PostgreSQL `postgresql://bolin@localhost/room_design`；`.env` 里用户显式设了 `KUYAO_IMAGE_TIMEOUT_SECONDS=900`
- **后端改动后必须重启 API**（2026-08-05 起改用项目 venv，conda 环境的 Pillow 已损坏）：
  `pkill -f "uvicorn app.main:app"; sleep 1; cd apps/api && nohup .venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --timeout-graceful-shutdown 15 > /tmp/room_design_api.log 2>&1 & sleep 4; curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/docs`（期望 200）
- 需要真实 provider 生成测试图验证是**允许的**（用户已授权）； provider 当前延迟 125~216s/张
- 提交规范：`git commit -F 消息文件`（消息含单引号会破坏 heredoc，所以写文件）；提交信息写清 W 编号 + 内容
- **绿态铁律：每次运行结束时，HEAD 必须测试通过（136/4 基线不恶化）+ 前端 build 通过。做不完一个子项时，先提交能过测试的增量，绝不留下破损状态**
- 产出文件只允许放在工作区仓库内
- 每完成一个子项：提交推送代码 → 更新 `画布执行进度.md`（勾选、commit hash、一句话备注）→ 再次提交推送

---

## 2. 已拍板的决策（不要重开）

- **D1 variant 粒度审批/分叉（W0-X）**：**做，但排在最后**——W6 完成且全绿后才开始；此前对已有下游的节点"改批"入口置灰并说明原因，不让用户撞 409
- **D2 画布引擎**：**先 spike `@xyflow/react`**（限 0.5 天当量）：验证 React 19.2 兼容 + 自定义节点塞现有卡片 + 深色 CSS 变量覆盖。成功就用它；失败立即回落自研 DOM + CSS Transform `CanvasViewport`，并在进度文件记录 spike 结论
- **D3 深色主题**：**作用域化**——新画布/新页面用深色 token（背景 #0a0a0d、卡片 #17181c、边框 rgba(255,255,255,.08)、主色 #3b82f6、点阵背景、卡片圆角 12px、悬浮栏胶囊），**不做**全站 359 个 hex 审计；「01 编辑器 + AssetLibrary 亮暗接缝」登记为已知问题
- **D4 多画布**：**直接做完整版**（`canvases` + `canvas_nodes` 双表），但「画布 1 ▾」选择器 UI 先不做（v2）
- **D5 视觉能力表裁决**：
  - 必做：深色+点阵、节点卡片+贝塞尔连线、右键菜单+快捷键（带 `isEditableTarget()` 守卫）、节点浮动操作条、小地图、缩放百分比、底部悬浮工具栏、左下资产管理入口、首页（左侧 56px 图标导航 + 问候语 + 大输入框接 `design_prompt` + 新建项目卡片 + 最近项目横排）
  - 降级：模型级联 → 静态 pill（显示 `/health` 配置的模型名）；精选案例瀑布流 → 用用户自己的资产缩略图喂；提示条文案按我们的交互写（08 是手绘闭合红圈，**不照抄**"点击图片上的物体进行标记"）
  - 不做：⚡点数/会员/限时折扣/赛事/风暴学堂/PS插件/合伙人/邀请有礼等全部 SaaS 计费社区外壳；节点四角缩放手柄；「优化工作流布局」菜单项；「画布 1 ▾」UI（v2）

---

## 3. 执行顺序与验收标准

> 严格按顺序。每项的详细技术背景见 `无限画布改造方案.md` 第 3、5、6 章。

### W0-a 查询解锁（先做）
- `/v1/assets` 加 `projectId` 查询参数，过滤下推 SQL（修掉 Python 侧过滤 + offset 语义错误）
- `backfill_scene_assets` 移出请求热路径（改为启动时执行一次 + 显式 POST 端点）
- 新增 `GET /v1/jobs?projectId=&status=` 列表端点
- 验收：新参数/端点有测试；全量测试绿；重启 API

### W0-b 谱系补齐
- `FLOORPLAN_ANALYZE` 建资产（补 asset_type/deliverables/thumbnail 分支），或合成虚拟根节点——**优先建真资产**
- `/layouts/ai`、`/floorplan-scenes` payload 补 `asset_parent_id`；03/04/05 的 `asset_parent_id` 从 `Form(None)` 改必填（前端对应字段从折叠手输框改为自动携带）
- 历史数据回填脚本（`tools/` 下，可重复执行）
- 验收：新建任务的 parent_asset_id 不再为 NULL；测试绿

### W0-c 缩略图
- 后端生成 256/512 webp 写 `thumbnail_url`，原图另存 `fullUrl`；`/artifacts` StaticFiles 加 Cache-Control；存量回填
- 验收：新产物有 webp 缩略图；测试绿

### W0-d 画布持久化
- 新表 `canvases(id, project_id, name, viewport_json, updated_at)` + `canvas_nodes(id, canvas_id, asset_id, variant_id, job_id, x, y, w, h, z, source_node_id, deleted_at)` + CRUD + 批量坐标 PATCH
- 删除/副本/粘贴落 canvas_nodes 软删，`SceneAsset` 保持不可删
- 迁移方式照 `database.py` 现有 shim（`create_all` 对新表零成本；既有表加列手写 shim，sqlite/postgres 双分支）
- 验收：CRUD 测试；全量绿

### W0-e 逐图展开器
- 新文件 `app/canvas.py`：统一「资产 → 逐图变体」服务，覆盖 ai_workflow/layout/floorplan/white_model/material 五种 deliverables 形状
- layout 的 `previewUrls` 与 `metadata.variantIds` 数组下标隐式配对改成结构化配对
- 验收：五种模块各有展开测试

### W0-f 部分结果可见
- 取消/失败 job 的 partial outputs 作为临时节点出现在画布数据里（或 `ensure_scene_asset` 接受 CANCELED-with-partial 并打不可审批标记）
- 验收：取消中途批次后，已生成输出仍可查

### W0-g 后端测试修复
- `test_assets.py` / `test_ai_workflow_derivatives.py` / `test_workflow_resume.py` 同步到 W0 新行为
- 验收：全量绿（136/4 基线 + 新增用例）

### W1 设计令牌化（作用域）
- 新建 `apps/web/src/canvas/theme.css`（或等价）：深色 token 一套；画布与新页面引入
- 验收：build 通过

### W2 画布引擎（拆成 3 个子项，每次运行只做 1 个）
- **W2-1 spike**：`npm i @xyflow/react`，建一个最小页面验证 React 19.2 兼容 + 自定义节点渲染现有卡片 + 深色 CSS 变量覆盖；结论写进度文件。失败则后续子项改用自研 DOM+CSS Transform `CanvasViewport`
- **W2-2 画布壳**：新标签页挂画布——点阵背景、缩放平移、小地图、缩放百分比、自动分列布局（按 workflowStage 分列、变体纵向）、贝塞尔连线，数据来自 `GET /v1/projects/{id}/canvas-graph`（W0-e）；存量资产 project_id 全为 NULL——默认项目策略：前端用一个固定本地项目 id，或图谱端点对无项目资产做回退（实现时任选其一并在进度文件注明）
- **W2-3 节点卡片**：缩略图 + 阶段标签 + 审批徽章（✓ 已批准/基准）+ 骨架节点（增量发布中的 job 轮询态）
- 验收：浏览器里能缩放平移看到全项目谱系；build 通过

### W3 向导状态层重构（拆成 3 个子项，必须先于 W4）
- **W3-1 纯函数模块**：把生成/审批/设基准从 `AiDesignWorkflow.tsx` 抽成不依赖 `activeStage` 的模块（入参显式传 sourceAssetId/variantId/versionId/spaceId），向导改为调用它——本步不改并发模型
- **W3-2 并发模型**：`jobs` 改 `Map<nodeId, Job>`；`busyStage` 改 `Set<jobId>`（修 `finally` 解锁 bug）；`AbortController` 改 Map
- **W3-3 baseline 派生化**：三个 baseline 单值槽改节点派生值；设基准不再删下游 job、不再跳阶段
- 验收：原向导 8 阶段全流程功能不回归（手动冒烟 + build）

### W4 节点操作条 + 右键菜单（拆成 2 个子项）
- **W4-1 操作表与可用性**：`canRunAction(node, action)` 纯函数（严格按 `main.py:648-672` 父类型约束，方案第 6 章）+ 节点浮动操作条（非法项置灰+说明）
- **W4-2 右键菜单与快捷键**：深色右键菜单 + 快捷键分发器（带 `isEditableTarget()` 守卫）+ 409 时节点渲染「上游已变更，点此重新绑定基准」
- 验收：画布上能完成 批准/设基准/重试/局部修改入口跳转；build 通过

### W5 前端测试接线
- vitest + playwright（或仅 vitest）config + `npm test` 脚本；覆盖视口数学、`${jobId}:${variantId}` 节点身份、job→asset 身份交接
- 验收：`npm test` 绿

### W6 首页改版（拆成 2 个子项）
- **W6-1 路由与骨架**：引入 react-router-dom；左侧 56px 图标导航；深色首页骨架；后端 `Project.updated_at` + 封面列（+迁移 shim）
- **W6-2 首页内容**：问候语 + 大输入框（接已有 `design_prompt` 字段）+ 新建项目卡片 + 最近项目缩略图横排（用户自己的资产喂）；模型选择降级为静态 pill
- 验收：首页可见可用，点击进入项目画布；build 通过

### W0-X（仅当以上全部完成且全绿）
- variant 粒度审批/分叉，按方案第 3 章 W0-X 与第 6 章 D1 执行

---

## 4. 卡死处理与步数预算

- **步数预算（新增教训）**：单次运行最多约 100 个工具步（maxSteps 硬顶，W0-e 曾撞顶失败）。
  大件已拆小（见第 3 章 W2/W3/W4/W6 的子项划分）；每次运行**只做 1 个子项**，
  进入实现前先列好本项的文件清单再动手，避免在探索中烧掉步数
- 遇到无法自行解决的硬阻塞（缺凭据、需求矛盾到无法判断）：在 `画布执行进度.md` 把该项标记 `blocked: 原因`，**结束本次运行**，不要反复重试烧 token
- 每次运行开始先 `git pull --rebase origin agent/v0.2-floorplan-scene`（防冲突）；结束必须 push
- 每次运行只做 1 个子项；宁可少做，不可破绿
