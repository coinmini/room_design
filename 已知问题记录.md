# 已知问题记录

> 记录日期：2026-08-05
> 来源：W0-e 执行过程中发现的两个环境/数据问题

---

## 问题 1：conda 环境 llf_v1 的 Pillow 损坏（libjpeg 符号缺失）

### 现象

2026-08-05 上午重启 API 时进程起不来，`/tmp/room_design_api.log` 报错：

```
File ".../llf_v1/lib/python3.12/site-packages/PIL/Image.py", line 103, in <module>
    from . import _imaging as core
ImportError: dlopen(.../PIL/_imaging.cpython-312-darwin.so, 0x0002):
  symbol not found in flat namespace '_jpeg_resync_to_restart'
```

### 根因

- conda 环境里的 Pillow 是 **9.5.0**，其 C 扩展 `_imaging.cpython-312-darwin.so` 依赖环境内的
  libjpeg 动态库；当前 `llf_v1/lib/` 下**已没有 libjpeg**（`otool -L` 也查不到有效链接），
  `_jpeg_resync_to_restart` 是 libjpeg 导出的符号——环境内某个包后来升级/移除时把 libjpeg
  带走了，Pillow 的编译产物就变成了"缺符号的死库"（典型的 conda 环境腐化）
- 这个问题在 W0-c（2026-08-05 凌晨）之前不暴露：`app/assets.py` 此前不 import PIL；
  W0-c 引入 `app/thumbnails.py`（`from PIL import Image`）后，import 链
  `main.py → assets.py → thumbnails.py → PIL` 一启动就炸

### 为什么凌晨 W0-c/W0-d 重启时是好的

夜间运行时 conda 环境的 libjpeg 还在（或进程没退出、沿用旧内存映像）。
上午 `pkill` 后重新冷启动进程，import 才触发符号解析失败。**这类"运行中无恙、重启即炸"
的问题只有冷启动能暴露。**

### 当前处置（已生效）

- API 重启命令已从 conda python 切换为**项目自己的 venv**：
  `apps/api/.venv/bin/python -m uvicorn app.main:app ...`（Pillow 12.3.0，正常）
- `画布执行手册.md` 第 1 章的重启命令已同步更新，夜间定时任务也走 venv
- 测试本来就跑在 `.venv`（`pytest` 全程 161 passed），不受影响

### 后续建议（不急）

- 若还想保留 conda 环境跑 API：`llf_v1` 里 `pip install --force-reinstall pillow`（或 conda 重装
  `libjpeg-turbo` + Pillow）。但既然项目 venv 完整可用，**建议直接以 `apps/api/.venv` 为
  唯一运行环境**，conda 环境弃用，少一个会腐化的依赖源
- 经验：重启类操作后必须 `curl /docs` 验证 200 再算完成（手册已含此步，本次正是这一步抓到的）

---

## 问题 2：存量 156 个资产的 project_id 全为 NULL

### 现象

W0-e 的 `GET /v1/projects/{project_id}/canvas-graph` 端点上线后，用真实库冒烟发现：
无论传哪个项目 id，返回都是空的。查库确认：

```sql
SELECT project_id, COUNT(*) FROM scene_assets GROUP BY project_id;  -- (None, 156)
SELECT project_id, COUNT(*) FROM jobs GROUP BY project_id;          -- (None, 177)
```

**全部 156 个资产、177 个任务的 project_id 都是 NULL。**

### 根因

- 前端的项目 ID 是一个**手输文本框**（`AiDesignWorkflow.tsx` 里折叠在
  「项目与版本谱系（可选）」details 中），用户日常留空
- 建任务端点的 `project_id` 是 `Form(None)` 可空；资产行的 `project_id` 直接继承自 job
- 所以历史上没有任何数据带项目归属——这在"单人本地单项目"用法下无所谓，
  但画布是**按项目组织**的（一个项目 = 一张画布），按项目过滤自然全空

### 影响面

- `GET /v1/projects/{id}/canvas-graph`（W0-e）：对存量数据返回空图谱
- `/v1/assets?projectId=...`（W0-a）：同理
- W0-d 的 canvases/canvas_nodes 双表按 project_id 归属，同样受影响
- **注意**：这不影响现有 8 阶段向导——向导的资产选择器不按项目过滤

### 当前处置

- 已在 `画布执行进度.md` 的「已知事项」登记
- 端点行为本身正确（空项目返回空图谱），不动

### 后续方案（W2-2 落地时二选一，已写入手册）

1. **默认本地项目**：前端固定使用一个本地默认项目 id（如 `project_local_default`），
   新建任务自动带上；存量 177 个 job + 156 个资产写一次性回填脚本归到该项目下
   （jobs.project_id 与 scene_assets.project_id 同时回填，资产也可经 parent 链推导）
2. **空项目回退**：canvas-graph / assets 查询在目标项目无数据时回退到"全部本地资产"

倾向方案 1（显式归属比隐式回退干净，回填一次永逸），最终由 W2-2 执行时在进度文件注明选择。
